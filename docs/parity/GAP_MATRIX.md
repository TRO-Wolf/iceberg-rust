<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# Java ↔ Rust Capability Gap Matrix

> **Goal:** 1-to-1 capability parity between this Rust implementation and the Apache Iceberg
> **Java `iceberg-core` / `iceberg-api`** library (the engine-agnostic table-format library — *not*
> the Spark engine integration). This is a **living document**: re-run the audit after every upstream
> sync and after each parity phase lands.

## Status legend

- ✅ **present** — implemented to a usable degree
- 🟡 **partial** — exists but incomplete vs Java
- ❌ **missing** — not implemented

> **Two ✅ bars — read the count accordingly (clarified 2026-06-16).** Per the parity mandate
> ([CLAUDE.md](../CLAUDE.md)), a row the FORK actively flips to ✅ requires unit tests **and** a
> bidirectional Java interop round-trip. **Base-inherited upstream primitives** (data/eq-delete/partitioning
> writers, the parquet reader, FileIO, transforms, name-mapping, `upgrade_format_version`, …) carry their ✅
> on the weaker "implemented + usable" bar and are **exempt from the fork's interop requirement** — they
> were proven upstream, not re-interop'd here. So the headline ✅ count is **not** uniformly an
> "interop-proven" count; ~half of the greens are fork-flipped-with-interop and the rest are
> inherited-and-usable. When a base-inherited ✅ row has an un-proven slice under the strict bar, the cell
> names it (e.g. `upgrade_format_version`).

## Audit provenance

- **Rust base audited:** owned fork on upstream **`iceberg` 0.9.1** (datafusion 52.2, arrow 57.1,
  parquet 57.1, MSRV 1.92), **re-audited 2026-06-07** after the Phase 0 sync, **resynced to reality
  2026-06-13** (post R1/R2/R3): status flips (`merge_append` 🟡→✅ data-level interop both directions;
  `LockManager` 🟡→❌ ZERO code; `Catalogs` umbrella 🟡 — Glue/S3Tables view ops + Catalog accessors
  residue surfaced), partition-stats de-dup (one home on the Maintenance row), and 10 new rows for
  previously-untracked Java capabilities (conflict-detection/`caseSensitive` builders,
  `deleteFromRowFilter`, `ScanTaskGroup`/`planTasks`, `ExpressionParser` JSON, aggregate expressions,
  Catalog accessors, `RewriteFiles` add-delete, `ActionsProvider`+actions, a consolidated LOW backlog).
  Test count measured 2026-06-13: **2,238** `#[test]`/`#[tokio::test]` in `crates/iceberg/src`, **2,332**
  incl. `crates/iceberg/tests`, **≈2,721** workspace-wide (`grep -rho '#\[\(tokio::\)\?test\]'`). Source
  of truth: `crates/iceberg/src/{spec,expr,scan,transaction,writer,arrow,io,inspect,puffin,catalog,maintenance,variant}`
  plus the `crates/storage/opendal` FileIO crate and `crates/catalog/{glue,s3tables}`.
- **Java reference:** `apache/iceberg` `main`, modules `api/` + `core/` + `data/` + `orc/` +
  `parquet/` + `arrow/`.
- **Re-audit policy:** re-run after every upstream sync and after each parity phase; date-stamp this
  block and strike the rows the sync/phase solved.
- **Interop oracle (test-only):** `dev/java-interop/` drives Java `iceberg-core` **1.10.0** as a
  read/write oracle for bidirectional fixtures (not shipped, not a Cargo dependency). One
  `generate`/`verify` pass now covers all THREE Phase-1 evolution capabilities — it backs the
  ✅-by-interop rows `UpdateSchema`, `UpdatePartitionSpec`, and the `ManageSnapshots` ref-operation surface
  (incl. "Snapshot model + refs"), all dated 2026-06-07.
- **What the 0.7→0.9.1 sync changed (flipped rows below):** `timestamp_ns` type, column default
  values (`initial_default`/`write_default`), merge-on-read **read** application of position-deletes +
  deletion-vectors during scan *(2026-06-10 audit correction: the sync brought PARQUET
  position/equality deletes only — the deletion-vector scan path was a loader TODO until
  Increment D1; see the merge-on-read read row)*, the `upgrade_format_version` transaction action, and a real
  `TransactionAction`/`ApplyTransactionAction` extension seam. The **headline gaps are unchanged**:
  write-engine actions, schema/partition/snapshot evolution, incremental scans, ORC/Avro data files,
  variant/geo/unknown types, catalog view ops, maintenance actions, encryption.
- **Cell style (de-triplication pass, 2026-06-10):** this matrix is the ONLY status record (the
  one-home-per-fact rule in [CLAUDE.md](../../CLAUDE.md)). Cells stay TERSE — Rust location, a 1–2
  sentence summary, flip dates, links. The full per-increment narratives the cells used to carry
  moved verbatim to [archive/2026-06_matrix-cell-narratives.md](archive/2026-06_matrix-cell-narratives.md)
  (grep by row Area). When a status flips: update the cell here, link evidence, write the narrative
  in the todo/lessons flow — do NOT grow the cell back.

## Matrix

| Area | Status | Java reference | Rust location / note |
|---|---|---|---|
| Primitive + nested types | ✅ | `api/.../types/Types.java` | `spec/datatypes.rs`. **Type-name case fold (O3-a, 2026-06-12):** `PrimitiveType` deserialize lowercases the name (incl. `decimal(..)`/`fixed[..]`) + the variant marker folds case, matching Java `Types.fromTypeName`'s `toLowerCase(Locale.ROOT)` (1.10.0 bytecode); object WRAPPERS `struct`/`list`/`map` are NOT folded (Java `SchemaParser.typeFromJson` uses `String.equals`) — scoped, mixed-case + negative pins. **O3 REVIEWER closures (2026-06-12, live-Java-probed):** (1) `fixed[..]`/`decimal(..)` parse now mirrors Java's anchored regex exactly — inner whitespace (`fixed[ 16 ]`, `decimal( 38 , 2 )`) ACCEPTED, missing close bracket/paren (`fixed[16`, `decimal(38,2`) REJECTED (was: opposite both ways via `trim_end_matches`); (2) the `Type` read path now rejects wrong-cased WRAPPER names (`{"type":"STRUCT"/"LIST"/"MAP"}`) via `SerdeType::wrapper_type_mismatch` re-imposing Java's `String.equals` — closes the read-leniency the builder documented (the untagged enum had ignored the `type` string). Both bytecode + live `Types.fromTypeName`/`SchemaParser.fromJson` oracle-confirmed, mutation-pinned. |
| V3 types: variant | 🟡 | `api/.../variants/`, `core/.../variants/`, `api/.../types/Types.java` (`VariantType`) | `variant/` — binary format BOTH sides: READ (2026-06-11; metadata dict, all 21 primitive ids, short strings, objects, arrays; bounds-checked) + WRITE (B2 2026-06-11; `Variants` factories, shredding, Avro `metadata`+`value` shape with `r<fieldId>` naming). Stays 🟡 on the shredded-parquet FILE I/O + file-level interop, blocked at the pinned parquet 57. [full narrative](archive/2026-06_matrix-cell-narratives.md#v3-types-variant) |
| V3 types: geometry / geography | ❌ | `api/.../geospatial/` | none |
| V3 types: timestamp_ns | ✅ | `types/Types.java` | `spec/datatypes.rs` (`PrimitiveType::TimestampNs`/`TimestamptzNs`); **V3-only format-version gate enforced** — `Schema::check_compatibility` rejects a `timestamp_ns`/`timestamptz_ns` field (incl. nested) on a V1/V2 table (Java `Schema.MIN_FORMAT_VERSIONS`, "Invalid type for {col}: timestamp_ns is not supported until v3") |
| V3 types: unknown | ✅ | `types/Types.java` (`UnknownType extends PrimitiveType`) | `spec/datatypes.rs` `PrimitiveType::Unknown` (Java PRIMITIVE arm, not a top-level `Type` like variant), JSON serde = bare `"unknown"` for free via `rename_all="lowercase"` + Display, case-folded read. **V3 gate (G2, 2026-06-17):** same `min_format_version`/`check_compatibility` mechanism as `timestamp_ns`/`variant` (shared-gate-pinned; a V2 schema carrying `unknown` is REJECTED, "Invalid type for {col}: unknown is not supported until v3"; mutation-verified — removing the arm reds 3 tests). Compiler-forced match arms: arrow→`DataType::Null` (Java `TypeToMessageType` = null, no physical column), avro→Avro `null` (`TypeToSchema`), datum/literal→reject (no `PrimitiveLiteral` — always-null), value-producing transforms reject via the existing catch-all (identity/void accept = Java `Identity.UNSUPPORTED_TYPES` excludes UNKNOWN), partition-stats/readable-metrics loud-error, glue/hms schema → loud `FeatureUnsupported` (no Hive type, Java throws). **Deferred-loud:** data-file always-null write/read I/O (`arrow/value.rs` `FeatureUnsupported` — the all-null synth read/write is a deeper change; metadata round-trip does not reach it). **Interop ✅ 2026-06-17** — metadata-only schema round-trip via `crates/iceberg/tests/interop_unknown.rs` (env-gated GEN; Java `UnknownTypeOracle` writes a V3 schema with `unknown` at every placement, Rust reads+writes, Java verifies the Rust-written struct; `dev/java-interop/run-interop-unknown.sh`). |
| Column default values (initial/write) | ✅ | `Schema`/`Types` | `spec/datatypes.rs` `NestedField` carries `initial_default`/`write_default` |
| Partition transforms (identity/bucket/truncate/year/month/day/hour/void) | ✅ | `api/.../transforms/` | `spec/transform.rs` |
| Schema evolution (`UpdateSchema`) | ✅ | `api/UpdateSchema.java`, `core/SchemaUpdate.java`, `schema/UnionByNameVisitor.java` | `transaction/update_schema.rs` — full Java `SchemaUpdate` parity: add/rename/update-type/doc/make-optional/require/delete/move/set-identifier-fields, `union_by_name` (full `UnionByNameVisitor`), LEVEL-ORDER fresh field-id assignment, column initial/write defaults, and the V3-only initial-default + type gates (`Schema::check_compatibility` wired into `TableMetadataBuilder::add_schema`). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_schema.rs` (7 scenarios). Full narrative + edge cases: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Partition evolution (`UpdatePartitionSpec`) | ✅ | `api/UpdatePartitionSpec.java`, `core/BaseUpdatePartitionSpec.java` | `transaction/update_partition_spec.rs` — full `BaseUpdatePartitionSpec` parity: auto-naming, field-id+name recycling, V1 void replacement, guard rails; the partition-name↔schema collision guard was relaxed to identity-OR-void (a real divergence interop surfaced and fixed). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_partition_spec.rs` (7 scenarios). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Sort order (`ReplaceSortOrder`) | ✅ | `api/ReplaceSortOrder.java` | `transaction/sort_order.rs`. **Parse-time bind (O3-b, 2026-06-12):** `TableMetadata` normalization now runs `SortOrder.check_compatibility` on the DEFAULT sort order against the current schema (`spec/table_metadata.rs` `try_normalize_sort_order`), matching Java `TableMetadataParser`→`SortOrderParser.fromJson(schema, node, defaultId)` which `bind`s ONLY the default order (`build`→`checkCompatibility`) and `bindUnchecked`s the rest (1.10.0 bytecode); a default order on a missing/non-primitive/wrong-transform source fails to parse, non-default orders stay lenient. |
| Snapshot model + refs (branches/tags) | ✅ | `api/Snapshot.java`, `SnapshotRef.java`, `core/SnapshotParser.java` | Spec types + ref operations (`transaction/manage_snapshots.rs`). **Interop ✅ 2026-06-07** — the `SnapshotReference`/`SnapshotRetention` model round-trips with Java via `crates/iceberg/tests/interop_manage_snapshots.rs`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Snapshot management (`ManageSnapshots`: branch/tag CRUD, rollback, rollback-to-time, set-current, fast-forward) | ✅ (ref-op surface + `cherrypick`/WAP — all interop closed I3 2026-06-12) | `api/ManageSnapshots.java`, `core/SetSnapshotOperation.java`, `core/CherryPickOperation.java`, `api/SnapshotRef.java` | `transaction/manage_snapshots.rs` — branch/tag CRUD + rename, set-current, rollback (ancestry-checked), rollback-to-time (strict `<`), fast-forward, retention (non-positive rejected), no-op suppression. Interop ✅ 2026-06-07 (7 scenarios both directions, `crates/iceberg/tests/interop_manage_snapshots.rs`); `cherrypick`/WAP interop closed I3 2026-06-12. [full narrative](archive/2026-06_matrix-cell-narratives.md#snapshot-management-managesnapshots-branchtag-crud-rollback-rollback-to-time-set-current-fast-forward) |
| Manifest + manifest-list read/write | ✅ | `core/.../ManifestReader/Writer` | `spec/manifest`, `spec/manifest_list.rs`. Spec-mandated default-to-0 reads for V1→V2 fields VERIFIED + mutation-pinned 2026-06-07 (snapshot `sequence-number` lenient read fixed; the Avro siblings were already correct). Table-metadata `last-sequence-number` strict-on-V2/V3 read CLOSED Arc G 2026-06-11: Rust ALREADY matches Java 1.10.0 (bytecode — `TableMetadataParser.fromJson` does `JsonUtil.getLong` for `formatVersion>1`, which THROWS on absent; V1 hard-defaults to 0). `#[serde(default)]` was the WRONG fix (would accept V2 metadata Java rejects). Pinned in `spec/table_metadata.rs`. **W3 comparator groundwork (2026-06-11; sort position RESOLVED 2026-06-12, Opus reviewer):** `partition_spec_id` added to the manifest-level emitted JSON on all three sides (`crates/iceberg/tests/common/snapshot_meta_view.rs` + `interop_expire.rs` + Java `SnapshotMetaOracle`); constant 0 in all existing single-spec fixtures — byte-unaffected. The canonical-view sort tuple now carries `partition_spec_id` as the FINAL tiebreaker (position 10, Option B) symmetrically on all three sides — deterministic for future multi-spec fixtures, byte-invisible for single-spec (all five metadata chains stay green). **Z2 multi-spec interop ✅ 2026-06-12:** proven both directions via `dev/java-interop/run-interop-multi-spec.sh` + `crates/iceberg/tests/interop_multi_spec.rs` — single multi-spec fast-append commit produces TWO manifests with `partition_spec_id` 0 and 1 cross-language; the spec-id tiebreaker (position 10) is the SOLE disambiguator for tie-shaped ms4 manifests (F0.record_count == F3.record_count == 10); 4-sabotage battery proves both the spec-id field and the per-own-spec partition rendering are load-bearing (SB4 swaps the two spec field-definitions in the SOURCE metadata and RE-EMITS — the ms4 partition tuples then render under the wrong-arity spec and the view diverges). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). **Manifest-list ENTRY ORDER — DOCUMENTED DIVERGENCE (O3-c, 2026-06-12, cosmetic, not fixed):** Java assembles new-then-carried with all-DATA-before-all-DELETE (`FastAppend.apply` = `writeNewManifests()` then `snapshot.allManifests`; `MergingSnapshotProducer.apply` = `concat(prepareNewDataManifests, carriedData)` then `concat(prepareNewDelete, carriedDelete)`, 1.10.0 bytecode). Rust's SHARED `SnapshotProducer::manifest_file` emits carried (`process_deletes`, data+delete mixed) FIRST, then new-data, then new-delete — opposite order, in the ONE path every action uses. Manifest-list file order is writer-dependent (not a spec contract): the interop oracle SORTS by a deterministic tuple (`snapshot_meta_view.rs:127`) and both readers reconcile by sequence number, so all interop chains stay green — the divergence is byte-invisible after the canonical sort. Matching Java exactly needs a content-type-separated restructure of the shared assembly path (ripples into `manifests[0]`-indexed tests across rewrite_manifests/merge_append) — out of scope for a bounded cleanup; left as-is. |
| `RewriteManifests` | 🟡 | `api/RewriteManifests.java`, `core/BaseRewriteManifests.java` | `transaction/rewrite_manifests.rs` — re-organize manifests in one `Operation::Replace` snapshot with the live set unchanged (Java `BaseRewriteManifests`, extends `SnapshotProducer`): `cluster_by` re-groups live data-manifest entries via the provenance-preserving `add_existing_entry` path (original snapshot id + seq numbers kept, status → Existing); `rewrite_if`, `add_manifest`/`delete_manifest` (explicit replacement), `set`; `validateDeletedManifests` + `validateFilesCounts`; delete manifests carried forward byte-identical. 17 `MemoryCatalog` tests incl. the provenance re-stamp mutation pin + a merge-on-read delete-still-applies test; reviewer-added pins (2026-06-10): two-level-inheritance on-disk-seq pin (rewritten manifest stores ORIGINAL seqs explicitly, never null ⇒ resurrection-proof; fails under seq-strip mutation), multi-spec clustering axis (constant key over two partition-spec ids ⇒ one manifest per spec, no cross-merge; fails under spec-id-dropped key), and computed-count-vs-user-`set()` precedence (computed counts win, Java last-write-wins; fails under reversed ordering). Reviewer confirmed `changed-partition-count=0` IS emitted on both sides (parity — Java `SnapshotSummary.Builder.build` L200 with default `trustPartitionMetrics`). Adaptations (documented): String cluster key (vs Java `Object`), estimated-length size rolling (`ManifestWriter` has no incremental length), V1 `add_manifest` → `FeatureUnsupported` (Java `copyManifest` deferred), no-current-snapshot → `DataInvalid` (Java NPEs), `scanManifestsWith` not ported. **Metadata-level interop ✅ 2026-06-10 (cluster-by path):** the canonical snapshot-metadata view of the Rust `cluster_by` rewrite matches Java's `clusterBy` byte-for-byte in the eight-step write-actions chain (s6 re-groups the live data entries one manifest per partition, every entry carried Existing with original provenance), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); the add/delete-manifest explicit-replacement mode is NOT covered by the chain. **Landed 2026-06-10; reviewer-verified 2026-06-10; row stays 🟡 (data-level interop open).** |
| Write: fast append | ✅ | `api/AppendFiles.java`, `core/FastAppend.java` | `transaction/append.rs` — Java `newFastAppend`. **All-tombstone-manifest carry FIXED 2026-06-11 (Wave-4 Group O / O1, the A3 STOP-finding):** `FastAppendOperation::existing_manifest` now carries EVERY prior manifest-list entry forward UNFILTERED, matching Java `FastAppend.apply` (`manifests.addAll(snapshot.allManifests(io))`, bytecode offsets 89-94 — NO predicate; `BaseSnapshot.allManifests` returns the list verbatim). The previous `has_added_files()`-OR-`has_existing_files()` filter DROPPED a manifest left ALL-DELETED by a prior copy-on-write delete that emptied it — a manifest-LIST structure divergence on any append-after-emptying-delete history (the dropped manifest became unreferenced/GC-eligible where Java keeps it referenced; bites byte-level interop + tombstone-visibility consumers like orphan-protection / changelog scans). Pinned on-disk: re-parsed-manifest-list fail-before/pass-after reproduction (add → emptying delete → fast_append CONTAINS the all-tombstone manifest), scan-unchanged pin (carry does NOT resurrect deleted rows), merge_append documented-parity pin. Note: the MERGING append (`merge_append`) intentionally keeps the filter — see that row. Interop re-proof deferred (Group S). **Multi-spec fast-append interop ✅ 2026-06-12 (Z2):** a SINGLE `fast_append` call carrying files under two different partition specs produces TWO manifests in ONE snapshot (per-spec manifest grouping), with each manifest stamped with the file's OWN spec id (file's-own-spec rule) — proven cross-language both directions via `run-interop-multi-spec.sh` + `tests/interop_multi_spec.rs`. |
| Write: merge append | ✅ (data-level interop both directions, 2026-06-11; one-bin + multi-bin) | `api/AppendFiles.java`, `core/MergeAppend.java`, `core/ManifestMergeManager.java`, `core/util/BinPacking.java` | `transaction/merge_append.rs` — Java `newAppend`: append in one `Operation::Append` snapshot like `fast_append`, then bin-pack + merge the manifest list per `commit.manifest-merge.enabled` / `commit.manifest.min-count-to-merge` / `commit.manifest.target-size-bytes`. Group by spec id (reverse-sorted, never cross-spec), `BinPacking.packEnd` (ported in a private `bin_packing` submodule), three bin rules, provenance-preserving entry routing (carried → Existing + original snapshot id/seqs ON DISK; this-commit → Added + null seq ⇒ re-inherits; old tombstones suppressed). Summary shape == `fast_append` (no `manifests-*` keys — documented divergence from Java's `buildManifestCountSummary`). 19 `MemoryCatalog` tests incl. raw-avro on-disk-seq provenance pin + old-tombstone suppression + MoR-delete-carry crown jewel + `bin_disposition`/`pack_end` units; provenance-restamp + tombstone-suppression + bin-gate mutations verified. **Metadata-level interop ✅ 2026-06-10 (one-bin merge path):** the canonical snapshot-metadata view of the Rust `merge_append` matches Java's `newAppend` byte-for-byte in the eight-step write-actions chain (s7 sets `min-count-to-merge=2` as a table property — no snapshot — then s8 merges every tiny manifest into ONE merged manifest with carried Existing entries + the new Added entry), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); multi-bin / multi-spec merge paths NOT covered by the chain. **All-tombstone-manifest carry — SETTLED PARITY (no fix needed), Wave-4 Group O / O1 2026-06-11:** UNLIKE `fast_append` (which carries ALL prior manifests, see that row), the MERGING append DROPS all-tombstone prior manifests, matching Java `MergingSnapshotProducer.apply` (L1007-1011) which filters its carried set through the `shouldKeep` predicate `hasAddedFiles()` OR `hasExistingFiles()` OR `snapshotId() == snapshotId()`. Rust merge_append's `existing_manifest` filter (`has_added_files` OR `has_existing_files`) equals Java's `shouldKeep` minus the third clause, which is UNREACHABLE for a pure append (no carried manifest was written by the not-yet-committed snapshot) ⇒ no divergence. Pinned by `test_merge_append_drops_all_tombstone_manifest_unlike_fast_append` (in `append.rs`). **Deferred (named residue, does NOT block ✅):** delete-manifest merging (carried unchanged), `appendManifest`, retry cache/orphan cleanup. **Row flipped 🟡→✅ 2026-06-13:** the data-level interop below proves BOTH the one-bin (fixture A) and multi-bin (fixture G) merge paths both directions; multi-spec merge interop remains the named residue. **Data-level interop ✅ 2026-06-11 (fixture A — S1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → set min-count=2 → merge_append G(cat=a,60). All 5 rows `{10,20,30,40,60}` survive both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; proves merge boundary carries all Existing entries through real parquet. **Data-level interop ✅ 2026-06-11 (fixture G — multi-bin W3):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (22-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table: 4 fast_appends (A+B+C1+C2) → dynamic `target-size-bytes = max_manifest_len*2+1` + `min-count-to-merge=2` → merge_append G(cat=a,id=60). `pack_end` with 5 manifests bins into [new],[e1,e2],[e3,e4]; both bins of size 2 satisfy min-count → 2 merged manifests (bin-count assert ≥ 2). Live set: 7 rows a-rows={10,20,30,50,60}, b-rows={40,55}. Proves multi-bin merge path; partition column pinned (S3 lesson). Sabotage battery: truncate + bogus-path both fail closed. |
| Write: `OverwriteFiles` | 🟡 | `api/OverwriteFiles.java`, `core/BaseOverwriteFiles.java`, `core/MergingSnapshotProducer.java` | `transaction/overwrite_files.rs` — explicit add + delete in one snapshot, dynamic `operation()`, `overwrite_by_row_filter` (residual-based KEEP/DELETE/PARTIAL-error per file), conflict validations: `validate_no_conflicting_data` (filter-based), `validate_added_files_match_overwrite_filter`, `validateNewDeletes` branches A (row-filter, landed 2026-06-10) + B. Known SAFE divergence: equality-delete applicability over-approximates (over-rejects, never under). Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/conflict-validation/multi-spec paths NOT covered by the chain. **Stale deferral corrected 2026-06-10:** Java has NO `validateDataFilesExist` block in `BaseOverwriteFiles.validate` (L135-175 — three blocks, all ported; `validateDataFilesExist` is RowDelta-only and already landed there); OverwriteFiles' concurrent-removal protection is `failMissingDeletePaths`, mirrored by `resolve_delete_paths`. **Data-level interop ✅ 2026-06-11 (fixture C — W1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (steps 6+10 in the 12-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → overwrite_files DELETE B ADD B'(cat=b,41,d'). Live rows `{(10,a),(20,b),(30,c),(41,d')}` (id=40 absent) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson). row-filter / multi-spec paths NOT covered by these chains. **Conflict-validation interop ✅ 2026-06-15 (C1):** `validate_no_conflicting_data()` + `conflict_detection_filter` agrees with Java `BaseOverwriteFiles.validate` → `validateAddedDataFiles` on a concurrent-commit history (S0 base + S1 concurrent add) — BOTH directions (Rust `register_table`s Java's table; Java re-runs the overwrite on Rust's table) over 3 scenarios (overlap→REJECT, below-filter→ACCEPT, no-filter/`alwaysTrue`→REJECT) + a fail-closed sabotage battery (semantic-swap + truncate), via `dev/java-interop/run-interop-overwrite-conflict.sh` + `crates/iceberg/tests/interop_overwrite_conflict.rs`. Row stays 🟡 for its remaining residue (row-filter + multi-spec conflict interop; `DeleteFiles`/`RowDelta`/`ReplacePartitions`/`RewriteFiles` conflict interop are follow-on C-units). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md) (cell-split repair 2026-06-10 — the de-triplication move had split this row's narrative on a logical-OR inside a code snippet; the strand is rejoined in the archive). |
| Write: `ReplacePartitions` (dynamic/static overwrite) | 🟡 | `api/ReplacePartitions.java`, `core/BaseReplacePartitions.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/replace_partitions.rs` — DYNAMIC partition overwrite: replaces every partition an added file belongs to via `SnapshotProducer::resolve_partition_deletes` feeding the shared rewrite path; full replace on an unpartitioned table; `replace-partitions` marker; opt-in `validate_no_conflicting_data`. Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes in UNREPLACED partitions survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/conflict-validation/multi-spec paths NOT covered by the chain. Deferred (→ ✅ beyond the chain): static `replaceByRowFilter`/explicit-partition APIs. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). **Data-level interop ✅ 2026-06-11 (fixture E — W2):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (18-step chain, steps 8+12) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → replace_partitions E_new(cat=a,id=11,data="a'"). Live rows `{(11,a'),(40,d)}` (A's rows 10/20/30 all gone) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson); EXTRA assertion: partition-b file (B) carries forward as EXISTING manifest status — not re-added as ADDED (Java oracle step 3f), proving the untouched partition's file is byte-untouched in the metadata. **Conflict-validation interop ✅ 2026-06-15 (C4 — the 2nd conflict-validation slice, after `OverwriteFiles`' C1):** `validate_no_conflicting_data()` agrees with Java `BaseReplacePartitions`'s PARTITION-SCOPED conflict validation (`file_in_replaced_partition`, NOT an inclusive-metrics filter — `ReplacePartitions` has NO `conflict_detection_filter` and NO `case_sensitive`, `javap`-confirmed) on a concurrent-commit history over a PARTITIONED `identity(category)` `{id,category}` table (S0 base cat=a+cat=b + S1 concurrent add into ONE partition) — BOTH directions (Rust `register_table`s Java's table; Java re-runs the replace on Rust's table) over 2 scenarios (concurrent-add-into-replaced-partition cat=a → REJECT; concurrent-add-into-other-partition cat=b → ACCEPT) + a fail-closed sabotage battery (semantic-swap + truncate), via `dev/java-interop/run-interop-replace-partitions-conflict.sh` + `crates/iceberg/tests/interop_replace_partitions_conflict.rs`. Row stays 🟡 for its remaining residue (multi-spec conflict interop; static `replaceByRowFilter`/explicit-partition APIs). |
| Write: `DeleteFiles` | 🟡 | `api/DeleteFiles.java`, `core/StreamingDelete.java`, `core/ManifestFilterManager.java` | `transaction/delete_files.rs` — delete by path / `DataFile` reference, plus the foundational manifest-filter/rewrite machinery (`SnapshotProducer::process_deletes`: rewrite/keep/drop + provenance preservation) every delete-bearing action reuses; opt-in `validate_files_exist` (the status-axis walk, 2026-06-09). Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/multi-spec paths NOT covered by the chain. **Conflict-validation interop ✅ 2026-06-16 (C2):** the SINGLE files-exist axis (`validate_files_exist`, no filter — the simplest write-action conflict unit) proven both directions via `dev/java-interop/run-interop-deletefiles-conflict.sh` + `crates/iceberg/tests/interop_deletefiles_conflict.rs`. Unpartitioned V2 `{id long, y long}`, shared history (S0 append f0+f1; S1 overwrite removes f0 adds f2); 2 scenarios (`same_file_reject` targets the removed f0 → REJECT, `different_file_accept` targets the surviving f1 → ACCEPT), targeted path DERIVED from the table (live@S0 vs live@S1, anti-circular). Rust `validate_files_exist` (status-axis walk → non-retryable `DataInvalid`) agrees with Java `StreamingDelete.validateFilesExist` → `ManifestFilterManager.failMissingDeletePaths` (`ValidationException`) on the conflict DECISION. **Axis isolation (honest scope — softened 2026-06-16 post-review):** D2 (Java validates Rust) WOULD isolate the axis — stripping Java's `validateFilesExist` *should* flip `same_file_reject` REJECT→ACCEPT (Java gates the missing-file check on the flag) — but this is REASONED FROM the Java 1.10.0 source, NOT mutation-tested: no sabotage in the driver actually strips the flag (the semantic-rollback sabotage rolls the head to S0, where both Java mechanisms agree ACCEPT, so it proves the decision tracks the HEAD, not the axis). Rust's by-path `process_deletes` check is UNCONDITIONAL, so it also rejects a removed-file delete WITHOUT the flag (a documented mechanism divergence — both engines agree WITH the flag, which is what C2 pins). D1 (Rust validates Java) therefore corroborates the DECISION rather than isolating the axis; the axis MESSAGE itself is pinned directly by the `delete_files.rs` `test_delete_files_exist_*` unit tests (the only genuine axis-isolation evidence). To upgrade the interop claim, add a flag-stripped D2 re-run asserting `same_file_reject` flips to ACCEPT. Sabotage battery (semantic-rollback head→S0 pinned to the outcome-mismatch line + structural-truncate, both fail-closed). **Data-level interop ✅ 2026-06-11 (fixture D — W1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (steps 7+10 in the 12-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40)+C_file(cat=a,50,e) → delete_files {B} by path. Live rows `{(10,a),(20,b),(30,c),(50,e)}` (id=40 absent) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson). Deferred (→ ✅ beyond the chain): delete-by-row-filter/partition-predicate. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Write: `RowDelta` (merge-on-read) | 🟡 | `api/RowDelta.java`, `core/BaseRowDelta.java`, `core/MergingSnapshotProducer.java` | `transaction/row_delta.rs` — merge-on-read commit: data + position/equality DELETE files in ONE snapshot, added deletes inherit the new seq, conflict validations, interop-proven (incl. residue-stripped sabotage). Stays 🟡 on remaining multi-spec DATA-merge commit residue (pending the `BaseRowDelta`/`BaseOverwriteFiles` multi-spec-manifest-merge decision). Dates 2026-06-08→16. [full narrative](archive/2026-06_matrix-cell-narratives.md#write-rowdelta-merge-on-read) |
| Write: `RewriteFiles` (compaction commit) | 🟡 | `api/RewriteFiles.java`, `core/BaseRewriteFiles.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/rewrite_files.rs` — atomically replace DATA files in one `Operation::Replace` snapshot (Java `BaseRewriteFiles`); `dataSequenceNumber` preservation landed 2026-06-10; conflict-validation interop-proven (sabotage battery). Stays 🟡 on remaining multi-spec interop residue + the DELETE-file-rewrite-surface interop (row 150). [full narrative](archive/2026-06_matrix-cell-narratives.md#write-rewritefiles-compaction-commit) |
| Transaction action extension seam | 🟡 | `core/.../BaseTransaction` | `transaction/action.rs` — `TransactionAction`/`ApplyTransactionAction` + `ActionCommit` exist (trait is `pub(crate)`; we own it → make `pub` in Phase 2) |
| Write: `upgrade_format_version` action | ✅ | format-version upgrade | `transaction/upgrade_format_version.rs` (new in 0.9) — base-inherited ✅ on the "implemented + usable" bar (upstream 0.9, unit-tested in `upgrade_format_version.rs`). **Strict-bar gap (named 2026-06-16):** no bidirectional interop fixture exists for the V2→V3 bump; it is the one un-proven slice under the fork's interop bar — cheap (metadata-only, no data files) if a future increment wants to flip it to fork-proven. See the legend note on the two ✅ bars. |
| Multi-op transactions + optimistic-concurrency retry | 🟡 | `api/Transaction.java`, `core/SnapshotProducer.java`, `core/MergingSnapshotProducer.java` | `transaction/mod.rs` + `catalog.update_table` retry (`backon`, retryable-only) + the conflict-validation FOUNDATION (2026-06-08): tx-captured `starting_snapshot_id` surviving the re-base, the `validate` hook against the refreshed base, non-retryable `DataInvalid` conflicts, the shared `files_after` walk family. **Multi-spec writes landed 2026-06-11:** the `SnapshotProducer` now groups added DATA + DELETE files by `partition_spec_id` and writes ONE manifest per (content × spec) — Java parity with `MergingSnapshotProducer.newDataFilesBySpec`/`newDeleteFilesBySpec` + `FastAppend.newDataFilesBySpec` (was default-spec-only). `validate_added_data_files`/`validate_added_delete_files` check spec EXISTENCE (Java's exact "Cannot find partition spec %s for {data,delete} file: %s") + partition-value compat against the FILE's own spec; the summary collector partitions by each file's own spec (Java `addedFile(spec(file.specId()), file)`). 8 producer tests (two-spec data/delete manifests, unknown-spec rejection ×2, wrong-spec-type, cumulative totals, plus reviewer-added per-spec partition-SUMMARY-key pin and a V1 two-spec append probe) + the cherrypick multi-spec replay conversion; grouping-revert, validation-revert, summary-revert, file-loss, and default-spec-stamp mutations all verified. Deferred (→ ✅): remaining per-action validation blocks; the WRITER-LAYER spec threading (`DataFileWriter`/`DeletionVectorWriter` still stamp the table default spec — a single writer instance produces single-spec files, so a multi-spec commit is assembled by the CALLER passing files that already carry their specs); multi-spec Java↔Rust interop; validation against the REAL catalogs (Glue + S3 Tables). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Writer: data file | ✅ | `data/` | `writer/base_writer/data_file_writer.rs` |
| Writer: equality-delete | ✅ | `data/` | `writer/base_writer/equality_delete_writer.rs` |
| Writer: position-delete | 🟡 | `data/`, `core/.../deletes/PositionDeleteWriter.java` | `writer/base_writer/position_delete_writer.rs` — reserved field ids (2147483546/2147483545), write-as-given (sorting is the caller's job, matching Java), schema built FROM the reserved metadata-column fields. Proven Java-readable via the scan-exec Direction-2 interop (2026-06-09). Deferred: the sorting writer variant; the optional `row` column. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Writer: deletion-vector (V3 puffin DV) | ✅ | `core/.../deletes` (`BaseDVFileWriter`, `BitmapPositionDeleteIndex.serialize`) | `delete_vector.rs` `serialize_deletion_vector_v1` (Java-faithful DENSE layout + `runLengthEncode` parity via `RoaringBitmap::optimize` + framing/CRC, hand-computed exact-byte goldens) + `writer/base_writer/deletion_vector_writer.rs` `DVFileWriter` (one Puffin per close, blob per referenced file). D2 landed 2026-06-10; public hook required `pub mod delete_vector`. [full narrative](archive/2026-06_matrix-cell-narratives.md#writer-deletion-vector-v3-puffin-dv) |
| Writer: partitioning (fanout/clustered/unpartitioned) | ✅ | — | `writer/partitioning/` |
| Read: Parquet → Arrow | ✅ | `parquet/` | `arrow/reader.rs` |
| Read: merge-on-read apply (position-deletes + DVs during scan) | 🟡 | `data/.../DeleteFilter`, `data/.../BaseDeleteLoader`, `core/DeleteFileIndex.java` | `arrow/delete_filter.rs`, `arrow/caching_delete_file_loader.rs`, `delete_file_index.rs`, `delete_vector.rs`. **Parquet position/equality deletes: data-level interop ✅ 2026-06-09 — the full cross-product** {position, equality} × {Java-writes-Rust-reads, Rust-writes-Java-reads} × {unpartitioned, partitioned}, via `crates/iceberg/tests/interop_scan_exec.rs` + run scripts (mutation-pinned). **HONESTY CORRECTION 2026-06-10: the previous ✅ over-claimed DVs** — it came from the 0.9.1 sync notes and was never scan-verified; until D1 a Puffin DV was misrouted to the parquet reader (the DV loader was a TODO) and a V3+DV scan FAILED. **D1 (2026-06-10) added the DV scan READ path:** `deletion-vector-v1` blob decode (BE length / `D1 D3 39 64` magic / BE CRC-32 / portable 64-bit roaring, Java `BitmapPositionDeleteIndex.deserialize` + `RoaringPositionBitmap.deserialize` validations, fuzz-shaped negative tests), single-ranged-read loading per Java `BaseDeleteLoader.readDV` keyed by `referenced_data_file`, and Java `DeleteFileIndex.forDataFile` precedence (a file's DV supersedes its parquet position deletes; eq deletes still apply). **Direction-1 interop ✅ 2026-06-10** (`run-interop-dv.sh` + `tests/interop_dv_scan.rs` + the raw-blob decode pin incl. >2^32 positions + run containers): Rust scans a Java-1.10.0-written V3 table with a real `BaseDVFileWriter` DV. Residue (the row stays 🟡): Java's two index-level `ValidationException`s are not index-level here — duplicate-DV-per-file is rejected at the load door instead (`DeleteFileIndex.java` L528-535); `dv.dataSequenceNumber() >= seq` (Java `findDV` L208-214, an invalid-table state) now LANDED 2026-06-11 (multispec-closeout 3) — the index was made FALLIBLE (one production caller, `scan/context.rs`, already `Result`-returning; the index is the only place both sequence numbers are in hand, vs the load door which receives neither — `FileScanTaskDeleteFile` drops them) and a `dv_seq < data_seq` lookup returns the EXACT 1.10.0 message "DV data sequence number (%s) must be greater than or equal to data file sequence number (%s)" (hand-built invalid metadata test + check-disabled mutation; valid boundary dv_seq==data_seq / dv_seq>data_seq still applies the DV); deferred from 2026-06-09: multi-file-per-partition, non-identity transforms, more column types. **DVs data-level interop ✅ BOTH directions 2026-06-10:** Direction 1 = D1 above; Direction 2 = D4's table-level proof — Java's production scan reads a Rust-COMMITTED V3 table whose merge-on-read deletes are two Rust-written DVs in one puffin (see the DV-writer row; `run-interop-dv.sh` + `tests/interop_dv_table.rs`). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Read: ORC data files | 🟡 | `orc/` (`ORCSchemaUtil.buildOrcProjection`, `GenericOrcReader`, `GenericAppenderFactory` ORC writer) | **READ landed (U1+U2, 2026-06-18) — scan-wired + interop-proven; WRITE absent.** U1 landed the decode core `arrow/orc_reader.rs` (`read_orc_data_file`/`read_orc_data_bytes`): hand-parses the ORC Footer to recover each type's `iceberg.id` attribute and resolves **by field-id, never by name** (a file without `iceberg.id` errors loudly), mirrors `ORCSchemaUtil.buildOrcProjection` (by-id projection + Java promotion `int→long`/`float→double`/decimal precision-widen, missing-required vs optional-all-null priority) and `GenericOrcReader`'s per-type physical read, stamping `PARQUET_FIELD_ID_META_KEY`. **U2 WIRED it into the scan read path:** `arrow/reader.rs::process_file_scan_task` `Orc` arm now dispatches to `process_orc_file_scan_task` — a VERBATIM clone of the Avro arm (materialize via U1 → the SAME `RecordBatchTransformer` for schema-evolution + `_file`/identity-partition constants → MoR deletes applied POST-materialization since this reader does not push into ORC stripe metadata: positional via absolute-row-position membership in the `DeleteVector`, equality + scan residual via the shared survival mask) with the ONLY change being the U1 ORC materialize call. 6 offline scan tests (`arrow/reader.rs` `avro_scan_tests`: read-all / projection / positional-delete / equality-delete / by-field-id RENAME proof / positional mutation-bait) over the committed Java-Iceberg golden fixture, plus the U1 decode tests. **Interop ✅ Direction-1 (2026-06-18):** `OrcDataOracle` + `run-interop-orc-data.sh` + `tests/interop_orc_data.rs` — Java's `GenericAppenderFactory.newDataWriter(..., FileFormat.ORC, null)` (iceberg-orc `GenericOrcWriter`) writes a V2 table whose DATA file is ORC over every primitive+logical type + an optional/null column, a parquet position-delete removes one row; Rust scans → Arrow and asserts ROW-IDENTITY (every column value/type/null) against the hand-declared expected (anti-circular: hand-declared identically both sides; Java self-checks its OWN IcebergGenerics read == expected before emitting). Fail-closed sabotage (corrupt one decimal → compare DIVERGES, exit 101, restored). **NAMED residue keeping it 🟡:** (1) **WRITE absent** — there is no ORC data writer (the W1 analogue), so no Direction-2 (Java-reads-Rust) proof; (2) **footer-codec residue** — only an UNCOMPRESSED or ZLIB-compressed ORC *footer* is parsed for `iceberg.id` (the raw-DEFLATE codec); a SNAPPY/ZSTD/LZ4/LZO *footer* yields a clean `FeatureUnsupported` (the data-stream decode itself is `orc-rust`'s and codec-complete — only OUR footer parse is codec-limited); (3) **nested / V3 deferral** — only top-level structs of primitive/logical columns are read; nested struct/list/map schema-evolution-by-id and the V3 `variant`/`geometry`/`geography` types are deferred, and a non-null `initial-default` for a missing column errors (Java parity — ORC cannot read defaults). |
| Read/write: Avro data files | 🟡 | `core/.../avro` (data), `data/.../avro/PlannedDataReader.java`, `data/GenericAppenderFactory.java` | `arrow/avro_reader.rs` (`read_avro_data_file`, a faithful `PlannedDataReader`/`ValueReaders.buildReadPlan` port: id-based read plan, all primitive+logical types, projection/skip/missing-default). READ landed U2 2026-06-18, scan-wired + interop-proven. Stays 🟡 on the parked `timestamptz` `adjust-to-utc` mapping gap (user decision 2026-06-18) plus deferred name-mapping fallback + V3 row-lineage + variant reads. [full narrative](archive/2026-06_matrix-cell-narratives.md#readwrite-avro-data-files) |
| Scan planning + partition pruning | ✅ | `api/TableScan.java` | `scan/` — manifest-list → manifest → file pruning (manifest evaluator, partition filter, metrics evaluator), `use_ref` branch/tag scanning (2026-06-08), per-task PARTITION-REDUCED residuals (2026-06-08). **Scan-PLANNING interop ✅ 2026-06-09 (A4):** planned file set + per-file delete association + residual-always-true bit match Java; full residual-expression string comparison deferred (cross-language syntax). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Metrics evaluators (inclusive/strict) + residual evaluation | 🟡 | `expressions/`, `api/.../expressions/ResidualEvaluator.java` | `expr/visitors/` — inclusive/strict metrics evaluators + `ResidualEvaluator` (core 2026-06-08 + scan wiring: each `FileScanTask` carries the partition-reduced residual). Identity-partition constant materialization (Java `PartitionUtil.constantsMap`) ACTIVATED 2026-06-11: `context.rs` threads each manifest's `partition_spec` onto its tasks; `record_batch_transformer` materializes identity-partition columns as PLAIN arrays of the declared scan-schema type (the prior REE-leak bug fixed) coerced to the column's Iceberg type via `Datum::to` (int→long widening fixed), and forces the constant to OVERRIDE a file-present column (verified against Java 1.10.0 bytecode: `BaseParquetReaders.ReadBuilder.struct` → `ParquetValueReaders.replaceWithMetadataReader` returns the constant reader unconditionally when `idToConstant` has the field id, discarding the file-column reader — constant wins by construction). BEHAVIOR CHANGE: any identity-partitioned table whose data-file values diverge from partition metadata now returns the PARTITION value (was the file value); for spec-consistent tables (the norm) results are unchanged. Multi-spec: each file's constants use its own spec. Gated on `cargo test -p iceberg-datafusion` (lib + MemoryCatalog integration incl. `test_insert_into_partitioned`). Unit + scan tests ship; data-level Java interop round-trip still pending (keeps 🟡). **Residue (verified 2026-06-13):** NO variant-type datum handling in the inclusive/strict metrics evaluators (`expr/visitors/{inclusive,strict}_metrics_evaluator.rs` have zero `Variant` arms) — a variant field's min/max bounds are treated as unknown (no pruning), matching the "variant behaves as unknown" posture, not Java's typed variant bounds. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `IncrementalAppendScan` | ✅ | `api/IncrementalAppendScan.java`, `core/BaseIncrementalAppendScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — exclusive/inclusive `from` ranges, append-only snapshot walk, own-added-manifest + `Added`-entry filters, reuses the normal scan's plan machinery via the `_from_files` seam. **Interop ✅ BOTH directions 2026-06-17** (`tests/interop_incremental_scans.rs` + `dev/java-interop/run-interop-incremental-scans.sh` + `IncrementalScanOracle`): a 4-snapshot V2 chain (S1 append a / S2 append b / S3 append c / S4 overwrite delete-a/add-d, deterministic basenames) where Rust's `incremental_append_scan` planned data-file BASENAME set == Java's REAL `IncrementalAppendScan.fromSnapshotExclusive/fromSnapshotInclusive(...).toSnapshot(...).planFiles()` over the SAME ranges == the hand-declared anti-circular ground truth, BOTH directions (D1 Rust-plans-Java-table, D2 Java-plans-Rust-table). The inclusive/exclusive boundary is pinned explicitly: `append_excl` from S1-EXCLUSIVE → {b,c} vs `append_incl` from S1-INCLUSIVE → {a,b,c} (S1's own a is the only difference); `append_to_cur` from S2-exclusive default-to-current → {c} (the S4 overwrite excluded by the append-only filter). Off-by-one sabotage (shift the exclusive bound S1→S2 ⇒ {b,c}→{c}) fails closed, non-vacuity verified. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `IncrementalChangelogScan` | ✅ | `api/IncrementalChangelogScan.java`, `core/BaseIncrementalChangelogScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — Insert/Delete changelog tasks with OLDEST→0 ordinals, deletes sourced from the deleting snapshot's own rewritten manifests; delegates range resolution to the append-scan builder. **DATA-FILE-changelog interop ✅ BOTH directions 2026-06-17** (`tests/interop_incremental_scans.rs` + `run-interop-incremental-scans.sh` + `IncrementalScanOracle`, same 4-snapshot chain as row 120): Rust's `incremental_changelog_scan` over (S1 exclusive, S4] emitted the `(basename, operation)` set {+b, +c, −a, +d} (the S4 overwrite removes a ⇒ DELETE, adds d ⇒ INSERT; the S2/S3 appends ⇒ INSERT) == Java's REAL `IncrementalChangelogScan` / `IncrementalDataTableScan` over the SAME range == the hand-declared anti-circular ground truth, BOTH directions. **NAMED residue (not over-claimed):** this is the WHOLE-FILE-added/deleted DATA-FILE changelog, matching Java's CURRENT `IncrementalDataTableScan` behavior — NOT row-level CDC: `ChangelogOperation::{UpdateBefore, UpdateAfter}` net-row changes are unimplemented and `BatchScan` (row 122) is absent; a range whose snapshots carry row-level DELETE manifests is rejected (`FeatureUnsupported`), exactly as Java's data-file changelog rejects them. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `BatchScan` | ✅ | `api/BatchScan.java`, `api/BatchScanAdapter.java` | **LANDED + interop-proven ✅ both directions 2026-06-17 (U2).** `scan/batch.rs` (`BatchScan`, built by `Table::batch_scan()`) — the THIN typed adapter mirroring Java `BatchScanAdapter` (javap-confirmed: `Table.newBatchScan()` → `new BatchScanAdapter(newScan())`, every method `getfield scan; invoke...` delegates 1:1). Wraps a `TableScanBuilder`; `plan_files`/`plan_tasks` DELEGATE to the `TableScan` pipeline (row 146 — split/bin-pack REUSED, not reimplemented). Adds the three time-travel selectors (Java `SnapshotScan.useSnapshot`/`useRef`/`asOfTime`, bytecode-verified): `use_snapshot(id)`/`use_ref(name)`/`as_of_time(ms)`, FIRST-WINS mutual exclusion (`checkArgument(snapshotId == null, "Cannot override snapshot/ref, ...")` — a second pin is a hard `DataInvalid`; `use_ref("main")` is the table-default no-op, never a conflict); `as_of_time` = the GREATEST `timestamp_ms <= ms` over `metadata().history()` (Java `SnapshotUtil.snapshotIdAsOfTime` / `nullableSnapshotIdAsOfTime`), erroring "Cannot find a snapshot older than %s" when none. 11 offline tests (delegation `plan_tasks`==`scan().plan_tasks` + `plan_files`; selector resolution; `as_of_time` inclusive `<=` boundary at-exact-ts + 1ms-before + before-all-errors; conflict matrix; `main` no-op) with 2 mutation-baits (`rfind`→`find` greatest-vs-earliest; `<=`→`<` inclusive-boundary — each reds the boundary test). **Bidirectional interop ✅** (`tests/interop_scan_plan.rs` + `run-interop-scan-plan.sh` + `ScanPlanOracle`, extends the U1 row-146 oracle): `table.newBatchScan().planTasks()` asserted == `newScan().planTasks()` IN JAVA (the adapter delegation), and Rust `BatchScan::plan_tasks` == Java's `newBatchScan().planTasks()` == the scan plan, BOTH directions (D1 11 groups; D2 16 groups, `0 failures`). Subsumed-row 146 → ✅ alongside this. |
| Scan/commit metrics reporting (`ScanReport`, `MetricsReporter`) | 🟡 | `metrics/` | `metrics/mod.rs` (data model, 2026-06-08: Java-shape serde incl. counter/timer wire format) + OPT-IN scan emission wiring (2026-06-09: `with_metrics_reporter`, report ONCE on full stream consumption; no-reporter path byte-unchanged). **`filter`-field divergence RETIRED 2026-06-17:** the `filter` now serializes via the canonical `ExpressionParser` codec (custom serde, row 147) so `ScanReport.filter` byte-matches Java `ScanReportParser` (serialize via `ExpressionParser.toJson`, deserialize via the schema-less untyped path). Still 🟡 for the OTHER residue: `LoggingMetricsReporter` (needs a logging-facade dep approval) and the 6 not-yet-collectable counters stay `None`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Catalogs: REST, Hive, Glue, S3 Tables, SQL/JDBC, in-memory | 🟡 (TABLE ops ✅ all; residue below) | `core/.../{rest,jdbc,inmemory}`, `aws`, `hive-metastore` | `crates/catalog/*`, `catalog/memory`. TABLE lifecycle ✅ across all impls. **Residue the umbrella hid:** (a) **PARITY-CORRECT, not a gap (verified 2026-06-17):** the PRIORITY catalogs Glue (`crates/catalog/glue`) + S3 Tables (`crates/catalog/s3tables`) override NO view method, so all 7 fall through to the trait's default `FeatureUnsupported` stubs — this MIRRORS Java, where neither service supports views: Java `GlueCatalog` does NOT implement `ViewCatalog` (issue #12488 — an OPEN feature request to ADD Glue view support, confirming 1.10.0 does not have it), and the S3 Tables service has no view API (`aws-sdk-s3tables` 1.51 `src/operation/` exposes only namespace/table/bucket ops, ZERO `*view*` operations — SDK-verified offline; the Glue claim rests on #12488 + the Rust-side no-override evidence, the Java `iceberg-aws` jar being unavailable to javap offline). The `FeatureUnsupported` view stubs on Glue/S3Tables are therefore Java-faithful (see the ViewCatalog row); (b) the `Catalog` trait accessor surface (`name()`/`properties()`/`invalidate_table`/`invalidate_view`) is now LANDED as non-breaking default methods (2026-06-17, see the Catalog-trait-accessors row); `commitTransaction(List<TableCommit>)` is split out as its own deferred unit (REST-server multi-table atomic commit, concrete on `RESTCatalog` only). |
| `ViewCatalog` + view operations (create/replace/drop/list, versions) | 🟡 | `api/catalog/ViewCatalog.java`, `api/view/`, `core/view/` | `spec/view_metadata*`, `view_version.rs`, `view.rs` — view metadata spec + builder is a 1:1 port of Java `ViewMetadata.Builder`; `ViewMetadata::read_from`/`write_to` + `View` handle landed U1 2026-06-12. Stays 🟡 on byte-exact residue; Glue/S3Tables "views unsupported" verified 2026-06-17 as parity-correct (mirrors Java, issue #12488 OPEN) — `FeatureUnsupported` stubs are Java-faithful, no view sprint owed. [full narrative](archive/2026-06_matrix-cell-narratives.md#viewcatalog--view-operations-createreplacedroplist-versions) |
| `SessionCatalog` | ❌ (ASSESSED-DEFERRED — dead surface, 2026-06-17) | `api/catalog/SessionCatalog.java` | none — **deliberately not ported.** Java `SessionCatalog` (javap-confirmed) threads a `SessionContext` arg through every op (`loadTable(ctx, ident)`, `buildTable(ctx, …)`, `name()`/`properties()`, …) so ONE catalog instance can serve many per-session credential scopes (the multi-tenant REST pattern; `RESTSessionCatalog` is the real impl, wrapped to a plain `Catalog` via `BaseSessionCatalog.asCatalog(ctx)` which binds a FIXED context). The Rust design binds identity per-catalog AT CONSTRUCTION — each `Catalog` instance already holds its own credentials/config — so a `SessionContext` parameter has NO consumer here; the `asCatalog(ctx)` wrapper proves the session layer is optional (a fixed-context `SessionCatalog` IS just a `Catalog`). Revisit only if a multi-tenant single-instance use-case appears. |
| `LockManager` | ❌ | `api/LockManager.java`, `core/.../LockManagers.java` | none — ZERO code (no trait, no `acquire`/`release`, grep-confirmed 2026-06-13; the prior 🟡 "partial" was wrong). Java's `LockManager` + `LockManagers.{from,defaultLockManager}` + the in-memory/no-op impls are entirely unported. |
| Encryption (`EncryptionManager`, KMS, encrypted FileIO/manifests) | ❌ | `api/encryption/`, `core/.../encryption` | V3 `spec/encrypted_key.rs` stub only |
| FileIO (S3/GCS/Azure/OSS/fs/memory) | ✅ | `core/.../io`, cloud modules | `io/` + extracted `crates/storage/opendal` (OpenDAL) |
| Puffin read/write + blob types (theta NDV, DV) | 🟡 | `core/.../puffin`, `api/.../puffin` | `puffin/` (blob coverage partial) |
| Maintenance: `ExpireSnapshots` | 🟡 (`ReachableFileCleanup` interop-proven both directions A3; only `IncrementalFileCleanup` + `cleanExpiredMetadata` + ref-age deferred) | `api/ExpireSnapshots.java`, `core/RemoveSnapshots.java`, `core/ReachableFileCleanup.java` | `transaction/expire_snapshots.rs` (2026-06-11, B1 — metadata retention, 1.10.0 bytecode-verified) + `transaction/expire_cleanup.rs` (2026-06-11, B2 — `ReachableFileCleanup` file cleanup: post-commit-only `ExpireSnapshotsCleanup::commit_and_clean` seam, candidates-minus-retained set algebra by path, live-entry content-file subtraction across data AND delete manifests incl. shared-puffin DVs, statistics files, injectable delete fn, failures collected in `CleanupReport`). **A3 INTEROP (2026-06-11) — deferral RESOLVED for retention + `ReachableFileCleanup`, BOTH directions** (`dev/java-interop/run-interop-expire.sh` + `crates/iceberg/tests/interop_expire.rs`, 5 fixtures: linear/tag_protected/stats/deletes/rewrite): Java's `expireSnapshots().cleanExpiredFiles(true).deleteWith(collector)` vs Rust `ExpireSnapshotsAction` + `ExpireSnapshotsCleanup::commit_and_clean`; D1 (Rust acts, Java judges) byte-equal canonical views + Java-verified surviving snapshots/refs + deleted set; D2 (Java acts, Rust verifies) Rust view == Java view + Rust cleanup deleted set == Java `deleteWith` set (path-independent `<funnel>@ord<N>#<discriminator>` descriptor — injective per-file via record-count / file-counts / size, reviewer-hardened). Java forced down `ReachableFileCleanup` (the only ported strategy) for EVERY fixture via the real engine selector `RemoveSnapshots.withIncrementalCleanup(false)` (reviewer fix: a HEAD tag alone leaves Java auto-selecting `IncrementalFileCleanup` because `hasNonMainSnapshots(current)` needs an off-main survivor; bytecode-cited). Documented divergences: cleanup is explicit opt-in (Java defaults `cleanExpiredFiles(true)`); retained-shared manifest lists spared (under-deletion, pinned). Deferred: `IncrementalFileCleanup` (optimization with stricter eligibility), `cleanExpiredMetadata`, ref-age (`max_ref_age_ms`) interop. STOP-FINDING (raised here, FIXED in a separate increment): Rust `FastAppend::existing_manifest` DROPPED all-tombstone manifests where Java `FastAppend.apply` carries ALL prior manifests forward — fixtured away in A3 (the `rewrite` fixture's delete leaves a file existing); **FIXED 2026-06-11 in Wave-4 Group O / O1 — see the fast-append row.** |
| Maintenance: `DeleteOrphanFiles` | 🟡 | `api/actions/DeleteOrphanFiles.java`, `spark/.../DeleteOrphanFilesSparkAction.java`, `core/ReachableFileUtil.java`, `core/util/FileSystemWalker.java`, `core/actions/FileURI.java`, `core/hadoop/HiddenPathFilter.java` | `maintenance/delete_orphan_files.rs` (2026-06-11, A2): builder action over `FileIO::list` (A1). Valid-file universe = ALL snapshots' content files (every entry incl. DELETED tombstones, Java `ManifestFiles.read` not `liveEntries`) + all manifests + all manifest lists + current/previous metadata.json (non-recursive) + version-hint + statistics/partition-statistics. Hidden-path filter (`_`/`.` except `_<partition-field>=`), `olderThan` cut (default now−3d), URI-normalized orphan join (scheme-less local path matches any actual scheme per Java `uriComponentMatch`), `PrefixMismatchMode {Error,Ignore,Delete}`, `equal_schemes` (defaults `{s3n,s3a→s3}` merged) / `equal_authorities`, GC gate, delete failures collected. CAVEAT: action is MAIN-source (no 1.10.0 Spark bytecode); helpers (`FileURI`/`PrefixMismatchMode`/`HiddenPathFilter`/`PartitionAwareHiddenPathFilter`) bytecode-verified vs core/api 1.10.0. Deferred: executor parallelism (sequential sweep), bulk deletes, `compareToFileList`/streaming, interop. |
| Maintenance: `RewriteDataFiles` (compaction) | 🟡 | `api/actions/RewriteDataFiles.java`, `core/actions/{SizeBasedFileRewritePlanner,BinPackRewriteFilePlanner,RewriteDataFilesCommitManager}.java` | `maintenance/rewrite_data_files.rs` (2026-06-11, O2): `RewriteDataFiles::new(table)` bin-pack action over the seq-preserving `RewriteFiles` commit. Planning (1.10.0): scan tasks grouped PER PARTITION (`groupByPartition` — current-spec partition else empty bucket), candidate filter `outsideDesiredFileSizeRange (length<min OR length>max) OR tooManyDeletes (deletes>=delete_file_threshold)`, forward greedy `pack` (lookback-1, `<= max_file_group_size_bytes`), group filter `enoughInputFiles (size>1 && size>=min_input_files) OR enoughContent (size>1 && inputSize>target) OR tooMuchContent (inputSize>max) OR anyMatch(tooManyDeletes)`. Reads each group's LIVE rows (merge-on-read deletes APPLIED, like the Spark runner) and rolls output at target size. `.filter(Predicate)` is FILE-SELECTION ONLY: the per-file RESIDUAL is STRIPPED before the rewrite read (O2 reviewer fix, the Rust analogue of Java's `BinPackRewriteFilePlanner.planFileGroups` `.ignoreResiduals()` + the Spark runner reading the group with NO row filter) so a file partially matching the filter keeps ALL its live rows — a leaked residual would silently DROP the non-matching rows (data loss). Defaults (bytecode-cited): `use_starting_sequence_number=true`, `min_input_files=5`, min=0.75·target, max=1.8·target, target=`write.target-file-size-bytes` (512MiB), `delete_file_threshold` disabled (`usize::MAX`), `max_file_group_size_bytes=100GiB`. SEQ RULE: when `use_starting_sequence_number`, added files stamped with the STARTING snapshot's seq (`RewriteDataFilesCommitManager.commitFileGroups` bytecode: `dataSequenceNumber(table.snapshot(startingSnapshotId).sequenceNumber())`) so a concurrent equality delete still applies. Empty plan = no-op zero-count result, NO commit. Builder: `.target_file_size_bytes/.min_input_files/.min_file_size_bytes/.max_file_size_bytes/.delete_file_threshold/.max_file_group_size_bytes/.use_starting_sequence_number/.filter(Predicate)`; `.execute(catalog)`. Result: `added_data_files_count/rewritten_data_files_count/rewritten_bytes_count/file_groups`. Deferred: partial progress (`PARTIAL_PROGRESS_*`), concurrency (`MAX_CONCURRENT_FILE_GROUP_REWRITES`/`executeWith`), sort/zorder strategies, `delete_ratio_threshold`/`tooHighDeleteRatio`, `output_spec_id`/`rewrite_all`/`rewrite_job_order`/`max_files_to_rewrite`, oversized-file input SPLITTING (Java's planner does NOT split input files — bin-packs whole tasks; output rolling only), Java interop. CAVEAT: the runner read-semantics (`SparkBinPackFileRewriteRunner`) is MAIN-source (no core/api bytecode); the planner predicate/defaults are MAIN with the literal constants bytecode-confirmed; the seq + result shape are api/core bytecode. The fork's `bin_packing` module (`transaction/merge_append.rs`) was NOT reused (it is `pub(crate)`-private to that module; opening it = a `transaction/` change, out of scope) — the lookback-1 forward `pack` is reimplemented locally, algorithm-verified against Java + the fork's packer. |
| Maintenance: `RewritePositionDeleteFiles` | ✅ (2026-06-17 — read-identity-proven offline + interop; V2 PARQUET only, V3 Puffin DV out by documented divergence) | `api/actions/RewritePositionDeleteFiles.java` | `maintenance/rewrite_position_delete_files.rs` — `RewritePositionDeleteFiles::new(table).filter(Predicate).execute(catalog)` is the 1:1 port of Java `RewritePositionDeleteFiles` (`extends SnapshotUpdate`, one own method `filter(Expression)`, `Result{rewrittenDeleteFilesCount()/addedDeleteFilesCount() (int), rewrittenBytesCount()/addedBytesCount() (long)}` — javap-confirmed; the four counts mirrored 1:1 by `RewritePositionDeleteFilesResult`). It IS one of the twelve `ActionsProvider` methods (`rewrite_position_deletes(Table)`), so it is WIRED into the provider (see row 151). A STRICT SUBSET of `ConvertEqualityDeleteFiles` (no data-file row matching, no survival-predicate inversion, no eq-tuple parsing). The planning+commit machinery is engine-agnostic iceberg-core; the read/sort/write materialization is Spark-surface, so built engine-agnostically: walk the snapshot manifests once for LIVE PARQUET position-delete entries grouped by `(spec_id, partition)` (Puffin DVs SKIPPED — `file_format != Parquet`); per group of 2+ files read each member's `(file_path, pos)` pairs by RESERVED FIELD ID (`2147483546`/`2147483545`, not by name, via `arrow/delete_file_loader` `parquet_to_batch_stream`); concat + sort; write FEWER pos-delete files under the group spec/partition key (reusing `PositionDeleteFileWriter`); commit ONE `RewriteFiles` per group `delete_delete_files(rewritten).add_delete_file_with_sequence_number(new, MAX rewritten data-seq).validate_from_snapshot(starting)`. **SEQ-STAMP staller (silent corruption) handled + mutation-proven:** the compacted file carries the group MAX rewritten data seq via `add_delete_file_with_sequence_number` (NOT inherit, NOT min) — a wrong (higher/inherited) seq resurrects deleted rows, a lower seq over-applies. Tests: 10 offline read-identity tests on throwaway MemoryCatalog tables (crown jewel = a data file masked by 2 parquet pos-deletes compacts to 1 with IDENTICAL live rows; exact-seq-stamp; resurrection guard; multi-file grouping; partition isolation; filter restriction; 2-DV-not-compacted scope; 3 no-op edges) — each mutation flips its test red (max→min seq, stamp→inherit, group-read-break, DV-skip-removed all caught). **Interop PROVEN** (`tests/interop_rewrite_pos_deletes.rs` + `dev/java-interop` `RewritePosDeleteOracle` + `run-interop-rewrite-pos-deletes.sh`, env-gated no-op offline): Rust writes the PRE (4 parquet pos-deletes) + POST (2 compacted) tables, Java's `IcebergGenerics` reads BOTH and asserts the live id set is IDENTICAL `{100,130,200,230}` before vs after + the compaction shape (PRE pos=4 > POST pos=2 > 0) — the no-Spark corroboration that the compacted pos-delete masks exactly the rows the originals masked, seq preserved; sabotage battery HARD-FAILs closed (read-identity-breaker via the read-identity-BROKEN line + structural-truncate via the load-error line). **Documented divergence:** V2 PARQUET position deletes only; V3 Puffin DELETION VECTORS are file-scoped (one DV per data file, never bin-packed) and SKIPPED — a DV is never compacted by this action. |
| Maintenance: `RemoveDanglingDeleteFiles` | ✅ | `api/actions/RemoveDanglingDeleteFiles.java`, `spark/.../RemoveDanglingDeletesSparkAction.java` | `maintenance/remove_dangling_delete_files.rs` (2026-06-11, O3) — `RemoveDanglingDeleteFiles::new(table).execute(catalog)` removes, in ONE `Replace` snapshot, every delete file in the current snapshot that can no longer apply to any live data file (dangling predicate ported verbatim from the Spark `findDanglingDeletes` SQL). Interop ✅ 2026-06-16 + DV e2e gap closed. [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-removedanglingdeletefiles) |
| Maintenance: `ComputeTableStats` / `ComputePartitionStats` | 🟡 | `core/PartitionStatsHandler.java`, `core/PartitionStats.java`, `api/Partitioning.java`, `api/util/PartitionUtil.java`, `api/actions/Compute*.java` | `maintenance/partition_stats.rs` — `compute_partition_stats` ports Java 1.10.0 `PartitionStatsHandler` FULL-compute aggregation (2026-06-12, X1); `UpdatePartitionStatistics` commit + interop sabotage battery landed. Stays 🟡 NOT for partition stats (that bar is met) but because `ComputeTableStats` (theta-NDV) is a separate Group-Y surface and full `Compute*Stats` breadth beyond NDV is not yet at Java parity. [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-computetablestats--computepartitionstats) |
| Maintenance: `SnapshotTable` / `MigrateTable` / `RewriteTablePath` | 🟡 (`RewriteTablePath` FULL-rewrite BUILT + interop-proven 2026-06-17; `SnapshotTable`/`MigrateTable` still ❌ — need an external source table this library has no surface for) | `api/actions/`, `core/RewriteTablePathUtil.java` | `maintenance/rewrite_table_path.rs` — the FULL-rewrite Rust port of Java 1.10.0's engine-agnostic CORE `RewriteTablePathUtil` (bytecode-decoded, NOT the Spark surface): `RewriteTablePath::new(table).rewrite_location_prefix(source,target).staging_location(dir).execute(file_io)` rewrites EVERY absolute path prefix `source`→`target` across the metadata graph (metadata.json + manifest-lists + manifests + position-delete CONTENT) into a STAGING location and returns a `RewriteTablePathResult{staging_location, staged_metadata_location, copy_plan: Vec<(String,String)>, latest_version}`. It is a STAGE-AND-PLAN action — it does NOT physically copy data files; it emits a `(source,target)` copy-plan (Java `RewriteResult.copyPlan()`). Mirrors the 1.10.0 divergences EXACTLY: `location` via `String.replaceFirst` (regex, the ONLY non-`newPath` field); the four `write.*.path` properties via `newPath`; `statisticsFiles` rewritten but `partitionStatisticsFiles` PASSED THROUGH un-rewritten (javap offset 142); `referenced_data_file` rewritten; position-delete is the ONLY content-rewritten payload (+ `replacePathBounds` on the file_path-column bounds: rewrite when lower==upper, clear when they span multiple files); copy-plan direction differs by class — manifest-list/manifest/pos-delete are STAGED (copy FROM staging), data/eq-delete are VERBATIM (copy FROM source). Manifests re-emitted dispatching on the original entry status (Java `appendEntryWithFile`: add/existing/delete) so seq/snapshot ids survive; `format_version` threaded; on-disk format STABLE (only path STRINGS change). Wired into `ActionsProvider`: `rewrite_table_path` flipped FeatureUnsupported→built, moving SUPPORTED 9→10 / UNSUPPORTED 3→2 (the two left, `snapshot_table`/`migrate_table`, need an external source table) with the partition-the-twelve + supported/unsupported tests + doc table updated in lockstep. Tests: 15 offline (path-helper parity + `replace_paths` field-by-field incl. the 4-property + partition-stats-passthrough + statistics mutation-baits, the full-graph no-source-leak, the per-class copy-plan direction, pos-delete content rewrite into staging, determinism + double-rewrite rejection, prefix/staging preconditions, `replace_path_bounds` single-vs-multi). **Interop PROVEN, BIDIRECTIONAL** (`tests/interop_rewrite_table_path.rs` + `dev/java-interop` `RewriteTablePathOracle` + `run-interop-rewrite-table-path.sh`, env-gated no-op offline): `RewriteTablePathUtil` is CORE so Java DRIVES the real rewrite — on a V2 `{id,cat,y}` fixture (data + a parquet position-delete + an equality-delete) the Rust rewritten-path GRAPH (the SET of rewritten target locations) AND the COPY-PLAN (the `(from,to)` pairs with the per-class STAGED-vs-SOURCE direction encoded) both EQUAL Java's REAL `replacePaths`/`rewriteManifestList`/`rewriteDataManifest`(8-arg, all-snapshots set)/`rewriteDeleteManifest`(8-arg) output (both directions), with a flip-one-direction sabotage that fails closed via the `copy-PLANs differ` path. **Deferred (named residue):** incremental mode (`startVersion`/`endVersion` + version-diff walk + version-hint write) + the Spark-side CSV file-list — FULL rewrite only. `SnapshotTable`/`MigrateTable` remain ❌. |
| Partition statistics: `UpdatePartitionStatistics` commit surface + `ComputePartitionStats` ACTION wrapper | ✅ (2026-06-17 — both residue items built; byte-format/registration interop-proven by Z3/R2/R3, see the Maintenance row) | `api/UpdatePartitionStatistics.java`, `core/.../SetPartitionStatistics`/`RemovePartitionStatistics`, `api/actions/ComputePartitionStats.java`, `api/PartitionStatisticsFile.java` | The COMPUTE core + on-disk write + read-back + interop (Z3/R2/R3) live in the `Maintenance: ComputeTableStats/ComputePartitionStats` row above — see there, ONE home. Both residue items now BUILT (G2, 2026-06-17): (a) `transaction/update_partition_statistics.rs` — `UpdatePartitionStatisticsAction` is the 1:1 port of Java `UpdatePartitionStatistics` (`extends PendingUpdate<List<PartitionStatisticsFile>>`): `set_partition_statistics(file)`/`remove_partition_statistics(snapshot_id)` (javap-confirmed) emitting `TableUpdate::SetPartitionStatistics`/`RemovePartitionStatistics`, exposed via `Transaction::update_partition_statistics()` (the `update_statistics()` seam clone); the commit attaches `AssertTableUUID` itself (a non-snapshot metadata update — Java `UpdateRequirements.forUpdateTable`), so it matches the proven `register_partition_stats_file` path, which now DELEGATES through this seam (ONE commit path, no duplicate). (b) `maintenance/compute_partition_stats.rs` — `ComputePartitionStats::new(table).snapshot_id(id).execute(catalog)` is the port of Java `ComputePartitionStats`/`BaseComputePartitionStats`: resolve snapshot (default current) → `compute_and_write_stats_file` → register via the seam → `ComputePartitionStatsResult{table, statistics_file}` (Java `Result.statisticsFile()`); the `Ok(None)` empty-collection case surfaces as a typed `DataInvalid` (Java `execute()` returns non-null, but there is no file to return). Wired into `ActionsProvider::compute_partition_stats` (row 151). 5 seam tests + 5 action tests (live compute/write/register over a partitioned throwaway MemoryCatalog table, explicit-snapshot, unknown-snapshot, unpartitioned-error, no-current-snapshot). |
| Table-level statistics (`UpdateStatistics`) | ✅ | `api/UpdateStatistics.java` | `transaction/update_statistics.rs` |
| Metadata inspection tables | 🟡 | `core/.../*Table` (~15 variants) | `inspect/` — the COMPLETE Java table set: `snapshots`, `manifests`, the `files` family, `entries`, `history`/`refs`/`metadata_log_entries`, `partitions`, the five `all_*` tables, and the `readable_metrics` virtual column; all SQL-queryable via `iceberg-datafusion`. **Inspection interop COMPLETE (2026-06-09→10, Direction-1 — read-only tables):** pure-metadata (offline JSON) + manifest-reading A1–A3 + scan-planning A4 + `readable_metrics` (run.sh-driven). Documented residual divergences keep the row 🟡: empty-partition column kept on unpartitioned tables (Java drops it), cross-spec partition-type unification deferred, interior readable_metrics field-id order (Java HashMap-order is non-portable; compare by name). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Name mapping (schema-less Parquet) | ✅ | `mapping/` | `spec/name_mapping/` |
| Events / listeners | ✅ (2026-06-19 — ported + events genuinely fire on real scans/commits, unit-proven) | `api/events/` (`Listener`, `Listeners`, `ScanEvent`, `IncrementalScanEvent`, `CreateSnapshotEvent`), fired from `core/.../SnapshotScan`/`BaseIncrementalScan`/`SnapshotProducer` | `events/mod.rs` — 1:1 port of Java's process-global event bus: the `Listener<E>` trait (`notify(&self, &E)`), the static registry (`register<E>`/`notify_all<E>` over a `LazyLock<RwLock<HashMap<TypeId, Vec<Box<dyn Any+Send+Sync>>>>>`, std-only — no new dep), and the 3 immutable event structs with Java-exact field/arg order + read accessors (`ScanEvent`, `IncrementalScanEvent` incl. `is_from_snapshot_inclusive()`, `CreateSnapshotEvent`). `notify_all` keys on EXACT `TypeId` (Java's `event.getClass()`, no supertype walk), CLONES the matching listeners and DROPS the read guard BEFORE invoking them (re-entrancy-safe + satisfies the no-lock-across-callback rule; reentrancy-deadlock test pins it). 3 emit sites WIRED + unit-proven to genuinely fire over REAL fixtures: (1) `ScanEvent` @ `scan/mod.rs` `TableScan::plan_files` after the snapshotless guard — UNBOUND row filter (`plan_context.predicate`, `AlwaysTrue` default), snapshot id + projection schema; table name threaded from `identifier()` at `build()`; unguarded like Java `SnapshotScan` (panic propagates — pinned). (2) `CreateSnapshotEvent` @ `transaction/mod.rs` `do_commit` AFTER `update_table` succeeds, ONE per `TableUpdate::AddSnapshot` (property-only commit fires none — pinned), BEST-EFFORT `catch_unwind` isolation so a panicking listener never fails a committed tx (pinned); the event `summary` IS the snapshot's `additional_properties` map = Java's `snapshot.summary()` map, which EXCLUDES the `operation` key (`SnapshotProducer.summary`/`SnapshotSummary.Builder.build` build only the `total-*`/`added-*`/`changed-partition-count` keys; `operation` is carried separately via the `CreateSnapshotEvent.operation()` accessor, mirrored by Rust's typed `operation` field), pinned byte-for-byte incl. `!summary.contains_key("operation")`. (3) `IncrementalScanEvent` @ `scan/incremental.rs` `plan_files` for BOTH the append scan AND the changelog scan (Java fires from the SHARED `BaseIncrementalScan`), re-resolving the collapsed `Option` `from`: present → `(id, inclusive=false)`; absent → `(oldest-ancestor-of-to, inclusive=true)` (both branches pinned). Empty scans fire nothing (guards precede every emit — pinned). Global-static test isolation: a crate-wide `events::test_support` process lock (serialize + clear) PLUS a per-thread arm gating dispatch under `cfg(test)` so concurrent non-event tests stay silent (prod dispatch is unconditional); 14 new tests green, full lib suite 2584 green ×3. Residue (NAMED): interop is N/A (in-process bus, no on-disk/wire artifact); table-name is `namespace.name` vs Java `table.name()` (catalog-qualified) — cosmetic; the commit-site best-effort swallows the panic SILENTLY (no `tracing` dep yet, same deferral as `metrics`). |
| Type utilities (prune/assign-ids/reassign/check-compat) | ✅ | `types/TypeUtil.java`, `types/CheckCompatibility.java`, `types/AssignFreshIds.java`, `types/ReassignIds.java`, `types/GetProjectedIds.java`, `types/PruneColumns.java` (all 1.10.0) | `spec/schema/` — full engine-agnostic `TypeUtil` port (2026-06-18), each fn bytecode-decoded from the 1.10.0 jars: prune/project (`prune_columns.rs`+`utils.rs`), assign-fresh-ids, reassign-ids, check-compatibility, get-projected-ids. All public + pure + Java-faithful, each pinned by a unit test (no dead code). [full narrative](archive/2026-06_matrix-cell-narratives.md#type-utilities-pruneassign-idsreassigncheck-compat) |
| Conflict-detection + `caseSensitive` + append-only builder surfaces on write actions | ✅ (full Java 1.10.0 public builder surface ported; the two former "DeleteFiles/ReplacePartitions `conflictDetectionFilter`" residue items are VOID — `javap`-proven NOT in the 1.10.0 API; `validateAppendOnly` ported 2026-06-16) | `api/DeleteFiles.java`, `api/OverwriteFiles.java`, `api/RowDelta.java`, `api/ReplacePartitions.java`, `core/MergingSnapshotProducer.java` | `transaction/snapshot.rs` (`validate_no_new_deletes_for_data_files` + the `files_after` walk) — the `MergingSnapshotProducer.validate` stack is implemented; `case_sensitive(bool)` landed 2026-06-13, interop-proven ✅ 2026-06-16 on `DeleteFiles`/`OverwriteFiles`/`RowDelta`; `validateAppendOnly` ported 2026-06-16. The two former `conflictDetectionFilter` residue items are VOID (`javap`-proven NOT in the 1.10.0 API). [full narrative](archive/2026-06_matrix-cell-narratives.md#conflict-detection--casesensitive--append-only-builder-surfaces-on-write-actions) |
| `DeleteFiles.deleteFromRowFilter(Expression)` (delete-by-predicate) | ✅ (2026-06-16, with 2 named fail-safe divergences) | `api/DeleteFiles.java` (`deleteFromRowFilter`), `core/StreamingDelete.java` | `transaction/delete_files.rs` `delete_from_row_filter(Predicate)` (2026-06-13) — per-file partition-residual KEEP/DELETE/PARTIAL-error via the shared `resolve_filter_deletes` (reused, not forked); 10 unit tests, 6/6 mutations caught (two-spec residual + by-path/filter dedup count); `operation()`=Delete constant, bytecode-verified vs 1.10.0. **Bidirectional interop ✅ 2026-06-16 (Wave 3):** the delete RESOLUTION (which file set is removed) agrees with Java `StreamingDelete.deleteFromRowFilter` BOTH directions over a PARTITIONED `identity(Category)` `{id,Category,y}` table — `filter_delete_partition` (DELETE cat=a, survivors={b}) + `filter_keep_complement` (DELETE cat=b, survivors={a}) + `filter_partial_error` (some-but-not-all ⇒ ERROR) — each engine runs the symmetric delete on the OTHER's on-disk manifests + a fail-closed sabotage battery (semantic-rename + structural-truncate), via `dev/java-interop/run-interop-builder-flips.sh` + `crates/iceberg/tests/interop_builder_flips.rs`. **Two named fail-safe divergences (Rust is STRICTER, never wrongly deletes/loses data):** (1) `markedForDelete` short-circuit unported — Rust errors where Java would delete a by-path file that ALSO partially matches (pinned by `delete_files.rs` `..._bypath_and_partial_match_diverges_failsafe`; fix belongs in `snapshot.rs`, deferred); (2) an empty-match filter (no live file matches) — Rust REJECTS the empty no-op commit, Java COMMITS a no-op Delete snapshot (live-oracle finding, kept out of the interop set as orthogonal to the capability). `caseSensitive(boolean)` co-proven on the same builder — see row 144. |
| `ScanTaskGroup<T>` + `Scan.planTasks()` split/grouping + `SplittableScanTask.split` + `BatchScan`/`ContentScanTask<F>`/`DataTask` | ✅ | `api/ScanTaskGroup.java`, `api/BaseScanTaskGroup.java`, `api/SplittableScanTask.java`, `api/BatchScan.java`, `api/ContentScanTask.java`, `api/DataTask.java` | **LANDED + interop-proven ✅ both directions 2026-06-17 (U1 split/bin-pack core + U2 typed `BatchScan`).** `scan/task_group.rs` (`ScanTaskGroup` trait + `CombinedScanTask`); `scan/task.rs` `FileScanTask::split(target)` ports Java `BaseContentScanTask.split` (offsets-aware — one sub-task per strictly-ascending split-offset, target IGNORED; else fixed-size `min(target,remaining)`; PUFFIN non-splittable → self; sub-tasks clone deletes/residual/partition, only start+length change) with a flagged-additive `split_offsets` field threaded from the manifest entry; `scan/bin_pack.rs` (faithful `BinPacking.PackingIterator` — `largestBinFirst` eviction, FIFO find/drain, lookback; weight `max(len + contentSizeInBytes(deletes), (1+#deletes)·openFileCost)`, DV→blob-size); `TableScan::plan_tasks()` sits ABOVE an unchanged `plan_files()` (the byte-unchanged/no-reporter invariant holds), builder `with_split_size`/`with_split_lookback`/`with_split_open_file_cost`, props `read.split.target-size`/`planning-lookback`/`open-file-cost` defaults 134217728/10/4194304 + scan-override. 30 offline tests (bin-pack eviction order/tie-break + open-file-cost floor, offsets-vs-fixed split, length conservation, MoR delete-bytes weight; 3 mutation-baits). **Bidirectional interop ✅** (`dev/java-interop/run-interop-scan-plan.sh` + `tests/interop_scan_plan.rs` + `ScanPlanOracle`): Rust `plan_tasks` group SHAPE == Java REAL `table.newScan().planTasks()` — the multiset of per-group `{(basename,start,length)}` sets + group count — BOTH directions (D1 Rust-plans-Java-table = 11 groups; D2 Java-plans-Rust-table = 16 groups, `0 failures`), anti-circular knobs 4096/5/0 hand-declared both sides, fail-closed sabotage (large-target re-pack 11→1; dropped split-offsets 8→2). **U2 — the typed `BatchScan` scan-kind (`scan/batch.rs`, row 122) is now ✅:** a `BatchScanAdapter`-shaped thin adapter (`Table::batch_scan()`) delegating `plan_tasks`/`plan_files` to THIS pipeline + the `use_snapshot`/`use_ref`/`as_of_time` selectors, interop-proven via the same `ScanPlanOracle` (`newBatchScan().planTasks()` == `newScan().planTasks()` in Java AND == Rust). **Remaining deferred (separate surfaces, do NOT block this row):** `DataTask` is the metadata-table row producer (separate inspect surface); `ContentScanTask<F>` generic not separately exposed (`FileScanTask` is the concrete `ContentScanTask<DataFile>`). |
| `ExpressionParser` JSON serialization (`toJson`/`fromJson`) | ✅ | `core/.../expressions/ExpressionParser.java` | `expr/expression_parser.rs` (2026-06-17) — canonical codec over `Predicate`: `to_json` emits the bytecode-confirmed wire shape (`{type,term,value}` / `{type,term,values}` / logical `{and,or,left,right}` / `{not,child}` / bare `true`/`false`), op hyphen-map (`is_null`→`is-null`…), and `SingleValueParser` value forms (int/long→JSON number, float/double→`Float`/`Double.toString` number-token `1.0E10`/`1.0E-4`, date/time/ts/tstz→ISO string with JDK fractional-trim, decimal→plain string, binary/fixed→UPPER hex, uuid lowercase); schema-aware `from_json(_, &Schema)` recovers the typed `Datum` from the bound field and REJECTS transform/aggregate terms (`FeatureUnsupported`); DEPTH-LIMITED recursion. Wired into `ScanReport.filter` (`metrics/mod.rs`) via custom serde; deser uses the schema-less untyped path matching Java `ScanReportParser`. **Interop ✅ BOTH directions 2026-06-17** (`tests/interop_expression.rs` + `run-interop-expression.sh` + `ExpressionParserOracle`): Rust `to_json` byte-matches Java REAL `ExpressionParser.toJson` AND Java `fromJson(rust_json,schema)`+bind+toJson byte-matches, over a 34-expr battery (every op + every value type incl. float/double + logical + always-true/false); set `in`/`not-in` compared set-semantically (unordered); anti-circular (Java builds the battery independently); 4-sabotage battery (op-hyphen + date-value + float-value corruption) fails closed. **Named residue (scoped OUT of byte-1:1):** float/double values where JDK 11 `FloatingDecimal` is NON-minimal (large-magnitude / long-mantissa, e.g. JDK 11 `6.0372357323402578E18` vs the shortest `6.037235732340258E18`; float quirks `9.9999999E14`/`1.00000003E16`) — Rust emits the minimal shortest-round-trip form (== JDK 19+), diverging only from the JDK 11 oracle; pinned by unit `float_residue_is_jdk_nonminimal`. Non-finite floats (`Infinity`/`NaN`) rejected. |
| `UnboundAggregate`/`BoundAggregate`/`AggregateEvaluator` (count/min/max) + `Unbound`/`BoundExtract` | 🟡 | `api/.../expressions/{UnboundAggregate,BoundAggregate,AggregateEvaluator,UnboundExtract,BoundExtract}.java` | `expr/visitors/aggregate_evaluator.rs` (2026-06-17) — the aggregate tree (`UnboundAggregate{count_star/count/min/max}` → `bind` → `BoundAggregate`) + `AggregateEvaluator` that folds count/min/max from manifest `DataFile` METRICS with NO data scan, mirroring Java `CountStar`/`CountNonNull`/`MinAggregate`/`MaxAggregate`/`AggregateEvaluator` (`javap`-confirmed 1.10.0). **Fold (Java-exact):** `count(*)`=Σ`record_count`; `count(col)`=Σ(`value_count[id]`−`null_value_count[id]`, null default 0, checked sub/add on `u64`); `min`=min over `lower_bounds[id]`, `max`=max over `upper_bounds[id]` via typed `Datum::partial_cmp`. **Not-pushable invalidation (the staller, mirrors `NullSafeAggregator.update`):** a required metric missing for ANY file latches the aggregator invalid — `count(col)` needs BOTH `value_counts` AND `null_value_counts` to hold `id`; `min`/`max` need the bound present OR an all-null column (`value_count>0 && null_count==value_count`, the Java `allNull` short-circuit — no bound to contribute, stays valid); `all_valid()`=false ⇒ `results()` yields `None` so the engine falls back to a real scan, never a silently-wrong partial. 17 unit tests incl. all the cant-push cases; mutation-proven (drop the invalidation ⇒ 5 cant-push tests fail). **`Bound`/`UnboundExtract` CUT (frontier-parked variant-shredding):** the only aggregate term modelled is a column `Reference` — there is no `Extract` term to build, so a shredding path cannot enter the tree; a future `Extract` term bound through an aggregate must be rejected (`FeatureUnsupported`). 🟡 not ✅: code + unit tests land; INTEROP DEFERRED (no live-Java round-trip of `AggregateEvaluator.result` over Java-written manifests yet) and the scan-side push-down wiring that consumes this surface is not yet built. |
| Catalog trait accessors: `name()`, `properties()`, `commitTransaction(List<TableCommit>)`, `invalidateTable`/`invalidateView` | 🟡 (4 accessors landed + unit-tested 2026-06-17; `commitTransaction` split out; local-value only, no interop) | `api/catalog/Catalog.java` (`name`/`invalidateTable` defaults), `api/catalog/ViewCatalog.java` (`invalidateView` default), `api/catalog/SupportsNamespaces.java`, `core/.../rest/RESTCatalog` (`properties`/`commitTransaction`) | `crates/iceberg/src/catalog/mod.rs` — four NON-BREAKING `Catalog`-trait default methods landed: `name() -> &str` (default = `UNNAMED_CATALOG` sentinel, mirroring Java `Catalog.name()`'s `Object.toString()` default body — javap-confirmed `public default`), `invalidate_table(&TableIdent) -> Result<()>` (default no-op = Java's empty-`return` default body), `invalidate_view(&TableIdent) -> Result<()>` (default no-op = Java `ViewCatalog.invalidateView` default body), and `properties() -> &HashMap` (default empty map). **`properties()` is a DOCUMENTED Rust-convenience, NOT claimed as a Java `Catalog`-interface method** — javap confirms `properties()` is absent from `Catalog`/`ViewCatalog`/`SupportsNamespaces` and is, on the plain-`Catalog` surface, concrete only on `RESTCatalog` (it also appears on `SessionCatalog`/`BaseSessionCatalog`); the default is surfaced so every impl can expose its load-time props the way `RESTCatalog.properties()` does. `name()`/`properties()` are OVERRIDDEN per impl from held config: REST (`user_config.name`/`props`), Glue/HMS/S3Tables (`config.name`/`props`), SQL (`name` field + a newly-retained `properties` field), MemoryCatalog (a 1-field fix — `new()` now RETAINS `name`+`props` it previously dropped). `#[automock]` auto-derives the mock for the new defaults (no manual mock churn); external implementors inherit the defaults unchanged (non-breaking). Unit tests: per-impl configured-name/props round-trip (memory/sql/rest/glue/s3tables, all offline; glue/s3tables construct offline with no network), default no-op `invalidate_*`, and a bare-impl test pinning the trait defaults (sentinel name + empty props + no-op invalidates); each accessor has a mutation guard (empty/sentinel fails). HMS override is compile-verified only (its `new()` resolves a socket address, not offline-constructible). **Split out / deferred:** `commitTransaction(List<TableCommit>)` (REST-server multi-table atomic commit, concrete on `RESTCatalog` only) is its own future unit, not built here. Row stays 🟡 (not ✅): these are local-value accessors with no Java interop round-trip (a `name()`/`properties()` interop assertion + the `commitTransaction` unit are the path to ✅). |
| `RewriteFiles` DELETE-file rewrite surface (`addFile(DeleteFile)`, 4-arg `rewriteFiles`) | 🟡 | `api/RewriteFiles.java` (`addFile(DeleteFile)`/`addFile(DeleteFile,long)`, 4-arg `rewriteFiles`) | `transaction/rewrite_files.rs` `add_delete_file`/`add_delete_files` + the explicit-seq overload (Java `addFile(DeleteFile,long)`) + the 4-set `rewrite_files` (data/delete replace+add) (2026-06-14) — added deletes route through the shared `snapshot.rs` `write_added_delete_manifests` (REUSED from RowDelta, not forked); explicit seq stamped vs inherited per `BaseRewriteFiles` (javap: `pendingDeleteFile(file,null)` inherit vs `Long.valueOf(seq)` explicit); negative explicit seq REJECTED fail-loud (manifest-writer strip → resurrection). 10 unit tests, 10/10 mutations caught incl. seq-strip + 4-arg cross-wire resurrection (scan-level) + shared-helper-from-RowDelta; the 3 preconditions byte-match the Java strings. Interop DEFERRED → 🟡. |
| `ActionsProvider` + `DeleteReachableFiles` + `ConvertEqualityDeleteFiles` actions; `SupportsNamespaces` partial property set/remove | ✅ (2026-06-17 — all components built; `ConvertEqualityDeleteFiles` is read-identity-proven offline + interop, G4 capstone) | `api/actions/{ActionsProvider,DeleteReachableFiles,ConvertEqualityDeleteFiles}.java`, `api/catalog/SupportsNamespaces.java` | `maintenance/{actions_provider,delete_reachable_files,convert_equality_delete_files}.rs` (2026-06-16/17) — `ActionsProvider` entry point (pub trait, 1:1 with Java's 12 default methods) + `DeleteReachableFiles` (DROP TABLE PURGE engine) + `ConvertEqualityDeleteFiles` (eq→pos, read-identity-proven offline + interop, G4 capstone) + `SupportsNamespaces` partial property set/remove. All components built. [full narrative](archive/2026-06_matrix-cell-narratives.md#actionsprovider--deletereachablefiles--convertequalitydeletefiles-actions-supportsnamespaces-partial-property-setremove) |
| LOW-importance backlog (consolidated) | ❌ | various `api/`/`core/` | One row for the small-surface residue so the plan can see it without bloat: variant predicates + variant metrics; `MetricsConfig`/`MetricsModes`; `SnapshotUpdate.toBranch`; `ExpressionUtil` helpers + `Literal.to(Type)`; `CatalogProperties` name constants. Each is a narrow add; none gates a phase. Promote to its own row when work starts. |

## Headline gaps (ranked by effort × value)

1. **Write engine** — everything beyond fast-append (`RowDelta`, `RewriteManifests`, merge append).
   **Started 2026-06-07: `DeleteFiles` 🟡 + `OverwriteFiles` 🟡 + `ReplacePartitions` 🟡 + `RewriteFiles` 🟡.**
   `DeleteFiles` delete-by-path/reference + the foundational manifest-filter / rewrite machinery in
   `SnapshotProducer::process_deletes`; `OverwriteFiles` composes that delete path with the fast-append
   add path in one `Overwrite` snapshot, reusing the now-shared `SnapshotProducer::{resolve_delete_paths,
   current_manifests}` (factored out of `DeleteFiles`; `current_manifests` carries DATA **and** DELETE
   manifests forward — Increment 2b 2026-06-10 — so outstanding MoR deletes survive every delete-bearing
   commit); `ReplacePartitions` (dynamic partition
   overwrite) adds a by-PARTITION sibling resolver `SnapshotProducer::resolve_partition_deletes` that feeds
   the SAME `process_deletes` rewrite path (every partition an added file belongs to is replaced; an
   unpartitioned table is a full replace; `replace-partitions=true` summary marker); `RewriteFiles` (the
   compaction-commit primitive) replaces a set of DATA files with a new set in one `Replace` snapshot,
   reusing the by-path `resolve_delete_paths` + `process_deletes` machinery unchanged (Java
   `BaseRewriteFiles`: `operation()` = `REPLACE`, delete set must be non-empty, `failMissingDeletePaths`).
   All four `MemoryCatalog`-tested (post-commit SCAN live set + cross-partition isolation + provenance +
   compaction-to-fewer-files). Deferred: `overwriteByRowFilter` / delete-by-row-filter / static
   `replaceByRowFilter` (metrics evaluators), DELETE-file rewrite + `dataSequenceNumber` preservation
   (delete-file write path), concurrent-commit conflict validation (serializable isolation), and
   data-level Java interop.
2. **Schema/partition evolution + snapshot management** — `UpdateSchema`, `UpdatePartitionSpec`, **and the
   `ManageSnapshots` ref-operation surface** are now **✅** (bidirectional Java interop round-trips landed
   2026-06-07 via the `dev/java-interop/` oracle + the `crates/iceberg/tests/interop_update_schema.rs`,
   `interop_update_partition_spec.rs`, and `interop_manage_snapshots.rs` Direction-1 tests; one
   `generate`/`verify` pass covers all THREE capabilities — 7 schema + 7 partition + 7 manage-snapshots
   scenarios, both directions, all 21 PASS). The `UpdatePartitionSpec` interop surfaced (and fixed,
   in-scope) a real Rust↔Java divergence: the partition-name↔schema collision check was identity-only and
   rejected the legitimate V1 void replacement; relaxed to identity-OR-void (source-id-gated) to mirror
   Java's bind path. `UpdateSchema` column initial/write **defaults** are plumbed and
   `Schema::check_compatibility` (in `TableMetadataBuilder::add_schema`) now mirrors Java
   `Schema.checkCompatibility` in FULL — both the **V3-only initial-default guard** (rejects a non-null
   `initial_default` below v3) AND the **V3-only type gate** (rejects a `timestamp_ns`/`timestamptz_ns`
   field, incl. nested, below v3). For `ManageSnapshots`, both the **"Snapshot model + refs"** row and the
   **ref-operation surface** of the **"Snapshot management"** row are now ✅; **`cherrypick` landed 2026-06-11**
   (`transaction/cherry_pick.rs` — the WAP publish commit replaying data files via `SnapshotProducer`) and is
   **🟡 unit-proven, interop deferred** — it is a standalone action, not part of the `manage_snapshots`
   ref-op surface. The manage-snapshots interop surfaced a
   read divergence the Rust V2/V3 snapshot reader required `sequence-number`, while the spec
   (`format/spec.md` lines 1979 & 2002) and Java's `SnapshotParser` both default an absent
   `sequence-number` to 0 on read; **FIXED 2026-06-07** with `#[serde(default)]` on
   `_serde::SnapshotV2`/`SnapshotV3.sequence_number`, so Rust now reads the seq-0 V1→V2-upgrade-carryover
   table class Java emits. The sibling spec-mandated default-to-0 read fields on the **Avro manifest /
   manifest-list path** — manifest-list `content`/`sequence-number`/`min-sequence-number`; manifest-entry
   `sequence_number`/`file_sequence_number`; data-file `content` — were **VERIFIED already-correct and pinned
   2026-06-07** (per-field empirical proof + mutation-verified regression tests; see the "Manifest +
   manifest-list read/write" row). The table-metadata **`last-sequence-number`** sibling is CLOSED (Arc G
   2026-06-11): Rust's strict-required-on-V2/V3 read ALREADY MATCHES Java 1.10.0 — bytecode shows
   `TableMetadataParser.fromJson` reads it via `JsonUtil.getLong` (THROWS when absent) for `formatVersion>1`
   and hard-defaults to 0 for V1; spec line 179 explicitly permits the V2 throw. The proposed
   `#[serde(default)]` "fix" would have DIVERGED (accepting V2 metadata Java rejects); strict is correct and
   is now pinned.
3. **Format & type breadth** — ORC + Avro data files; remaining V3 types (variant, geo, unknown). The
   V3-only **type** gate (Java `Schema.MIN_FORMAT_VERSIONS`: `timestamp_ns`/`variant`/`unknown`/`geometry`/
   `geography` require v3) is now **enforced for the representable types** (`timestamp_ns`/`timestamptz_ns`,
   and `variant` since F1 2026-06-11) in `Schema::check_compatibility`; `unknown`/`geometry`/`geography`
   get a one-line `min_format_version` arm each when those types land (tracked in `task/todo.md`).
   (`timestamp_ns` and column default values already landed in the 0.8/0.9 base — see the matrix.)
4. **Views in catalogs** (`ViewCatalog` + view operations).
5. **Maintenance actions** (expire/orphan/compaction/rewrite-deletes/compute-stats/migrate).
6. **Encryption** (`EncryptionManager`, KMS, encrypted FileIO/manifests).
</content>
</invoke>
