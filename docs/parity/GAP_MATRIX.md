<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# Java ↔ Rust Capability Gap Matrix

> **Goal:** 1-to-1 capability parity between this Rust implementation and the Apache Iceberg
> **Java `iceberg-core` / `iceberg-api`** library (the engine-agnostic table-format library — *not*
> the Spark engine integration). This is a **living document**: re-run the audit after every upstream
> sync and after each parity phase lands.

## Status legend

- ✅ **present** — implemented to a usable degree
- 🟡 **partial** — exists but incomplete vs Java
- ❌ **missing** — not implemented

> **Two ✅ bars — read the count accordingly (clarified 2026-06-16).** Per the parity mandate
> ([AGENTS.md](../../AGENTS.md)), a row the FORK actively flips to ✅ requires unit tests **and** a
> bidirectional Java interop round-trip. **Base-inherited upstream primitives** (data/eq-delete/partitioning
> writers, the parquet reader, FileIO, transforms, name-mapping, `upgrade_format_version`, …) carry their ✅
> on the weaker "implemented + usable" bar and are **exempt from the fork's interop requirement** — they
> were proven upstream, not re-interop'd here. So the headline ✅ count is **not** uniformly an
> "interop-proven" count; ~half of the greens are fork-flipped-with-interop and the rest are
> inherited-and-usable. When a base-inherited ✅ row has an un-proven slice under the strict bar, the cell
> names it (e.g. `upgrade_format_version`).

## Audit provenance

- **Rust base audited:** owned fork on upstream **`iceberg` 0.9.1** (dependency family bumped
  2026-08-05 to datafusion 54.1, arrow 58.4, parquet 58.4, `orc-rust` 0.8; MSRV 1.94), **re-audited 2026-06-07** after
  the Phase 0 sync, **resynced to reality 2026-06-13** (post R1/R2/R3), **last refreshed 2026-06-19**
  (audit P1 panic-hardening + QUAL-01 tracing + `LockManager` in-memory impl landed; the `BatchScan` ✅, ORC+Avro READ 🟡,
  `RewriteTablePath` 🟡, and events ✅ rows are reflected): status flips (`merge_append` 🟡→✅ data-level interop both directions;
  `LockManager` ❌→🟡 (in-memory impl landed 2026-06-19); `Catalogs` umbrella 🟡 — Glue/S3Tables view ops + Catalog accessors
  residue surfaced), partition-stats de-dup (one home on the Maintenance row), and 10 new rows for
  previously-untracked Java capabilities (conflict-detection/`caseSensitive` builders,
  `deleteFromRowFilter`, `ScanTaskGroup`/`planTasks`, `ExpressionParser` JSON, aggregate expressions,
  Catalog accessors, `RewriteFiles` add-delete, `ActionsProvider`+actions, a consolidated LOW backlog).
  Test count measured 2026-06-19: **2,593** `#[test]`/`#[tokio::test]` in `crates/iceberg/src`, **2,721**
  incl. `crates/iceberg/tests`, **≈3,125** workspace-wide (`grep -rho '#\[\(tokio::\)\?test\]'`). Source
  of truth: `crates/iceberg/src/{spec,expr,scan,transaction,writer,arrow,io,inspect,puffin,catalog,maintenance,variant,events,metrics}`
  plus the `crates/storage/opendal` FileIO crate and `crates/catalog/{glue,s3tables}`.
- **Java reference:** `apache/iceberg` `main`, modules `api/` + `core/` + `data/` + `orc/` +
  `parquet/` + `arrow/`.
- **Re-audit policy:** re-run after every upstream sync and after each parity phase; date-stamp this
  block and strike the rows the sync/phase solved.
- **Interop oracle (test-only):** `dev/java-interop/` drives Java `iceberg-core` **1.10.0** as a
  read/write oracle for bidirectional fixtures (not shipped, not a Cargo dependency). One
  `generate`/`verify` pass now covers all THREE Phase-1 evolution capabilities — it backs the
  ✅-by-interop rows `UpdateSchema`, `UpdatePartitionSpec`, and the `ManageSnapshots` ref-operation surface
  (incl. "Snapshot model + refs"), all dated 2026-06-07.
- **What the 0.7→0.9.1 sync changed (flipped rows below):** `timestamp_ns` type, column default
  values (`initial_default`/`write_default`), merge-on-read **read** application of position-deletes +
  deletion-vectors during scan *(2026-06-10 audit correction: the sync brought PARQUET
  position/equality deletes only — the deletion-vector scan path was a loader TODO until
  Increment D1; see the merge-on-read read row)*, the `upgrade_format_version` transaction action, and a real
  `TransactionAction`/`ApplyTransactionAction` extension seam. The **headline gaps are unchanged**:
  write-engine actions, schema/partition/snapshot evolution, incremental scans, ORC/Avro data files,
  variant/geo/unknown types, catalog view ops, maintenance actions, encryption.
- **Cell style (de-triplication pass, 2026-06-10):** this matrix is the ONLY status record (the
  one-home-per-fact rule in [AGENTS.md](../../AGENTS.md)). Cells stay TERSE — Rust location, a 1–2
  sentence summary, flip dates, links. The full per-increment narratives the cells used to carry
  moved verbatim to [archive/2026-06_matrix-cell-narratives.md](archive/2026-06_matrix-cell-narratives.md)
  (grep by row Area). When a status flips: update the cell here, link evidence, write the narrative
  in the todo/lessons flow — do NOT grow the cell back.

## Matrix

| Area | Status | Java reference | Rust location / note |
|---|---|---|---|
| R87 · Primitive + nested types | ✅ | `api/.../types/Types.java` | `spec/datatypes.rs`. **Type-name case fold (O3-a, 2026-06-12):** `PrimitiveType` deserialize lowercases the name (incl. `decimal(..)`/`fixed[..]`) + the variant marker folds case, matching Java `Types.fromTypeName`'s `toLowerCase(Locale.ROOT)` (1.10.0 bytecode); object WRAPPERS `struct`/`list`/`map` are NOT folded (Java `SchemaParser.typeFromJson` uses `String.equals`) — scoped, mixed-case + negative pins. **O3 REVIEWER closures (2026-06-12, live-Java-probed):** (1) `fixed[..]`/`decimal(..)` parse now mirrors Java's anchored regex exactly — inner whitespace (`fixed[ 16 ]`, `decimal( 38 , 2 )`) ACCEPTED, missing close bracket/paren (`fixed[16`, `decimal(38,2`) REJECTED (was: opposite both ways via `trim_end_matches`); (2) the `Type` read path now rejects wrong-cased WRAPPER names (`{"type":"STRUCT"/"LIST"/"MAP"}`) via `SerdeType::wrapper_type_mismatch` re-imposing Java's `String.equals` — closes the read-leniency the builder documented (the untagged enum had ignored the `type` string). Both bytecode + live `Types.fromTypeName`/`SchemaParser.fromJson` oracle-confirmed, mutation-pinned. **Decimal read-gate correction (2026-08-09):** the `decimal(P,S)` READ path now applies Java `Types$DecimalType.<init>`'s ONLY rule — `precision <= 38` (`ensure_java_decimal_precision`, `spec/datatypes.rs`) — not the deliberately stricter `Type::decimal` CONSTRUCTOR, which also demands `precision >= 1` and `scale <= precision`. Live-probed on the 1.10.0 jars: Java loads `decimal(0,0)`, `decimal(1,2)` and `decimal(10,11)`, and only `decimal(39,0)` throws. Pinned by `java_legal_decimal_type_strings_still_deserialize`. |
| R88 · V3 types: variant | 🟡 | `api/.../variants/`, `core/.../variants/`, `api/.../types/Types.java` (`VariantType`) | `variant/` — binary format BOTH sides: READ (2026-06-11; metadata dict, all 21 primitive ids, short strings, objects, arrays; bounds-checked) + WRITE (B2 2026-06-11; `Variants` factories, shredding, Avro `metadata`+`value` shape with `r<fieldId>` naming). Stays 🟡 on file-level I/O + interop. SCOPE CORRECTED 2026-08-24: shredded-PARQUET variant is NOT a parity gap — the 1.10.0 `iceberg-core` jar carries variant classes only under `avro/` and `variants/` (`ValueReaders$VariantReader`, `ValueWriters$Variant*Writer`, `VariantConversion`, `VariantLogicalType`); Java's parquet variant lives in the `iceberg-parquet` module, OUTSIDE the parity scope, so a shredded-parquet port would be a fork-only extension gated on the `parquet` crate's experimental `variant_experimental` feature. The IN-SCOPE remainder is variant over AVRO, which needs no dependency change: the Avro→Iceberg SCHEMA direction is already correct (`avro/schema.rs:722` returns `Type::Variant`; the throw at :455 is the visitor DEFAULT, mirroring Java's own default). What is unbuilt is the DATA read/write plumbing — `arrow/avro_reader.rs`'s `Type::Variant` arm refuses ("Avro data-file read of the variant type is not supported yet") — plus file-level parquet I/O. FEATURE NOW ENABLED (2026-08-24, user-approved): `parquet`'s `variant_experimental` is on, adding parquet-variant / -json / -compute at 58.4.0, and `arrow/schema.rs`'s `variant()` arm now emits the CANONICAL Arrow extension type (`arrow.parquet.variant`, a `Struct<metadata: Binary, value: Binary>`) in BOTH directions, keyed on the field's extension name rather than the struct's shape. That is a deliberate EXTENSION beyond Java, which throws here. **BREAKING:** `type_to_arrow_type` and `schema_to_arrow_schema` are `pub` and previously returned `Err` for `Type::Variant`; they now return `Ok`. A downstream that gated on that error proceeds instead — both in-tree seams refuse variant I/O explicitly (`ArrowReader::reject_variant_projection`, `ParquetWriterBuilder::build`), but an external writer built on the conversion no longer gets a schema-level refusal. Still owed for file-level I/O: the Iceberg→`ShreddedSchemaBuilder` bridge, the Avro data read (`arrow/avro_reader.rs`'s variant arm refuses), and an interop leg. SCANNING a variant column is REFUSED reader-side (`ArrowReader::reject_variant_projection`) and WRITING one is refused at `ParquetWriterBuilder::build`, before any bytes reach storage — both at any depth — top level, struct field, list element, map KEY and map value. (The key position was missed at first, defended by the false premise that Iceberg map keys are primitives: Java's `Types$MapType` factories constrain only the VALUE type.) That refusal replaced the incidental one the Iceberg→Arrow throw used to provide, and is a deliberate bound on the capability, not a parity gap: Java refuses the whole conversion. RISK: the upstream `parquet::variant` module labels itself experimental and work-in-progress, so its API may move on any family bump. [full narrative](archive/2026-06_matrix-cell-narratives.md#v3-types-variant) **Scope-correction caveat (2026-08-25):** the audit's finding that shredded-PARQUET variant I/O is outside parity scope is a statement about the FORK's parity envelope only — Java's shredding lives in `iceberg-parquet`, not `iceberg-core`, so it cannot be a `iceberg-core` parity obligation. It does NOT remove shredded variant from RePark's v1.0 gate; that is an owner ruling still to be made. If the ruling is that v1.0 needs it, this becomes a fork feature request outside the parity mandate, not a parity row. |
| R89 · V3 types: geometry / geography | ❌ (v1.0 "not yet" — DECLARED 2026-08-25) | `api/.../geospatial/` | none **Engine-gate disposition (2026-08-25):** the RePark v1.0 gate allows per-feature support OR a dated DECLARED "not yet"; this row takes the latter. Nothing exists in the fork — no `geometry`/`geography` type entry, no geospatial predicates, and the `MIN_FORMAT_VERSIONS` arms for both are the only V3 type gates still unbuilt. Revisit only on an owner ruling that a consumer writes these types. |
| R90 · V3 types: timestamp_ns | ✅ | `types/Types.java` | `spec/datatypes.rs` (`PrimitiveType::TimestampNs`/`TimestamptzNs`); **V3-only format-version gate enforced** — `Schema::check_compatibility` rejects a `timestamp_ns`/`timestamptz_ns` field (incl. nested) on a V1/V2 table (Java `Schema.MIN_FORMAT_VERSIONS`, "Invalid type for {col}: timestamp_ns is not supported until v3") |
| R91 · V3 types: unknown | 🟡 | `types/Types.java` (`UnknownType extends PrimitiveType`) | `spec/datatypes.rs` `PrimitiveType::Unknown` (Java PRIMITIVE arm, not a top-level `Type` like variant), JSON serde = bare `"unknown"` for free via `rename_all="lowercase"` + Display, case-folded read. **V3 gate (G2, 2026-06-17):** same `min_format_version`/`check_compatibility` mechanism as `timestamp_ns`/`variant` (shared-gate-pinned; a V2 schema carrying `unknown` is REJECTED, "Invalid type for {col}: unknown is not supported until v3"; mutation-verified — removing the arm reds 3 tests). Compiler-forced match arms: arrow→`DataType::Null` (Java `TypeToMessageType` = null, no physical column), avro→Avro `null` (`TypeToSchema`), datum/literal→reject (no `PrimitiveLiteral` — always-null), value-producing transforms reject via the existing catch-all (identity/void accept = Java `Identity.UNSUPPORTED_TYPES` excludes UNKNOWN), partition-stats/readable-metrics loud-error, glue schema → `"unknown"` (byte-parity: Java `IcebergToGlueConverter.toTypeString` default `typeId().name().toLowerCase` = "unknown", never throws — bytecode-verified javap iceberg-aws-1.10.0.jar, G1 2026-07-17), hms schema → loud `FeatureUnsupported` "unknown is not supported" (byte-parity: Java `HiveSchemaUtil.convertToTypeString` has no UNKNOWN `$SwitchMap` entry → default throws `UnsupportedOperationException(type + " is not supported")` — bytecode-verified javap iceberg-hive-metastore-1.10.0.jar, G3 2026-07-17; same throw covers both `timestamp_ns` variants — the fork-invented `"timestamp_ns"` HMS emission was removed, and timestamptz(µs) is the Hive-version-gated string via the `hive_version` catalog knob). **Parquet write loud (2026-09-01):** `ParquetWriterBuilder::build` refuses a schema carrying `unknown` at any depth with `FeatureUnsupported` naming the column (`Writing the unknown column '{path}'`). `DataFileWriter::write` surfaces that on the first batch (pins `data_file_writer_refuses_unknown_null_column_loud`, `data_file_writer_refuses_omitted_optional_unknown_column`, `an_unknown_schema_is_refused_before_any_bytes_are_written`). Avro already refused at build. Position-delete schema cannot carry `unknown`. Equality-delete parquet writers use a projected schema, so they refuse only when `unknown` is an equality id. **Read still deferred-loud:** `arrow/value.rs` `FeatureUnsupported` (always-null synth is a deeper change). A V3 table with `unknown` cannot emit a parquet data file until that synth lands. **Interop ✅ 2026-06-17** — metadata-only schema round-trip via `crates/iceberg/tests/interop_unknown.rs` (env-gated GEN; Java `UnknownTypeOracle` writes a V3 schema with `unknown` at every placement, Rust reads+writes, Java verifies the Rust-written struct; `dev/java-interop/run-interop-unknown.sh`). |
| R92 · Column default values (initial/write) | ✅ | `Schema`/`Types` | `spec/datatypes.rs` `NestedField` carries `initial_default`/`write_default` **Read-path application (recorded here 2026-08-25 — the fact lived only in Roadmap.md, which is not a status record):** `initial_default` is APPLIED on the Parquet (`arrow/record_batch_transformer.rs`) and Avro (`arrow/avro_reader.rs`) readers, so a column added with a default reads back as that default rather than NULL. The ORC reader REFUSES a non-null `initial-default` with `FeatureUnsupported`, mirroring Java's `UnsupportedOperationException` — a faithful refusal, not a gap, and NOT a third application site. **Write-path application (2026-08-27, F-15):** `writer/write_defaults.rs` `apply_write_defaults` fills missing top-level primitive columns from `write_default` at `DataFileWriter::write` only, so Parquet and Avro data files both get it. Delete writers do not fill. A required missing field with no `write-default` fails `DataInvalid`. An optional missing field with none writes null. A supplied column is not replaced. **Named residue:** nested/non-primitive `write-default` fill is `FeatureUnsupported`. The row keeps its base-inherited ✅; nested fill is the remaining un-proven slice. |
| R93 · Partition transforms (identity/bucket/truncate/year/month/day/hour/void) | ✅ | `api/.../transforms/` | `spec/transform.rs` Note (2026-08-16): void-transform result-type semantics are consumed by the cross-spec unifier (row R165). |
| R94 · Schema evolution (`UpdateSchema`) | ✅ | `api/UpdateSchema.java`, `core/SchemaUpdate.java`, `schema/UnionByNameVisitor.java` | `transaction/update_schema.rs` — full Java `SchemaUpdate` parity: add/rename/update-type/doc/make-optional/require/delete/move/set-identifier-fields, `union_by_name` (full `UnionByNameVisitor`), LEVEL-ORDER fresh field-id assignment, column initial/write defaults, and the V3-only initial-default + type gates (`Schema::check_compatibility` wired into `TableMetadataBuilder::add_schema`). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_schema.rs` (7 scenarios). Full narrative + edge cases: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R95 · Partition evolution (`UpdatePartitionSpec`) | ✅ | `api/UpdatePartitionSpec.java`, `core/BaseUpdatePartitionSpec.java` | `transaction/update_partition_spec.rs` — full `BaseUpdatePartitionSpec` parity: auto-naming, field-id+name recycling, V1 void replacement, guard rails; the partition-name↔schema collision guard was relaxed to identity-OR-void (a real divergence interop surfaced and fixed). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_partition_spec.rs` (7 scenarios). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). Note (2026-08-16): the multi-spec history this surface maintains is the input the cross-spec unifier walks (row R165). |
| R96 · Sort order (`ReplaceSortOrder`) | ✅ | `api/ReplaceSortOrder.java` | `transaction/sort_order.rs`. **Parse-time bind (O3-b, 2026-06-12):** `TableMetadata` normalization now runs `SortOrder.check_compatibility` on the DEFAULT sort order against the current schema (`spec/table_metadata.rs` `try_normalize_sort_order`), matching Java `TableMetadataParser`→`SortOrderParser.fromJson(schema, node, defaultId)` which `bind`s ONLY the default order (`build`→`checkCompatibility`) and `bindUnchecked`s the rest (1.10.0 bytecode); a default order on a missing/non-primitive/wrong-transform source fails to parse, non-default orders stay lenient. |
| R97 · Snapshot model + refs (branches/tags) | ✅ | `api/Snapshot.java`, `SnapshotRef.java`, `core/SnapshotParser.java` | Spec types + ref operations (`transaction/manage_snapshots.rs`). **Interop ✅ 2026-06-07** — the `SnapshotReference`/`SnapshotRetention` model round-trips with Java via `crates/iceberg/tests/interop_manage_snapshots.rs`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R98 · Snapshot management (`ManageSnapshots`: branch/tag CRUD, rollback, rollback-to-time, set-current, fast-forward) | ✅ (ref-op surface + `cherrypick`/WAP — all interop closed I3 2026-06-12) | `api/ManageSnapshots.java`, `core/SetSnapshotOperation.java`, `core/CherryPickOperation.java`, `api/SnapshotRef.java` | `transaction/manage_snapshots.rs` — branch/tag CRUD + rename, set-current, rollback (ancestry-checked), rollback-to-time (strict `<`), fast-forward, retention (non-positive rejected), no-op suppression. Interop ✅ 2026-06-07 (7 scenarios both directions, `crates/iceberg/tests/interop_manage_snapshots.rs`); `cherrypick`/WAP interop closed I3 2026-06-12. [full narrative](archive/2026-06_matrix-cell-narratives.md#snapshot-management-managesnapshots-branchtag-crud-rollback-rollback-to-time-set-current-fast-forward) |
| R168 · `SnapshotUpdate.toBranch` (branch commit target) | 🟡 (unit-proven 2026-08-30; DataFusion provider 2026-09-01; Java interop not in this unit) | `api/SnapshotUpdate.java` `toBranch`, `core/SnapshotProducer.java` `targetBranch`/`latestSnapshot`/`setBranchSnapshot` | `transaction/to_branch.rs` + `SnapshotProducer::with_target_branch`. `to_branch(name)` on FastAppend, MergeAppend, OverwriteFiles, ReplacePartitions, RewriteFiles, RowDelta, DeleteFiles. Parent, manifests, `SetSnapshotRef`, `RefSnapshotIdMatch`, and conflict walks use `SnapshotUtil.latestSnapshot` (missing branch → current snapshot; `main` does not move). A missing branch is created at commit. A tag is rejected. Retry re-resolves the named ref. Pins in `to_branch.rs`. **DataFusion (F-6b commit 2026-09-01; F-6c branch-following reads 2026-09-01):** `IcebergTableProvider::with_commit_branch` stores the name, scans the named-ref head (`snapshot_for_ref` → `IcebergTableScan::plan` and CoW/MoR DML scans), and hands it to `to_branch` at every snapshot-producing site (INSERT INTO `fast_append`, INSERT OVERWRITE `overwrite_files`, CoW/MoR DELETE, CoW/MoR UPDATE). Default (no setter) still reads and stamps `main`. A missing ref is `DataInvalid` on the read leg (SELECT, DELETE, UPDATE); INSERT without a target scan still creates the branch. Advertised schema stays current; IcebergTableScan field-id bind null-fills columns the branch snapshot lacks. `validate_from_snapshot` is the scanned snapshot (named-ref head, else current). Pins in `crates/integrations/datafusion/tests/commit_branch.rs`. Residue: no Java interop; WAP/`stage_only`+`to_branch` out of scope; `RewriteManifests`/`CherryPick` keep Java's throwing default; catalog/session/per-statement overrides out of scope. Promoted from row R156. |
| R99 · Manifest + manifest-list read/write | ✅ | `core/.../ManifestReader/Writer` | `spec/manifest`, `spec/manifest_list.rs`. Spec-mandated default-to-0 V1→V2 reads verified + pinned 2026-06-07; table-metadata `last-sequence-number` strict-on-V2/V3 read confirmed already-Java-faithful (Arc G 2026-06-11). `partition_spec_id` threaded as the final canonical-view sort tiebreaker (W3, byte-invisible for single-spec). Z2 multi-spec interop ✅ 2026-06-12 both directions (`tests/interop_multi_spec.rs`). DOCUMENTED DIVERGENCE (O3-c, cosmetic): manifest-list ENTRY ORDER differs (Rust emits carried-then-new vs Java new-then-carried) but is writer-dependent and byte-invisible after the canonical sort; restructure out of scope. [full narrative](archive/2026-06_matrix-cell-narratives.md#manifest--manifest-list-readwrite) |
| R100 · `RewriteManifests` | ✅ | `api/RewriteManifests.java`, `core/BaseRewriteManifests.java` | `transaction/rewrite_manifests.rs` — re-organize manifests in one `Operation::Replace` snapshot, live set unchanged (Java `BaseRewriteManifests`): `cluster_by` re-groups live data-manifest entries via the provenance-preserving `add_existing_entry` path (original snapshot id + seqs kept, status → Existing), `rewrite_if`, `add_manifest`/`delete_manifest`, `set`, `validateDeletedManifests`/`validateFilesCounts`. 17 `MemoryCatalog` tests + reviewer pins (seq resurrection-proofing, multi-spec clustering axis, computed-vs-`set()` precedence). Metadata-level interop ✅ 2026-06-10 (cluster-by path). Data-level interop ✅ 2026-06-19: Java writes 3 real parquet files (ids 10/20, 30/40, 50/60) in 3 separate `newAppend` commits (3 manifests); Rust `rewrite_manifests().cluster_by(…)` (constant-key closure collapses all entries) re-clusters to 1 manifest; Rust scan confirms 6 live rows match `java_rows.json`; Java re-reads the Rust-rewritten table via `IcebergGenerics` and confirms rows + manifest count (`dev/java-interop/run-interop-rewrite-manifests.sh` + `tests/interop_rewrite_manifests.rs`). Non-vacuous: corrupted `java_rows.json` (id 10→999) causes Java verify to fail with FAIL sentinel. Residue: explicit `add_manifest`/`delete_manifest` low-level mode not exercised (Java-only concern, no parity gap). [full narrative](archive/2026-06_matrix-cell-narratives.md#rewritemanifests) |
| R101 · Write: fast append | ✅ | `api/AppendFiles.java`, `core/FastAppend.java` | `transaction/append.rs` — Java `newFastAppend`. **All-tombstone-manifest carry FIXED 2026-06-11 (Wave-4 Group O / O1, the A3 STOP-finding):** `FastAppendOperation::existing_manifest` now carries EVERY prior manifest-list entry forward UNFILTERED, matching Java `FastAppend.apply` (`manifests.addAll(snapshot.allManifests(io))`, bytecode offsets 89-94 — NO predicate; `BaseSnapshot.allManifests` returns the list verbatim). The previous `has_added_files()`-OR-`has_existing_files()` filter DROPPED a manifest left ALL-DELETED by a prior copy-on-write delete that emptied it — a manifest-LIST structure divergence on any append-after-emptying-delete history (the dropped manifest became unreferenced/GC-eligible where Java keeps it referenced; bites byte-level interop + tombstone-visibility consumers like orphan-protection / changelog scans). Pinned on-disk: re-parsed-manifest-list fail-before/pass-after reproduction (add → emptying delete → fast_append CONTAINS the all-tombstone manifest), scan-unchanged pin (carry does NOT resurrect deleted rows), merge_append documented-parity pin. Note: the MERGING append (`merge_append`) intentionally keeps the filter — see that row. Interop re-proof deferred (Group S). **Multi-spec fast-append interop ✅ 2026-06-12 (Z2):** a SINGLE `fast_append` call carrying files under two different partition specs produces TWO manifests in ONE snapshot (per-spec manifest grouping), with each manifest stamped with the file's OWN spec id (file's-own-spec rule) — proven cross-language both directions via `run-interop-multi-spec.sh` + `tests/interop_multi_spec.rs`. 2026-08-25 (F-7 U1): the manifest list now carries the NEW manifests before the carried-forward ones (Java `FastAppend.apply`), and this producer alone KEEPS an added file's `first_row_id` where every merging producer clears it — both recorded in row R166. |
| R102 · Write: merge append | ✅ (data-level interop both directions, 2026-06-11; one-bin + multi-bin) | `api/AppendFiles.java`, `core/MergeAppend.java`, `core/ManifestMergeManager.java`, `core/util/BinPacking.java` | `transaction/merge_append.rs` — Java `newAppend`: append in one `Operation::Append` snapshot, then bin-pack + merge the manifest list per the `commit.manifest*` props (per-spec grouping, ported `BinPacking.packEnd`, provenance-preserving entry routing; summary shape == `fast_append`). All-tombstone-manifest carry settled as Java parity (drops them, unlike `fast_append`). Metadata-level interop ✅ 2026-06-10; row flipped 🟡→✅ 2026-06-13 with data-level interop both directions (fixture A one-bin + fixture G multi-bin) via `tests/interop_write_data.rs`; multi-spec merge interop is named residue. [full narrative](archive/2026-06_matrix-cell-narratives.md#write-merge-append) |
| R103 · Write: `OverwriteFiles` | 🟡 | `api/OverwriteFiles.java`, `core/BaseOverwriteFiles.java`, `core/MergingSnapshotProducer.java` | `transaction/overwrite_files.rs` — explicit add + delete in one snapshot, dynamic `operation()`, `overwrite_by_row_filter`, the conflict-validation stack (`validate_no_conflicting_data` + `validateNewDeletes` A/B); delete-manifest carry fixed 2026-06-10. Known SAFE divergence: equality-delete applicability over-approximates. Metadata-level interop ✅ 2026-06-10; data-level interop ✅ 2026-06-11 (fixture C); conflict-validation interop ✅ 2026-06-15 (C1, both directions). V3 row-lineage interop landed 2026-08-25 (F-7 U1) — an overwrite's survivor keeps its `first_row_id` and its per-row `_row_id`, and the added file's stale id is suppressed; row R166. F-7 slice 1 (2026-08-30): DataFusion COW DELETE/UPDATE rewrites persist stored `_row_id` / `_last_updated_sequence_number` on v3 (matched UPDATE rows store a null last_updated so the reader uses the new file seq). Stays 🟡 on row-filter + multi-spec conflict interop residue. [full narrative](archive/2026-06_matrix-cell-narratives.md#write-overwritefiles) |
| R104 · Write: `ReplacePartitions` (dynamic/static overwrite) | 🟡 | `api/ReplacePartitions.java`, `core/BaseReplacePartitions.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/replace_partitions.rs` — DYNAMIC partition overwrite via `SnapshotProducer::resolve_partition_deletes`; full replace on unpartitioned tables; opt-in `validate_no_conflicting_data`; delete-manifest carry in unreplaced partitions fixed 2026-06-10. Metadata-level interop ✅ 2026-06-10; data-level interop ✅ 2026-06-11 (fixture E, untouched partition carries forward as EXISTING); conflict-validation interop ✅ 2026-06-15 (C4, partition-scoped, both directions; no `conflictDetectionFilter`/`caseSensitive` in the Java API). **`dataSpec()` scope branches ported 2026-08-23 (BEHAVIOR CHANGE, unit-proven):** apply-side `dataSpec().fields().isEmpty()` ⇒ `deleteByRowFilter(alwaysTrue)`, a full-table replace across EVERY spec, and validate-side `dataSpec().isUnpartitioned()` ⇒ the `alwaysTrue` overloads of `validateAddedDataFiles`/`validateDeletedDataFiles`/`validateNoNewDeleteFiles`; before this the fork was spec-keyed on both, so on a spec-evolved table it under-deleted and under-rejected vs Java. **Residue clause corrected 2026-08-23:** the previous "static `replaceByRowFilter`/explicit-partition APIs" residue named APIs Java does not have — `javap -p iceberg-api-1.10.0.jar` lists exactly five methods on `ReplacePartitions` (`addFile`, `validateAppendOnly`, `validateFromSnapshot`, `validateNoConflictingDeletes`, `validateNoConflictingData`), `BaseReplacePartitions` adds no public selector, and `overwriteByRowFilter` belongs to `OverwriteFiles` (already ported); Spark's static INSERT OVERWRITE reaches `OverwriteFiles`, only `overwriteDynamicPartitions()` reaches this action. Stays 🟡 on (a) multi-spec conflict INTEROP — no Java-oracle leg exists, every `ReplacePartitions` interop leg builds a single spec — and (b) the two `dataSpec()` `Preconditions.checkState` errors (no added files; added files spanning different specs), which the fork still accepts, and (c) the DELETE half of `deleteByRowFilter` — Java drives BOTH filter managers (`MergingSnapshotProducer.deleteByRowFilter` offsets 10 and 18) plus `removeDanglingDeletesFor` (offsets 103-114), so under `alwaysTrue` it removes every live delete file; this port keeps them (no row-resurrection hazard, but a manifest-list divergence byte-level interop would show). [full narrative](archive/2026-06_matrix-cell-narratives.md#write-replacepartitions-dynamicstatic-overwrite) |
| R105 · Write: `DeleteFiles` | 🟡 | `api/DeleteFiles.java`, `core/StreamingDelete.java`, `core/ManifestFilterManager.java` | `transaction/delete_files.rs` — delete by path / `DataFile` ref + the shared manifest-filter/rewrite machinery (`SnapshotProducer::process_deletes`) every delete-bearing action reuses; opt-in `validate_files_exist`; delete-manifest carry fixed 2026-06-10. Metadata-level interop ✅ 2026-06-10; data-level interop ✅ 2026-06-11 (fixture D); conflict-validation interop ✅ 2026-06-16 (C2, files-exist axis, both directions). Files-exist conflict guard corrected 2026-08-23: `Operation::Replace` added to `operation_removes_data_files` (Java `VALIDATE_DATA_FILES_EXIST_OPERATIONS = {overwrite, replace, delete}`, 1.10.0-bytecode-verified) — a concurrent compaction snapshot previously escaped the check; mutation-proved RED at base. Stays 🟡 — D2 axis-isolation is reasoned-not-mutation-tested, and row-filter/multi-spec paths are uncovered residue. [full narrative](archive/2026-06_matrix-cell-narratives.md#write-deletefiles) |
| R106 · Write: `RowDelta` (merge-on-read) | 🟡 | `api/RowDelta.java`, `core/BaseRowDelta.java`, `core/MergingSnapshotProducer.java` | `transaction/row_delta.rs` — merge-on-read commit: data + position/equality DELETE files in ONE snapshot, added deletes inherit the new seq, conflict validations, interop-proven (incl. residue-stripped sabotage). Files-exist conflict guard corrected 2026-08-23: `Operation::Replace` added to `operation_removes_data_files_skip_deletes` (Java `VALIDATE_DATA_FILES_EXIST_SKIP_DELETE_OPERATIONS = {overwrite, replace}`, 1.10.0-bytecode-verified) — the DEFAULT `skip_deletes == true` arm previously let a concurrent compaction remove a position delete's referenced data file with no error (the committed delete then references a non-live file; row-level resurrection is reasoned from that metadata state, not read back — the unit fixture is synthetic); mutation-proved RED at base. Stays 🟡 on remaining multi-spec DATA-merge commit residue (pending the `BaseRowDelta`/`BaseOverwriteFiles` multi-spec-manifest-merge decision). Dates 2026-06-08→16. [full narrative](archive/2026-06_matrix-cell-narratives.md#write-rowdelta-merge-on-read) |
| R107 · Write: `RewriteFiles` (compaction commit) | 🟡 | `api/RewriteFiles.java`, `core/BaseRewriteFiles.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/rewrite_files.rs` — atomically replace DATA files in one `Operation::Replace` snapshot (Java `BaseRewriteFiles`); `dataSequenceNumber` preservation landed 2026-06-10; conflict-validation interop-proven (sabotage battery). V3 row-lineage interop landed 2026-08-25 (F-7 U1) — a rewrite's survivor keeps its `first_row_id` and its per-row `_row_id` across two rewrites and across a V2-to-V3 upgrade; row R166. Stays 🟡 on remaining multi-spec interop residue + the DELETE-file-rewrite-surface interop (row R152). [full narrative](archive/2026-06_matrix-cell-narratives.md#write-rewritefiles-compaction-commit) **Java's REPLACE record-count precondition is NOT ported (found 2026-08-25 by the F-7 U1 fixture, row R166).** `SnapshotProducer.apply` (1.10.0, offsets 324-364) reads `added-records` and `deleted-records` off the summary and asserts `Preconditions.checkArgument(added <= deleted, "Invalid REPLACE operation: %s added records > %s replaced records")` for any `replace` operation. The fork carries no equivalent. It EMITS both keys when they are positive and OMITS them at zero (`spec/snapshot_summary.rs` `set_if_positive`, matching Java's `setIf`). It never reads them back to compare. So the port is a short comparison, not a plumbing job, and it must default an ABSENT key to 0 the way Java's `propertyAsLong` does — otherwise it trips on the `RewriteManifests` path, where both keys are absent and Java passes vacuously. A `RewriteFiles` commit whose replacement files hold MORE rows than the files they replace is accepted here and refused by Java. Proven by execution, not by grep: a 5-row replacement for a 3-row file committed as a `replace` snapshot that grew the table by 2 rows. Fail-OPEN where Java is fail-CLOSED, so a compaction that accidentally duplicates rows lands silently in a state Java would never produce. **Blast radius ENUMERATED against the 1.10.0 jar** by decoding `operation()` on every class: only `BaseRewriteFiles` and `BaseRewriteManifests` return `replace`, and `BaseRewriteManifests` reaches the check VACUOUSLY because it adds no data files (`0 <= 0`). `BaseOverwriteFiles` returns delete/append/overwrite and `BaseReplacePartitions` returns overwrite, so NEITHER can trip it — an earlier draft of this residue named those two, wrongly. `CherryPickOperation.operation()` DELEGATES to the replayed snapshot, so cherry-picking a `replace` snapshot re-emits `replace`; that is the path a producer-by-producer enumeration misses. The fork matches: its only production `Operation::Replace` emitters are `rewrite_files.rs` and `rewrite_manifests.rs`. |
| R108 · Transaction action extension seam | 🟡 | `core/.../BaseTransaction` | `transaction/action.rs` — `TransactionAction`/`ApplyTransactionAction` + `ActionCommit` exist (trait is `pub(crate)`; we own it → make `pub` in Phase 2) |
| R109 · Write: `upgrade_format_version` action | ✅ | format-version upgrade | `transaction/upgrade_format_version.rs` (new in 0.9) — base-inherited ✅ on the "implemented + usable" bar (upstream 0.9, unit-tested in `upgrade_format_version.rs`). **Strict-bar gap (named 2026-06-16):** no bidirectional interop fixture exists for the V2→V3 bump; it is the one un-proven slice under the fork's interop bar — cheap (metadata-only, no data files) if a future increment wants to flip it to fork-proven. See the legend note on the two ✅ bars. |
| R110 · Multi-op transactions + optimistic-concurrency retry | 🟡 | `api/Transaction.java`, `core/SnapshotProducer.java`, `core/MergingSnapshotProducer.java` | `transaction/mod.rs` + `catalog.update_table` retry (`backon`, retryable-only) + the conflict-validation FOUNDATION (2026-06-08): tx-captured `starting_snapshot_id` surviving the re-base, the `validate` hook against the refreshed base, non-retryable `DataInvalid` conflicts, the shared `files_after` walk family. **Multi-spec writes landed 2026-06-11:** the `SnapshotProducer` now groups added DATA + DELETE files by `partition_spec_id` and writes ONE manifest per (content × spec) — Java parity with `MergingSnapshotProducer.newDataFilesBySpec`/`newDeleteFilesBySpec` + `FastAppend.newDataFilesBySpec` (was default-spec-only). `validate_added_data_files`/`validate_added_delete_files` check spec EXISTENCE (Java's exact "Cannot find partition spec %s for {data,delete} file: %s") + partition-value compat against the FILE's own spec; the summary collector partitions by each file's own spec (Java `addedFile(spec(file.specId()), file)`). 8 producer tests (two-spec data/delete manifests, unknown-spec rejection ×2, wrong-spec-type, cumulative totals, plus reviewer-added per-spec partition-SUMMARY-key pin and a V1 two-spec append probe) + the cherrypick multi-spec replay conversion; grouping-revert, validation-revert, summary-revert, file-loss, and default-spec-stamp mutations all verified. Deferred (→ ✅): remaining per-action validation blocks; the WRITER-LAYER spec threading (`DataFileWriter`/`DeletionVectorWriter` still stamp the table default spec — a single writer instance produces single-spec files, so a multi-spec commit is assembled by the CALLER passing files that already carry their specs); multi-spec Java↔Rust interop; validation against the REAL catalogs (Glue + S3 Tables). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R111 · Writer: data file | ✅ | `data/` | `writer/base_writer/data_file_writer.rs` |
| R112 · Writer: equality-delete | ✅ | `data/` | `writer/base_writer/equality_delete_writer.rs` |
| R113 · Writer: position-delete | 🟡 | `data/`, `core/.../deletes/PositionDeleteWriter.java` | `writer/base_writer/position_delete_writer.rs` — reserved field ids (2147483546/2147483545), write-as-given (sorting is the caller's job, matching Java), schema built FROM the reserved metadata-column fields. Proven Java-readable via the scan-exec Direction-2 interop (2026-06-09). Spec-id stamp (2026-07-25): `with_partition_spec` on all three base writers, and the id is stamped for every caller that supplies EITHER a spec or a key (Java's required `PositionDeleteWriter(spec)` / `FileMetadata.Builder(spec)`), not only when a `PartitionKey` is present — **corrected 2026-08-25: NOT unconditionally.** `resolve_partition_spec_id`'s `(None, None)` arm now errors (2026-08-28). Call `unpartitioned()` or `with_partition_spec` / a `PartitionKey`. `rewrite_table_path` stamps the source delete spec. Misusing `unpartitioned()` on a partitioned table still silent-never-applies (pinned). See row R114 bound (c) — a wrong-spec PARTITION-SCOPED position delete commits and then silently never applies, isolated on the spec id alone by a same-tuple `truncate[5]`-vs-`identity` fixture (`spec_stamp_e2e_test`, 5 legs incl. the equality-delete GLOBAL-vs-scoped asymmetry); contract in [ENGINE_CONTRACT](../ENGINE_CONTRACT.md) §7a. 2026-08-03 (QB+BUG-001, U1): DF attach legs — (A) position-delete partition stamp fast path restricted to `partition_specs.len()==1` AND a ZERO-FIELD default spec (stricter than `is_unpartitioned()`: a single-spec all-Void table also walks — its stamp needs the null-tuple arity, not `None`) (evolved unpartitioned-default no longer silent-misses; pin `test_delete_mread_after_drop_partition_field_no_resurrection`); (B) equal-bounds path routing: `position_delete_writer_properties()` disables parquet 64-byte stats truncate + existing `MetricsConfig::for_position_delete` Full mode so long `file_path` bounds stay exact (pin `test_position_delete_long_file_path_bounds_are_full_and_equal`). Does NOT set `referenced_data_file` on v2 pos-deletes (Java PositionDeleteWriter never does). Status stays 🟡 pending Java-read interop of the evolved-DROP leg + sorting-writer residue. Deferred: the sorting writer variant; the optional `row` column. Scope note (2026-08-24, F-13 U4): this writer is now the V2 arm only. The DataFusion merge-on-read path dispatches V3 to deletion vectors (row R114), because the V3 spec forbids NEW position-delete files; `RowDelta` already refused one with "Must use DVs for position deletes in V3". Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R114 · Writer: deletion-vector (V3 puffin DV) | 🟡 (F-17 unit + DataFusion SQL + Java production-scan read-back 2026-08-28; other residues below) | `core/.../deletes` (`BaseDVFileWriter`, `BitmapPositionDeleteIndex.serialize`) | **F-17 (2026-08-28):** the shared-Puffin resurrection was reproduced (`expected [3, 4, 6]`, `actual [3, 4, 5, 6]`) then repaired. `delete_vector_container::close_touched_dv_containers` rewrites every live blob in an affected Puffin; `RowDeltaAction::add_delete_file_with_sequence_number` stamps sibling data sequences. DataFusion V3 `DELETE`/`UPDATE` arm `validate_data_files_exist` on every replacement reference; V3 `DELETE` also arms `validate_deleted_files` (named Java skip-delete divergence). Pins: `shared_puffin_dv` (T1–T5, T8–T23) plus Java production-scan of a Rust-written shared Puffin after SQL `DELETE` and `UPDATE` (`run-interop-dv-sql.sh` steps 4–5) and of a Java `BaseDVFileWriter` shared Puffin after Rust SQL `DELETE` (step 7). **Named residue:** T6/T7 (covered by T10/T1 union pins); a Spark-job-written fixture (Java `BaseDVFileWriter` is the production writer Spark uses). **Prior reopen:** DML loaded previous DVs only for touched paths, so path-keyed `RowDelta` removal dropped sibling blobs. Its safety contract requires `DELETE` to reject concurrent `Delete` removal of every replacement reference, which deliberately broadens Java's normal skip-delete behavior regardless of the selected validation API. `delete_vector.rs` `serialize_deletion_vector_v1` (Java-faithful DENSE layout + `runLengthEncode` parity via `RoaringBitmap::optimize` + framing/CRC, hand-computed exact-byte goldens) + `writer/base_writer/deletion_vector_writer.rs` `DVFileWriter` (one Puffin per close, blob per referenced file). D2 landed 2026-06-10; public hook required `pub mod delete_vector`. Spec-id stamp (2026-08-24, F-13 U1): `with_partition_spec` on `DVFileWriter`, closing the last of the four base writers — a keyless DV claimed `DEFAULT_PARTITION_SPEC_ID` (0) where Java takes the spec as a REQUIRED per-call argument. U1 supplied the MEANS to stamp a real spec; it did not make stamping unconditional — a caller that sets neither the spec nor a key now errors. See bound (c). Rejects late (at `close`, not construction) because the key arrives per `delete` call. Builder guards (2026-08-24, F-13 U2, BREAKING): `DataFileBuilder::build()` now enforces all six DV-bearing guards of Java `FileMetadata$Builder.build()` — three DV requirements (offsets 122/140/158), two forbidding DV-only fields on a non-Puffin file (179/197), and the position-delete sort-order guard (252) — as a `build_fn(validate)`. The gating question is ANSWERED: the Avro manifest READ path never calls the builder (`spec/manifest/_serde.rs` decodes into a struct literal, and `deserialize_data_file_from_json` delegates to it), so no already-written file can be rejected. Java keys the Puffin rules on the FORMAT, so the fork applies them to a Puffin file of ANY content type; that arm is a fork-only strengthening with NO Java oracle, because Java's `FileMetadata$Builder` builds delete files only and its data-file builder (`DataFiles$Builder`) has no blob-coordinate fields at all. V3 merge-on-read (2026-08-24, F-13 U4, BEHAVIOR CHANGE): the DataFusion integration's `DELETE`/`UPDATE` no longer refuses V3. `require_v2_for_merge_on_read` became a version dispatch — V1 refuses (no delete files at all), V2 writes position deletes, V3 writes DVs through `DVFileWriter`. Each position carries its data file's own `PartitionKey`, and a data file that already has a DV has it loaded through the new public `delete_vector::load_delete_vector` (U3a) and merged, with the superseded DV dropped by `remove_deletes_many` in the same commit. Proven end to end by `dev/java-interop/run-interop-dv-sql.sh`: Java's production scan reads the V3 table three SQL deletes produced. RESIDUE (U4): a data file still covered by a legacy PARQUET position delete is REFUSED, not merged. Java `BaseDVFileWriter.loadPreviousDeletes` (L114-129) unions whatever covers the file and rewrites the file-scoped sources; this port loads previous positions from Puffin DVs only. Reachable by upgrading a V2 table that has position deletes to V3 (`transaction/upgrade_format_version.rs`). Fail-closed and pre-IO — the refusal runs before the Puffin is opened, so no orphan reaches storage — but V3 merge-on-read stays unusable on such a table until its position deletes are rewritten as DVs. The pre-IO test and the commit door share one applicability rule: the reference comes from `referenced_data_file_location` (Java `ContentFileUtil.referencedDataFile` — the field OR equal `file_path` bounds), a named delete matches on PATH alone, and `delete_seq >= data_seq` filters the rest. COMMIT-DOOR FIX (same change): `validate_fresh_dvs_only` read `referenced_data_file` only, so a BOUNDS-scoped position delete — the shape Java's own `PositionDeleteWriter.close()` produces, since it never sets the field — was treated as partition-scoped. One stamped under a different spec passed the door and was silently superseded by the DV, resurrecting its rows. That mismatch is routine because SPARK defaults `write.delete.granularity` to FILE (`SparkWriteConf.deleteGranularity()` → `DeleteGranularity.FILE`); note this is Spark's default, NOT Iceberg's — core's `TableProperties.DELETE_GRANULARITY_DEFAULT` is PARTITION. RESIDUE (door-side fix only): the guard prevents FUTURE bad commits; it does not repair metadata already in that state. A DV committed over a live bounds-scoped delete BEFORE this fix still resurrects rows at scan, because `get_deletes_for_data_file` early-returns on a DV hit and never consults the position-delete maps. Java's `findDV` behaves identically, so the read side is parity-faithful — the corrupted table, not the reader, is the defect. RESIDUE: Java's equality-delete arm (switch case 2, offsets 273-293) DEFAULTS an absent `sort_order_id` to `SortOrder.unsorted().orderId()`; the fork writes the field absent. That is a divergence in written metadata, not a rejection, and is not ported here. F-13 U3b (2026-08-24) closes the last unit: `DVWriteResult` gains `referenced_data_files()` and `references_data_files()`, the DERIVED mirror of Java `DeleteWriteResult.referencedDataFiles()` / `referencesDataFiles()` — derived, not stored, because `BaseDVFileWriter.close` populates that set with `deletes.path()` once per written blob (1.10.0 bytecode, offset 205) and this writer emits exactly one `DataFile` per such entry. **F-13 delivered the original DV surface; F-17 owns the reported shared-Puffin closure defect.** [full narrative](archive/2026-06_matrix-cell-narratives.md#writer-deletion-vector-v3-puffin-dv) **F-13 answer to the engine (2026-08-25), recorded per the handoff's verify-before-chartering rule:** the DV write path EXISTS as the fork's own surface — not an upstream cherry-pick candidate and not absent. `DVFileWriter` writes one Puffin per `close`, file-scoped, one uncompressed `deletion-vector-v1` blob per referenced data file (`deletes_by_path: BTreeMap<String, DeletesForDataFile>`); it takes the spec via `with_partition_spec`, and `resolve_partition_spec_id` has FOUR arms: a `PartitionKey` wins over the configured spec; a partitioned configured spec with no key is an error at `close`; an unpartitioned configured spec with no key stamps that spec; and neither key nor configured spec errors — see bound (c) below; `RowDelta` admits DVs, requires `referenced_data_file`, and refuses a second live DV for the same referenced file. The engine's merge-on-read arm ALREADY drives it the way it drives `PositionDeleteFileWriter`: `physical_plan/delete.rs` dispatches `FormatVersion::V3 => MergeOnReadDeleteKind::DeletionVectors`, builds the writer with previous deletes loaded through the public `delete_vector::load_delete_vector`, and returns `(delete_files, rewritten_delete_files)` for the row delta. NOTE the DV path does NOT call `with_partition_spec`: the spec id arrives from each data file's own `PartitionKey`, resolved from the live partition map. An engine mirroring the described construction must supply one route or the other. **That earlier handoff ruling is superseded; F-17 must close the shared-Puffin defect before an engine repin.** THREE BOUNDS on that, all load-bearing for an engine that builds its own merge-on-read arm instead of wrapping `iceberg-datafusion`. **(a) Applicability and previous-DV discovery are public (2026-08-28).** `spec::referenced_data_file_location`, `spec::is_deletion_vector`, and `live_deletion_vectors_by_data_file` are exported. `delete_file_index` stays crate-private. **(b) "Fail-closed, pre-IO" is a property of the IN-TREE DataFusion arm, not of the surface.** That refusal lives in `physical_plan/delete.rs` and runs before the output file is created; the CORE crate's only equivalent is the commit door `RowDelta::validate_fresh_dvs_only`, which fires AFTER `close_with_result` has written the Puffin. So an engine driving `DVFileWriter` directly over a data file still covered by a LEGACY PARQUET position delete writes a full Puffin, is refused at commit, and leaves an ORPHAN on storage. Reachable only by upgrading a V2 table that has position deletes to V3. Closing that gap for external engines needs BOTH halves of (a) — the applicability rule AND previous-DV discovery — promoted to the public core surface. You cannot decide whether a legacy position delete covers a file without first enumerating the live delete files. Fork units, not engine ones. **(c) `(None, None)` errors (2026-08-28).** Call `unpartitioned()` to stamp spec 0 empty, or `with_partition_spec` / a `PartitionKey`. `rewrite_table_path` stamps the source delete spec. Misusing `unpartitioned()` on partitioned data still never applies (pinned on row R113). Spark-job-written shared-Puffin fixture remains. |
| R115 · Writer: partitioning (fanout/clustered/unpartitioned) | ✅ | — | `writer/partitioning/` |
| R116 · Read: Parquet → Arrow | ✅ | `parquet/` | `arrow/reader.rs` |
| R117 · Read: merge-on-read apply (position-deletes + DVs during scan) | ✅ | `data/.../DeleteFilter`, `data/.../BaseDeleteLoader`, `core/DeleteFileIndex.java` | `arrow/delete_filter.rs`, `arrow/caching_delete_file_loader.rs`, `delete_file_index.rs`, `delete_vector.rs`. Parquet position/equality deletes: data-level interop ✅ 2026-06-09 (full cross-product {pos,eq}×{both directions}×{partitioned,unpartitioned}). HONESTY CORRECTION 2026-06-10: the prior DV ✅ over-claimed (0.9.1-sync, never scan-verified); D1 added the DV scan READ path (blob decode + ranged load + `DeleteFileIndex.forDataFile` precedence) and DVs are now interop-proven BOTH directions 2026-06-10. FLIPPED 🟡→✅ 2026-06-21. Interop-proven BOTH directions, by axis: POSITION-deletes × multi-file-per-partition (offline `test_engine_deletefilter_multifile_partition_*` + bidirectional `test_multifile_scan_exec_*` via `run-interop-multifile-scan[-d2].sh`: a partition-scoped delete on file1 spares file2's same-ordinal row — path-keyed, not partition-broadcast) AND POSITION-deletes × non-identity transform (`truncate[10](id)` via `test_nonidentity_scan_exec_*` + `run-interop-nonidentity-scan[-d2].sh`: the delete matches the TRANSFORMED partition Struct, truncate=20 left intact); EQUALITY-deletes × {multi-file, identity} also interop-proven (offline `test_engine_deletefilter_multifile_partition_equality_*` + the 2026-06-09 eq cross-product). The `dv_seq>=data_seq` check landed 2026-06-11. NAMED residue (each non-blocking + reasoned): (1) EQUALITY-deletes × non-identity-transform is NOT separately interop-tested — but partition MATCHING is mechanism-IDENTICAL to position-deletes (the same `delete_file_index` `HashMap<Struct>` keying serves both pos and eq), and eq-delete APPLICATION is interop-proven bidirectionally UNPARTITIONED (2026-06-09 cross-product) plus offline-proven under partitioning, so the combination is code-provable from the union, not separately round-tripped; (2) DVs are keyed by referenced-file PATH (transform-irrelevant); (3) error-path only — Java's index-level `ValidationException`s land at the Rust load door instead (valid-table behavior is 1:1). DEMOTED ✅→🟡 2026-07-25 (WG4b, same change that closed the routing gap below): a SEPARATE read-path divergence surfaced while proving the routing — the per-scan `DeleteFilter` cross-file positional-delete leak described at the end of this cell — is a real OVER-delete vs Java and is NOT fixed here (it lives in `arrow/caching_delete_file_loader.rs` + `arrow/delete_filter.rs`, outside this group). Re-promotion is a defined act: scope the filter to each task's OWN delete files (Java builds one `DeleteFilter` per task over `task.deletes()`) and add the interop leg whose recipe is recorded below. RE-PROMOTED 🟡→✅ 2026-07-26 (unit QA, `fix/delete-filter-per-task-scope`): exactly that act, Java-exact — the loader now caches each positional source's parsed data-file→positions map PER SOURCE under its claim key (Java `BaseDeleteLoader.getOrReadPosDeletes` caches `readPosDeletes(deleteFile)` per `deleteFile.location()`, 1.10.0 bytecode offsets 22-39) and application unions ONLY the task's own `task.deletes` contributions at the task's own data file (`DeleteFilter.deletedRowPositions` → `loadPositionDeletes(posDeletes, filePath)` → `getOrDefault(filePath, empty)` + `PositionDeleteIndexUtil.merge` into a FRESH index — all bytecode-cited in the PR), memoized per task shape (Java's per-task `deleteRowPositions` field). Load/parse caching preserved: the claim/notify machinery is untouched and parse-once is pinned by claim-state assertions (`AlreadyLoaded` after first load). Proven: deterministic unit repro RED on the pre-fix tree (task A's vector `[1,2]` vs `[1]` — the foreign source's position leaked) + a two-task legitimate-share control + an eq-delete-scope pin (the equality path was verified ALREADY per-task — application iterates `task.deletes`; state is a pure per-file load cache — so no eq fix was needed, the pin guards it); interop leg 2 of `run-interop-file-scoped-deletes.sh` (the recorded recipe: control stamped `category=b`) RED pre-fix 4/4 runs (live `{10,40,60}`, id 30 wrongly deleted end-to-end) and green post-fix (`{10,30,40,60}` = Java's generator-asserted read, plan-level attachment pinned); 4 mutations RED from snapshot copies (shared-state revert → repro; over-scope → share control; eq cross-task fold → eq pin; drop-the-parquet-wait → the adapted waiter pin, proving the pin adaptations non-vacuous). FILE-SCOPED ROUTING GAP FOUND + CLOSED 2026-07-25 (WG4b) — the ✅ above was OVER-GREEN: Java routes any position delete with a derivable referenced data file into a PATH-keyed map (`DeleteFileIndex.Builder.add` → `findPathDeletes`) consulted with NO spec and NO partition condition, and derives that reference from EITHER the `referenced_data_file` field OR EQUAL `file_path`-column bounds (`ContentFileUtil.referencedDataFile`, 1.10.0 bytecode). The fork had no such map: every parquet position delete was keyed by `(spec_id, partition)`, so a file-scoped delete stamped with a different spec or partition than the file it names was DROPPED (silent under-delete — masked rows resurrect), and `RemoveDanglingDeleteFiles` then classified it dangling and REMOVED it (irreversible). Both legs landed in `delete_file_index.rs` (`referenced_data_file_location` + `pos_deletes_by_path`, EXCLUSIVE with the partition map exactly as Java's `add` is) and the same predicate now gates the maintenance action's liveness check. Interop-proven 2026-07-25 by `run-interop-file-scoped-deletes.sh` (Java writes a table evolved from unpartitioned spec 0 to `identity(category)` spec 1; a FIELD-leg and a BOUNDS-leg delete stamped spec 0/empty partition apply to files in spec 1 partitions a/b, a partition-scoped control in an EMPTY partition applies to nothing; Java's own read `{10,30,40,60}` is asserted inside the generator, and the Rust test pins the PLAN as well as the rows) plus 9 unit pins and an e2e maintenance pin, 11 mutations RED. EXTERNALLY CORROBORATED 2026-07-25 (RePark consumer, independent bytecode + empirical at two Iceberg versions): through 1.10.0 AND 1.11.0 the ONLY writer that calls `FileMetadata$Builder.withReferencedDataFile` is `deletes.BaseDVFileWriter`, the format-V3 DV writer (1.11.0 adds only `RewriteTablePathUtil`, a metadata-rewrite utility); `PositionDeleteWriter`, `SortingPositionOnlyDeleteWriter` and `FileScopedPositionDeleteWriter` never set it, and 1.4.3 does not contain the string at all. So for SPARK-WRITTEN V2 tables the EQUAL-BOUNDS leg is not a fallback but the ONLY signal a real writer emits — the field leg is reachable only for V3 DVs or hand-built metadata, which is what THIS suite's field leg is (`FileMetadata.deleteFileBuilder(...).withReferencedDataFile(...)` in the oracle), so the suite's real-world weight rides entirely on its bounds leg. The inverse trap is covered too: a PARTITION-granularity delete carries NO `file_path` bounds at all (Iceberg omits them rather than widening), so ABSENT bounds — not unequal bounds — is the partition-scoped signal (`test_pos_delete_with_unequal_path_bounds_stays_partition_scoped` pins the unequal case, `build_partitioned_pos_delete` the absent one). REAL-WRITER LEG WIRED 2026-07-26: Spark-written fixtures for all four shapes (file-granularity v2 at Spark 4.0.0 + Iceberg 1.10.0, partition-granularity v2, v3 DV, and a Spark 3.5.6 + Iceberg 1.4.3 table whose manifest Avro schema does not even DECLARE the field) are read by `crates/iceberg/tests/interop_spark_mor_fixtures.rs`, which pins per table the delete-file ATTACHMENTS and the live id set with zero resurrection. The equal-bounds fixtures are the first proof of the bounds leg against a genuine Spark writer rather than hand-built metadata, and the 1.4.3 fixture is the first test of the field-absent-from-the-Avro-SCHEMA decode path (`DataFileSerde.referenced_data_file` is `Option<String>` with NO `#[serde(default)]`, surviving only via serde's missing-field-to-`None` over `apache_avro::from_value` — previously an inference). Mutation-pinned: disabling the bounds leg turns the routing test RED with 4 attachments instead of 2. The ~220 KB binary warehouse is NOT committed here (deflate-compressed manifests with the generating machine's absolute paths baked in), so the suite is env-gated on `ICEBERG_SPARK_MOR_FIXTURE_DIR` and a clean no-op without it — checking the bytes in needs a prefix-remapping storage shim or regeneration, and explicit user approval. What these fixtures CANNOT pin, and the reason this is evidence-widening rather than a new capability: all four tables are UNPARTITIONED and single-spec, so the pre-routing `(spec_id, partition)` index over-attached but never missed, and the row-level resurrection class stays pinned only by the Java oracle. Separate from the DeleteFilter cross-task residue (CLOSED 2026-07-26 — see the RE-PROMOTED clause above). Residue found while proving it — the reader's per-scan `DeleteFilter` cross-task positional-delete leak (parsed positional deletes merged into SHARED data-file-keyed state, so a delete file loaded for one task contributed deletions to another task's file) — CLOSED 2026-07-26 by the re-promotion change above: per-source contribution cache + per-task application; the recorded `category=b` reproduction is now the permanent interop leg 2 of `run-interop-file-scoped-deletes.sh` plus a deterministic unit twin. Also named: the fork's OWN position-delete writer produces no usable `file_path` bounds — parquet-rs truncates byte-array statistics at 64 bytes by default and the metrics aggregator drops non-exact bounds, so fork-written file-granularity deletes are not recognised as file-scoped (Java's parquet-mr does not truncate row-group statistics); write-side, adjacent to R113. EQ-DELETE KEY-COLUMN COVERAGE EXTENDED 2026-06-28: `Time` and `Fixed(n)` key columns no longer error the scan (`get_arrow_datum` gained Time64/FixedSizeBinary arms) and are re-admitted to the `EqDeleteKeySet` fast path (integer-/byte-identical equality under both Arrow `eq` and `Datum` `Eq`); fast-path mask proven == predicate-oracle mask incl. a key-null bail, via `test_h6_set_time/fixed_matches_oracle` + `test_h6_time_fixed_key_null_bails_to_predicate_without_error`. Interop not separately round-tripped (the existing eq-delete oracle keys on a Long; a new Java oracle path was judged disproportionate) — proven by the predicate-oracle equivalence harness instead. [full narrative](archive/2026-06_matrix-cell-narratives.md#read-merge-on-read-apply-position-deletes--dvs-during-scan)  Path-leg equal-bounds routing now has real full `file_path` bounds from fork writers (2026-08-03 QB Leg B). |
| R118 · Read: ORC data files | 🟡 | `orc/` (`ORCSchemaUtil.buildOrcProjection`, `GenericOrcReader`, `GenericAppenderFactory` ORC writer) | READ landed (U1+U2, 2026-06-18) — `arrow/orc_reader.rs` hand-parses the ORC footer to resolve BY FIELD-ID (never name), mirroring `ORCSchemaUtil.buildOrcProjection` + `GenericOrcReader` (promotions, missing-required vs optional-null); U2 wired it into `arrow/reader.rs` `Orc` arm (RecordBatchTransformer + MoR deletes applied post-materialization). 6 offline scan tests + interop ✅ Direction-1 2026-06-18 (`tests/interop_orc_data.rs`, Java `GenericOrcWriter` writes, Rust scans row-identical). Stays 🟡 — NAMED residue: WRITE absent (no Direction-2), footer-codec limited to uncompressed/ZLIB, nested+V3 types deferred. [full narrative](archive/2026-06_matrix-cell-narratives.md#read-orc-data-files) |
| R119 · Read/write: Avro data files | 🟡 | `core/.../avro` (data), `data/.../avro/PlannedDataReader.java`, `data/GenericAppenderFactory.java` | `arrow/avro_reader.rs` (`read_avro_data_file`, a faithful `PlannedDataReader`/`ValueReaders.buildReadPlan` port: id-based read plan, all primitive+logical types, projection/skip/missing-default). READ landed U2 2026-06-18, scan-wired + interop-proven. Stays 🟡 on the parked `timestamptz` `adjust-to-utc` mapping gap (user decision 2026-06-18) plus deferred name-mapping fallback + variant reads. V3 row lineage is NO LONGER deferred here — it is implemented one layer later, in the shared `RecordBatchTransformer` (row R166), and `ArrowReader::build_expected_schema` lets the two reserved row-lineage ids reach this reader so a STORED `_row_id` is decoded rather than silently replaced by a computed one; the plain-column emission is the INPUT to the shared rule rather than a gap. [full narrative](archive/2026-06_matrix-cell-narratives.md#readwrite-avro-data-files) |
| R120 · Scan planning + partition pruning | ✅ | `api/TableScan.java` | `scan/` — manifest-list → manifest → file pruning (manifest evaluator, partition filter, metrics evaluator), `use_ref` branch/tag scanning (2026-06-08), per-task PARTITION-REDUCED residuals (2026-06-08). **Scan-PLANNING interop ✅ 2026-06-09 (A4):** planned file set + per-file delete association + residual-always-true bit match Java; full residual-expression string comparison deferred (cross-language syntax). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R121 · Metrics evaluators (inclusive/strict) + residual evaluation | 🟡 | `expressions/`, `api/.../expressions/ResidualEvaluator.java` | `expr/visitors/` — inclusive/strict metrics evaluators + `ResidualEvaluator` (core 2026-06-08 + scan wiring: each `FileScanTask` carries the partition-reduced residual). Identity-partition constant materialization (Java `PartitionUtil.constantsMap`) ACTIVATED 2026-06-11: `context.rs` threads each manifest's `partition_spec` onto its tasks; `record_batch_transformer` materializes identity-partition columns as PLAIN arrays of the declared scan-schema type (the prior REE-leak bug fixed) coerced to the column's Iceberg type via `Datum::to` (int→long widening fixed), and forces the constant to OVERRIDE a file-present column (verified against Java 1.10.0 bytecode: `BaseParquetReaders.ReadBuilder.struct` → `ParquetValueReaders.replaceWithMetadataReader` returns the constant reader unconditionally when `idToConstant` has the field id, discarding the file-column reader — constant wins by construction). BEHAVIOR CHANGE: any identity-partitioned table whose data-file values diverge from partition metadata now returns the PARTITION value (was the file value); for spec-consistent tables (the norm) results are unchanged. Multi-spec: each file's constants use its own spec. Gated on `cargo test -p iceberg-datafusion` (lib + MemoryCatalog integration incl. `test_insert_into_partitioned`). Unit + scan tests ship; data-level Java interop round-trip still pending (keeps 🟡). **Residue (verified 2026-06-13):** NO variant-type datum handling in the inclusive/strict metrics evaluators (`expr/visitors/{inclusive,strict}_metrics_evaluator.rs` have zero `Variant` arms) — a variant field's min/max bounds are treated as unknown (no pruning), matching the "variant behaves as unknown" posture, not Java's typed variant bounds. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R122 · `IncrementalAppendScan` | ✅ | `api/IncrementalAppendScan.java`, `core/BaseIncrementalAppendScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — exclusive/inclusive `from` ranges, append-only snapshot walk, own-added-manifest + `Added`-entry filters, reuses the normal scan's plan machinery via the `_from_files` seam. **Interop ✅ BOTH directions 2026-06-17** (`tests/interop_incremental_scans.rs` + `dev/java-interop/run-interop-incremental-scans.sh` + `IncrementalScanOracle`): a 4-snapshot V2 chain (S1 append a / S2 append b / S3 append c / S4 overwrite delete-a/add-d, deterministic basenames) where Rust's `incremental_append_scan` planned data-file BASENAME set == Java's REAL `IncrementalAppendScan.fromSnapshotExclusive/fromSnapshotInclusive(...).toSnapshot(...).planFiles()` over the SAME ranges == the hand-declared anti-circular ground truth, BOTH directions (D1 Rust-plans-Java-table, D2 Java-plans-Rust-table). The inclusive/exclusive boundary is pinned explicitly: `append_excl` from S1-EXCLUSIVE → {b,c} vs `append_incl` from S1-INCLUSIVE → {a,b,c} (S1's own a is the only difference); `append_to_cur` from S2-exclusive default-to-current → {c} (the S4 overwrite excluded by the append-only filter). Off-by-one sabotage (shift the exclusive bound S1→S2 ⇒ {b,c}→{c}) fails closed, non-vacuity verified. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R123 · `IncrementalChangelogScan` | ✅ | `api/IncrementalChangelogScan.java`, `core/BaseIncrementalChangelogScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — Insert/Delete changelog tasks with OLDEST→0 ordinals, deletes sourced from the deleting snapshot's own rewritten manifests; delegates range resolution to the append-scan builder. **DATA-FILE-changelog interop ✅ BOTH directions 2026-06-17** (`tests/interop_incremental_scans.rs` + `run-interop-incremental-scans.sh` + `IncrementalScanOracle`, same 4-snapshot chain as row R122): Rust's `incremental_changelog_scan` over (S1 exclusive, S4] emitted the `(basename, operation)` set {+b, +c, −a, +d} (the S4 overwrite removes a ⇒ DELETE, adds d ⇒ INSERT; the S2/S3 appends ⇒ INSERT) == Java's REAL `IncrementalChangelogScan` / `IncrementalDataTableScan` over the SAME range == the hand-declared anti-circular ground truth, BOTH directions. **NAMED residue (re-audited 2026-07-09 against the 1.10.0 jar bytecode):** Java CORE's changelog is itself whole-data-file only — `BaseIncrementalChangelogScan.orderedChangelogSnapshots` throws `UnsupportedOperationException` for ANY delete-manifest range and `CreateDataFileChangeTasks` constructs only `BaseAddedRowsScanTask`/`BaseDeletedDataFileScanTask` with `NO_DELETES` (never `BaseDeletedRowsScanTask`); the Rust DEFAULT matches that rejection surface exactly (`FeatureUnsupported`, guard mutation-proven both directions). The Java-api task TAXONOMY is PORTED (2026-07-09): `ChangelogTaskKind::{AddedRows, DeletedDataFile, DeletedRows}` with `operation()` derived per the api default methods, `added_deletes`/`existing_deletes` attachments, and `ChangelogOperation` now declares `UpdateBefore`/`UpdateAfter`. Net-change UPDATE_BEFORE/UPDATE_AFTER PAIRING is Spark-side (`ChangelogIterator` — absent from the core/api jars), so it is NOT a core-parity gap and stays unimplemented here. ENGINE-FIRST extension (beyond 1.10.0 core, NOT claimed as parity; unit-tested + 6 mutations RED, no interop): opt-in `with_row_level_deletes(true)` accepts MoR-delete ranges and emits the row-level split — same-snapshot deletes fold into added-rows, deleted-data-file carries pre-existing deletes, deleted-rows carries EXACTLY the range snapshot's ADDED deletes split from existing ones (2026-07-09); the deleted-rows Arrow projection (emitting the deleted rows) is engine-side, like Java's Spark `ChangelogRowReader`. The changelog-through-`BatchScan` surface remains the other named residue (corrected 2026-07-01). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R124 · `BatchScan` | ✅ | `api/BatchScan.java`, `api/BatchScanAdapter.java` | **LANDED + interop-proven ✅ both directions 2026-06-17 (U2).** `scan/batch.rs` (`BatchScan`, built by `Table::batch_scan()`) — the THIN typed adapter mirroring Java `BatchScanAdapter` (javap-confirmed: `Table.newBatchScan()` → `new BatchScanAdapter(newScan())`, every method `getfield scan; invoke...` delegates 1:1). Wraps a `TableScanBuilder`; `plan_files`/`plan_tasks` DELEGATE to the `TableScan` pipeline (row R148 — split/bin-pack REUSED, not reimplemented). Adds the three time-travel selectors (Java `SnapshotScan.useSnapshot`/`useRef`/`asOfTime`, bytecode-verified): `use_snapshot(id)`/`use_ref(name)`/`as_of_time(ms)`, FIRST-WINS mutual exclusion (`checkArgument(snapshotId == null, "Cannot override snapshot/ref, ...")` — a second pin is a hard `DataInvalid`; `use_ref("main")` is the table-default no-op, never a conflict); `as_of_time` = the GREATEST `timestamp_ms <= ms` over `metadata().history()` (Java `SnapshotUtil.snapshotIdAsOfTime` / `nullableSnapshotIdAsOfTime`), erroring "Cannot find a snapshot older than %s" when none. 11 offline tests (delegation `plan_tasks`==`scan().plan_tasks` + `plan_files`; selector resolution; `as_of_time` inclusive `<=` boundary at-exact-ts + 1ms-before + before-all-errors; conflict matrix; `main` no-op) with 2 mutation-baits (`rfind`→`find` greatest-vs-earliest; `<=`→`<` inclusive-boundary — each reds the boundary test). **Bidirectional interop ✅** (`tests/interop_scan_plan.rs` + `run-interop-scan-plan.sh` + `ScanPlanOracle`, extends the U1 oracle, row R148): `table.newBatchScan().planTasks()` asserted == `newScan().planTasks()` IN JAVA (the adapter delegation), and Rust `BatchScan::plan_tasks` == Java's `newBatchScan().planTasks()` == the scan plan, BOTH directions (D1 11 groups; D2 16 groups, `0 failures`). Subsumed-row R148 → ✅ alongside this. |
| R125 · Scan/commit metrics reporting (`ScanReport`, `MetricsReporter`) | 🟡 | `metrics/` | `metrics/mod.rs` (data model, 2026-06-08: Java-shape serde incl. counter/timer wire format) + OPT-IN scan emission wiring (2026-06-09: `with_metrics_reporter`, report ONCE on full stream consumption; no-reporter path byte-unchanged). **`filter`-field divergence RETIRED 2026-06-17:** the `filter` now serializes via the canonical `ExpressionParser` codec (custom serde, row R149) so `ScanReport.filter` byte-matches Java `ScanReportParser` (serialize via `ExpressionParser.toJson`, deserialize via the schema-less untyped path). Still 🟡 for the OTHER residue: `LoggingMetricsReporter` (needs a logging-facade dep approval) and the 6 not-yet-collectable counters stay `None`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R126 · Catalogs: REST, Hive, Glue, S3 Tables, SQL/JDBC, in-memory | 🟡 (TABLE ops ✅ all; residue below) | `core/.../{rest,jdbc,inmemory}`, `aws`, `hive-metastore` | `crates/catalog/*`, `catalog/memory`. TABLE lifecycle ✅ across all impls. **Residue the umbrella hid:** (a) **PARITY-CORRECT, not a gap (verified 2026-06-17):** the PRIORITY catalogs Glue (`crates/catalog/glue`) + S3 Tables (`crates/catalog/s3tables`) override NO view method, so all 7 fall through to the trait's default `FeatureUnsupported` stubs — this MIRRORS Java, where neither service supports views: Java `GlueCatalog` does NOT implement `ViewCatalog` (issue #12488 — an OPEN feature request to ADD Glue view support, confirming 1.10.0 does not have it), and the S3 Tables service has no view API (`aws-sdk-s3tables` 1.51 `src/operation/` exposes only namespace/table/bucket ops, ZERO `*view*` operations — SDK-verified offline; the Glue claim rests on #12488 + the Rust-side no-override evidence, the Java `iceberg-aws` jar being unavailable to javap offline). The `FeatureUnsupported` view stubs on Glue/S3Tables are therefore Java-faithful (see the ViewCatalog row); (b) the `Catalog` trait accessor surface (`name()`/`properties()`/`invalidate_table`/`invalidate_view`) is now LANDED as non-breaking default methods (2026-06-17, see the Catalog-trait-accessors row); `commitTransaction(List<TableCommit>)` is split out as its own deferred unit (REST-server multi-table atomic commit, concrete on `RESTCatalog` only). **(c) S3 Tables `register_table` (F-9, 2026-08-27) — dated permanent service gap:** the AWS S3 Tables Iceberg REST mapping has no `registerTable`; native `UpdateTableMetadataLocation` requires a metadata URI that begins with the table warehouse. Java `iceberg-aws` 1.10.0 has no `S3TablesCatalog`. The fork keeps `FeatureUnsupported` with that citation (`crates/catalog/s3tables/src/catalog.rs`); this is not a "yet". |
| R167 · Hadoop-convention metadata names (`vN.metadata.json`) | 🟡 (unit + MemoryCatalog commit 2026-08-28; Spark Hadoop-catalog interop is the engine pin) | `core/hadoop/HadoopTableOperations.java` (`metadataFilePath` = `v` + version + `TableMetadataParser.getFileExtension`; `VERSION_PATTERN` = `v([^.]*)\..*`) | `catalog/metadata_location.rs` — `FromStr` accepts Hadoop `vN.metadata.json` (and gzip `.gz.metadata.json` / `.metadata.json.gz`). `with_next_version` emits uncompressed `v(N+1).metadata.json` and keeps Hive/REST `<version>-<uuid>` on that convention. `TableCommit` uses this seam, so `register_table` of a Hadoop pointer then a property commit writes `v4` after `v3`. New tables from `new_with_table_location` stay Hive/REST. Named residue: no `version-hint.text` writer (HadoopTableOperations-only); no `renameToFinal` exists-fail / temp-rename — Hadoop `vN` is deterministic, so a concurrent MemoryCatalog write-then-CAS can overwrite the winner's bytes (Hive uuid names made the same race an orphan). Engine pin `call_register_table_of_hadoop_named_metadata_writes_name_the_convention` retargets after Spark Hadoop-catalog interop. |
| R127 · `ViewCatalog` + view operations (create/replace/drop/list, versions) | 🟡 | `api/catalog/ViewCatalog.java`, `api/view/`, `core/view/` | `spec/view_metadata*`, `view_version.rs`, `view.rs` — view metadata spec + builder is a 1:1 port of Java `ViewMetadata.Builder`; `ViewMetadata::read_from`/`write_to` + `View` handle landed U1 2026-06-12. Stays 🟡 on byte-exact residue; Glue/S3Tables "views unsupported" verified 2026-06-17 as parity-correct (mirrors Java, issue #12488 OPEN) — `FeatureUnsupported` stubs are Java-faithful, no view sprint owed. [full narrative](archive/2026-06_matrix-cell-narratives.md#viewcatalog--view-operations-createreplacedroplist-versions) |
| R128 · `SessionCatalog` | ❌ (ASSESSED-DEFERRED — dead surface, 2026-06-17) | `api/catalog/SessionCatalog.java` | none — **deliberately not ported.** Java `SessionCatalog` (javap-confirmed) threads a `SessionContext` arg through every op (`loadTable(ctx, ident)`, `buildTable(ctx, …)`, `name()`/`properties()`, …) so ONE catalog instance can serve many per-session credential scopes (the multi-tenant REST pattern; `RESTSessionCatalog` is the real impl, wrapped to a plain `Catalog` via `BaseSessionCatalog.asCatalog(ctx)` which binds a FIXED context). The Rust design binds identity per-catalog AT CONSTRUCTION — each `Catalog` instance already holds its own credentials/config — so a `SessionContext` parameter has NO consumer here; the `asCatalog(ctx)` wrapper proves the session layer is optional (a fixed-context `SessionCatalog` IS just a `Catalog`). Revisit only if a multi-tenant single-instance use-case appears. |
| R129 · `LockManager` | ✅ | `api/LockManager.java`, `core/.../LockManagers.java` | `catalog/lock.rs` — `LockManager` trait + `InMemoryLockManager` + `LockManagers::{from,default_lock_manager}` + `LockConfig` (5 `lock.*` knobs; bytecode-verified defaults 180000/5000/3000/15000/4); acquire/acquireOnce/release/expiry/heartbeat semantics decoded from 1.10.0 bytecode; 11 unit tests, 4-mutation-proven. **Behavioral-conformance interop ✅ 2026-06-20** — outcome-equivalence vs the REAL Java `InMemoryLockManager` over a deterministic 7-step acquire/release sequence (`dev/java-interop/run-interop-lock.sh` + `tests/interop_lock.rs`; non-vacuous — a corrupted Java oracle reds the Rust assert). A byte round-trip is N/A for an in-process primitive, so outcome-conformance is the applicable interop. Acquire-timeout exhaustion returns `false` on BOTH sides — Java `acquire` internally catches the `IllegalStateException` raised by `Tasks.throwFailureWhenFinished()` and returns `false` (1.10.0 bytecode exception-table `[0,52)->53` verified), identical to Rust; the conformance is exact (no divergence). Minor residue: per-instance lock table vs Java process-static (mitigated by the `default_lock_manager` singleton); `heartbeat-threads` surface-only (one parked thread/lock). |
| R130 · Encryption (`EncryptionManager`, KMS, encrypted FileIO/manifests) | ❌ (DECLARED — engine registry ENC-1, owner ruling 2026-08-24) | `api/encryption/`, `core/.../encryption` | V3 `spec/encrypted_key.rs` stub only **Engine-gate disposition (2026-08-25):** DECLARED, matching engine registry **ENC-1** (owner ruling 2026-08-24). The fork carries the declaration, not an `EncryptionManager` — no KMS client, no encrypted FileIO, no encrypted manifests, no envelope crypto. What DOES exist is the metadata key REGISTRY and nothing that consumes it: `TableUpdate::AddEncryptionKey`/`RemoveEncryptionKey`, `TableMetadataBuilder::add_encryption_key`/`remove_encryption_key`, `TableMetadata::encryption_keys`, and snapshot `encryption_key_id` — a metadata round-trip only. Unrelated and easy to mistake for one: `io/storage/config/s3.rs` exposes `server_side_encryption_aws_kms_key_id`, which is object-store SSE-KMS, not Iceberg encryption. Do NOT charter a build against this row without a new owner ruling that supersedes ENC-1. |
| R131 · FileIO (S3/GCS/Azure/OSS/fs/memory) | ✅ | `core/.../io`, cloud modules | `io/` + extracted `crates/storage/opendal` (OpenDAL) |
| R132 · Puffin read/write + blob types (theta NDV, DV) | 🟡 | `core/.../puffin`, `api/.../puffin` | `puffin/` (blob coverage partial) |
| R133 · Maintenance: `ExpireSnapshots` | 🟡 (`ReachableFileCleanup` interop-proven both directions A3; only `IncrementalFileCleanup` + `cleanExpiredMetadata` + ref-age deferred) | `api/ExpireSnapshots.java`, `core/RemoveSnapshots.java`, `core/ReachableFileCleanup.java` | `transaction/expire_snapshots.rs` (B1, metadata retention, 1.10.0 bytecode-verified) + `transaction/expire_cleanup.rs` (B2, `ReachableFileCleanup`: post-commit `commit_and_clean` seam, candidates-minus-retained set algebra, live-entry subtraction across data+delete manifests incl. shared-puffin DVs + stats). A3 interop ✅ 2026-06-11 BOTH directions (`tests/interop_expire.rs`, 5 fixtures; Java forced down `ReachableFileCleanup` via `withIncrementalCleanup(false)`). Stays 🟡 — only `IncrementalFileCleanup` + `cleanExpiredMetadata` + ref-age (`max_ref_age_ms`) interop deferred. `CleanupReport`'s content funnel is SPLIT by `DataContentType` 2026-08-23 (additive: the union field is unchanged; typed views are DERIVED from it) so a consumer can fill Spark's three per-content-type `expire_snapshots` columns; DV puffins count as POSITION deletes (Java tags by `content()` alone — `BaseSparkAction$ReadManifest.toFileInfo`, 1.10.0 bytecode). The fast-append all-tombstone STOP-FINDING raised here was FIXED 2026-06-11 (Wave-4 O1). [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-expiresnapshots) |
| R134 · Maintenance: `DeleteOrphanFiles` | 🟡 | `api/actions/DeleteOrphanFiles.java`, `spark/.../DeleteOrphanFilesSparkAction.java`, `core/ReachableFileUtil.java`, `core/util/FileSystemWalker.java`, `core/actions/FileURI.java`, `core/hadoop/HiddenPathFilter.java` | `maintenance/delete_orphan_files.rs` (2026-06-11, A2): builder action over `FileIO::list` (A1). Valid-file universe = ALL snapshots' content files (every entry incl. DELETED tombstones, Java `ManifestFiles.read` not `liveEntries`) + all manifests + all manifest lists + current/previous metadata.json (non-recursive) + version-hint + statistics/partition-statistics. Hidden-path filter (`_`/`.` except `_<partition-field>=`), `olderThan` cut (default now−3d), URI-normalized orphan join (scheme-less local path matches any actual scheme per Java `uriComponentMatch`), `PrefixMismatchMode {Error,Ignore,Delete}`, `equal_schemes` (defaults `{s3n,s3a→s3}` merged) / `equal_authorities`, GC gate, delete failures collected. CAVEAT: action is MAIN-source (no 1.10.0 Spark bytecode); helpers (`FileURI`/`PrefixMismatchMode`/`HiddenPathFilter`/`PartitionAwareHiddenPathFilter`) bytecode-verified vs core/api 1.10.0. Deferred: executor parallelism (sequential sweep), bulk deletes, `compareToFileList`/streaming, interop. |
| R135 · Maintenance: `RewriteDataFiles` (compaction) | 🟡 | `api/actions/RewriteDataFiles.java`, `core/actions/{SizeBasedFileRewritePlanner,BinPackRewriteFilePlanner,RewriteDataFilesCommitManager}.java` | `maintenance/rewrite_data_files.rs` (2026-06-11, O2) — `RewriteDataFiles::new(table)` bin-pack over the seq-preserving `RewriteFiles` commit, per-partition grouping with the 1.10.0 candidate/group predicates + bytecode-cited defaults; `.filter` is FILE-SELECTION only (residual stripped before the rewrite read — O2 reviewer fix, the data-loss guard); STARTING-snapshot seq stamp when `use_starting_sequence_number`. Empty plan = no-op, no commit. `remove-dangling-deletes` COMPOSED 2026-08-23 (F-3): opt-in builder, Java default false, runs only on a NON-empty plan (both Java `EMPTY_RESULT` early returns precede the step at offset 113), errors propagate, and the total folds into the new top-level `RewriteDataFilesResult::removed_delete_files_count` — `FileGroupRewriteResult` gains no field because Java's per-group accessor is a constant-zero `default`; `RewriteDataFilesResult` gains `#[non_exhaustive]`. `delete-ratio-threshold` (Java `tooHighDeleteRatio`, default 0.3; only FILE-SCOPED deletes count) and v3 DV-removal-on-rewrite landed 2026-08-27. F-16r (2026-09-01): bounds-only file-scoped parquet position deletes fire Java `tooHighDeleteRatio` (via `ContentFileUtil.referencedDataFile` equal `file_path` bounds). A 100%-dead in-band file is rewritten and the fork drops its parquet delete in the same `RewriteFiles` commit (`plan_dv_removal`); Java 1.10.0 `ManifestFilterManager.isDanglingDV` drops DVs only. Named divergence: `removed_delete_files_count` includes those apply-path parquet drops, which Java's Result does not; Java's Result DOES count apply-path DV drops (`RewriteFileGroup.asResult` from `danglingDVs.size()`). Deferred: partial progress / concurrency / sort+zorder strategies / output-spec / input SPLITTING / Java interop. Named divergence (2026-08-21, RES-1): this action rolls OUTPUT at the RESOLVED TARGET under its own recorded deviation, where Java's `SizeBasedFileRewritePlanner.writeMaxFileSize()` rolls at `target + 0.5 * (max - target)`; the sibling `RewritePositionDeleteFiles` (row R136) ports `writeMaxFileSize` exactly, so the two ports deliberately differ on the roll bound. F-7 slice 1 (2026-08-30): format v3 writes stored `_row_id` / `_last_updated_sequence_number` through the rewrite (Java `SparkRewriteTable.rewriteSchema`); v2 does not. Compact D2 (`run-interop-row-lineage.sh`) is a Java `IcebergGenerics` read of a fork-compacted table: stored ids match pre-compaction. Stays 🟡 on remaining action residues (partial progress / concurrency / sort+zorder / output-spec / input splitting) and on Spark-action output comparison. Runner read-semantics is MAIN-source. [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-rewritedatafiles-compaction) |
| R136 · Maintenance: `RewritePositionDeleteFiles` | ✅ (2026-06-17 — read-identity-proven offline + interop; V2 PARQUET only, V3 Puffin DV and V2 ORC/Avro out by documented divergence; 2026-08-21 Java size gate ported, with 4 named divergences and 2 named residues in the evidence cell; 2026-08-25 F-7 U3 extended it to format v3 — an ENGINE-FIRST arm whose only Java counterpart is the Spark surface, evidenced by read identity) | `api/actions/RewritePositionDeleteFiles.java`, `core/actions/{SizeBasedFileRewritePlanner,BinPackRewritePositionDeletePlanner,RewritePositionDeletesCommitManager}.java` | `maintenance/rewrite_position_delete_files.rs` — port of Java `RewritePositionDeleteFiles` (engine-agnostic core; wired as one of the twelve `ActionsProvider` methods, row R153) — planner-complete for the size gate; the per-group `rewriteResults()` list is deferred (named residue (e)). Bin-packs live PARQUET position-deletes into groups of at most `max-file-group-size-bytes` per `(spec_id, partition)` — Java keys on `coercePartition` with the spec NOT in the key — then admits a bin on Java's three-clause size gate `enough_input_files` OR `enough_content` OR `too_much_content`, `min-input-files` default 5 (2026-08-21; the pre-gate port admitted ANY group of two, and a LONE file above `max-file-size-bytes` is admitted by `too_much_content`, which carries no `size > 1` guard). Reads pairs by reserved field id, sorts, and rewrites under one `RewriteFiles` per BIN, possibly into several outputs rolled at Java `writeMaxFileSize`, each stamped with THAT BIN's MAX rewritten data-seq (silent-corruption seq staller mutation-proven). 64 offline read-identity and admission tests (literal count, `grep -c` of `#[tokio::test]` over `maintenance/rewrite_position_delete_files_tests.rs`) + interop PROVEN 2026-06-17 (`tests/interop_rewrite_pos_deletes.rs`, Java reads identical live ids PRE vs POST). Row ✅ 2026-06-17. **4 named DIVERGENCES.** (a) GROUPING KEY — Java keys on `coercePartition(unifiedType, spec, partition)` with the spec never in the key, the fork on `(spec_id, partition)`; strictly finer, and at a floor of five it is OUTCOME-FLIPPING, so the pre-gate "only ever compacts more than Java" no longer holds (grouping residue homed on row R165; `integrations/datafusion/src/physical_plan/delete.rs` describes the same key and would follow a future unification). (b) FORMAT SKIP — the fork skips EVERY non-Parquet position delete where Java's planner is format-blind: V3 Puffin DELETION VECTORS (never bin-packed — file-scoped, one DV per data file) AND, newly named 2026-08-21, V2 ORC and Avro position deletes, which the same `file_format() != Parquet` predicate drops. NARROWED 2026-08-25 (F-7 U3): the skip is now reached only on V1/V2, because `execute` dispatches a V3 table to the DV arm below before it runs — so the skip's Puffin leg is UNREACHABLE end to end (a V1/V2 table cannot commit a Puffin position delete either) and survives as defensive code for externally written metadata; the ORC and Avro legs are unchanged and still pinned. (c) COMMIT GRANULARITY — one `RewriteFiles` per BIN against Java's single `commitOrClean(Set<group>)`, so the snapshot COUNT diverges and a mid-loop failure leaves earlier bins committed with no rollback. (d) SATURATING input-size sum where Java's `long` WRAPS. **2 named RESIDUES.** (e) the per-group `Result` list (Java's one ABSTRACT `rewriteResults() -> List<FileGroupRewriteResult>`, from which the four counts are DEFAULT methods) is NOT ported — inferable from the aggregates while a partition produced exactly ONE group, no longer inferable now that a partition can yield several bins. (f) NAMED NON-PORT of `inputSplitSize` / `expectedOutputFiles` — both are Java READ-side split options with no consumer in this action — together with the fork-specific bounded chunk feed that replaces them and its residual writer overshoot. **FORMAT-VERSION DISPATCH (2026-08-25, F-7 U3) — ENGINE-FIRST.** V1/V2 keep the bin-pack arm above unchanged. On V3 `execute` runs `rewrite_to_deletion_vectors` instead: every live filter-matching PARQUET position delete becomes one Puffin DELETION VECTOR per referenced data file, merged with that data file's existing DV, swapped in ONE `RewriteFiles`. **THE JAVA COUNTERPART EXISTS AND IS SPARK-ONLY — CORRECTED 2026-08-26 after a first pass wrongly claimed it does not exist.** `iceberg-core` 1.10.0 carries the planner, the group, the result and the commit manager; the RUNNER is `RewritePositionDeleteFilesSparkAction` in `iceberg-spark-runtime-4.0_2.13-1.10.0`. Decoded from that jar: `execute()` early-returns `EMPTY_RESULT` ONLY when `TableUtil.formatVersion >= 3` (offset 66-70) AND `requiresRewriteToDVs()` is false (74-77), where `requiresRewriteToDVs` scans the `position_deletes` metadata table under the same filter for a delete file that is not already a DV. So on a v3 table that still holds legacy parquet position deletes Java FALLS THROUGH to `BinPackRewritePositionDeletePlanner.plan()` and `doExecute` — **Java converts them to DVs on v3**, writing through `SparkPositionDeletesRewrite$DVWriter` over `PartitioningDVWriter(OutputFileFactory, loadPreviousDeletes)`. Not mirroring it is a scope decision (`iceberg-spark` is outside the core/api parity envelope this fork tracks), not an absence. **TWO DELIBERATE DIVERGENCES FROM THAT SPARK ACTION, argued rather than inherited.** (V-1) NO SIZE GATE: Java reaches the planner on v3, so it DOES apply `outsideDesiredFileSizeRange`, the bin packer and the three-clause group gate; the fork converts EVERY admitted legacy file. Reasons: a DV is file-scoped, so the gate's output-size objective describes a shape this arm cannot produce; a gated run leaves a table half-legacy, and the fork's own V3 merge-on-read DML arm REFUSES a data file still covered by a legacy position delete (row R114's U4 residue), so a permanently-declined group is a permanently-unusable table; and gating can produce an UNREADABLE table. Java's rewrite writer passes `path -> null` as `loadPreviousDeletes` (decoded: `SparkPositionDeletesRewrite$DVWriter.lambda$new$0` is `aconst_null; areturn`), unlike the DML writer `SparkPositionDeltaWrite$BaseDeltaWriter`, which passes a real `PreviousDeleteLoader`. So a Java group holding a data file's parquet deletes but NOT that file's existing DV writes a SECOND DV for it and removes only its own group's files — two live DVs for one data file, which `DeleteFileIndex` rejects at scan. An ungated single-commit arm cannot reach that state. Converting everything also makes the fork's arm converge in one run. (`path -> null` says nothing about whether an unconverted PARQUET delete gets shadowed; that is the closure below, not this clause — corrected 2026-08-26.) (V-2) ONE COMMIT PER RUN: Java commits per group through `RewritePositionDeletesCommitManager` (or per partial-progress batch); the fork commits once. There are no groups on this arm to commit per, and one commit makes the conversion atomic — a mid-run failure cannot leave a table where some data files have DVs and others still have the parquet deletes those DVs would shadow. It also does NOT inherit divergence (c)'s snapshot-count multiplication. THE COST, stated beside the benefit: there is NO PARTIAL PROGRESS. The single `RewriteFiles` runs under `validate_from_snapshot`, so one conflicting concurrent commit discards the whole conversion, and on a large table under steady write traffic the action may never land. Java carries `partial-progress.enabled` for exactly this. Atomicity is still the call — a half-converted table is one where some data files have DVs and others still carry the parquet deletes those DVs would shadow — but the trade is real. EVIDENCE: read identity, by the V3 leg of `dev/java-interop/run-interop-rewrite-pos-deletes.sh` (Java 1.10.0's `IcebergGenerics` reads live ids 100/130/200/230 identically on the upgraded-V3 table with four legacy parquet position deletes and on the same world after conversion to two DVs, plus the conversion shape; a V3 read-identity sabotage leg fails closed) and by 11 offline tests. No Spark-output comparison is claimed. WHY THE OLD ARM COULD NOT SERVE: a fresh PARQUET position delete is rejected on V3 by `SnapshotProducer::validate_delete_file_for_version`, so compacting one into another cannot commit at all. **TWO CLOSURES, both delete-correctness lines, both mutation-proven.** (1) THE PUFFIN CLOSURE: delete-file removal is PATH-keyed (`SnapshotProducer::resolve_delete_file_paths`) while one Puffin holds a blob per data file, so superseding one DV removes every SIBLING blob in the same Puffin — each sibling is rewritten too, or its deleted rows come back. It deliberately rewrites siblings in partitions the FILTER EXCLUDED; the filter restricts what is CONVERTED, not what a superseded Puffin drags in. (2) THE SHADOW CLOSURE (`refuse_shadowed_deletes`): a DV wins over every position delete for the same data file (`get_deletes_for_data_file` returns on the `dv_by_path` hit, Java `findDV`), so a live delete the filter EXCLUDED goes inert the moment this run writes a DV over a data file it still covers. Reachable because a file-scoped delete is routed BY PATH with NO partition condition while the filter judges it by its stamped tuple, which is routinely a different spec's: two file-scoped deletes for one data file stamped `x=0` and `x=1`, run under `filter(x = 0)`, returned `Ok` with live rows `{12}` becoming `{12, 11}` before the fix. The run now FAILS CLOSED. BOTH legs are pinned: the path leg by two file-scoped deletes stamped differently, the PARTITION leg — the fork's DEFAULT shape, since a fork-written delete carries `truncate(16)` path bounds — by an admitted file-scoped delete plus an excluded partition-scoped one, whose mutation returned `Ok` with live rows `{10}` becoming `{10, 12}`. HONEST ZEROS: `Ok` with four zero counts means "looked, found nothing to convert", and on this arm that reading is total WITHIN THE FILTER'S SCOPE — an unreadable position-delete format, two live DVs for one data file, a DV whose data sequence number would fall below its data file's, and a shadowed excluded delete are all `Err`. QUALIFIED: an unreadable delete the filter rejects that touches nothing this run converts is still skipped silently. That is the distinction the downstream engine needed, expressed in the EXISTING `Result` type; no public API member was added. STALE POSITIONS ARE DROPPED, NOT REFUSED (corrected 2026-08-26): a position naming a data file the current snapshot no longer holds can delete nothing, so it is discarded and only the delete FILE is consumed — the V1/V2 arm carries such positions forward to the same effect. Refusing dead-ended the table, because `RemoveDanglingDeleteFiles` (row R137) keys on `(spec_id, partition)` and the partition's live data sequence, so it cannot clear a delete file that still names ONE live data file. **DIVERGENCE (g), CORRECTED 2026-08-26 — its safety envelope was refuted by the fork's own decode.** The merge makes a SHADOWED position effective, which DELETES rows the table returns today. The earlier text said that shape is not producible by a real writer; that is false. Java's REWRITE passes `path -> null`, so a run that puts a data file's parquet deletes in an ADMITTED group while a differently-stamped file-scoped delete for the same file sits in a DECLINED group writes a DV that is NOT a superset and leaves the unfolded delete live and inert. Java 1.10.0's own action produces it. Measured on that shape with NO filter, before the fix: `Ok` with rewritten 2 / added 1 and live rows `{11, 12}` becoming `{11}` — y=12 silently gone, recoverable only by time travel. CLOSED, symmetrically: `plan_deletion_vectors` now REFUSES when an admitted legacy delete carries a position its data file's live DV does not already hold. A superset DV — what Java's DML path writes — still merges and moves nothing. The remedy is to rewrite the vector to cover them or drop the legacy delete; widening the filter does NOT help and the message no longer says it does. So the arm is total in BOTH directions: it never resurrects a row and never silently deletes one — at the cost of limit (k) below, which is that trade's price. **NAMED LIMIT (k), 2026-08-26 — the price of the (g) fix, and it has an IN-TREE ESCAPE (corrected 2026-08-27, after a first pass wrongly called it a dead end).** A JAVA-REWRITE-SHAPED table — a NON-SUPERSET deletion vector beside a live legacy position delete for the same data file — cannot be converted BY THIS ARM at any filter width. The refusal is the right trade: converting would DELETE rows the table returns today. **THE ESCAPE, EXECUTED not reasoned — and corrected 2026-08-27: the default `delete-ratio-threshold` 0.3 now admits this 1/3 file.** `RewriteDataFiles` with `remove_dangling_deletes(true)` clears it. On the exact shape (live `{11, 12}`, the vector holding only position 0, the legacy delete masking position 2) that returns 1 data file rewritten / 1 added / 2 delete files removed, live rows stay `{11, 12}`, no delete file survives, and a second V3 run returns honest zeros. Without `remove_dangling_deletes` the rewrite still drops the DV (apply-path accounting) and leaves the shadowed parquet delete live. `DELETE_FILE_THRESHOLD_DEFAULT` is `usize::MAX` (Java's `Integer.MAX_VALUE`), so the delete-count candidate clause is OFF by default. The ratio clause (default 0.3) still admits a well-sized file whose file-scoped deletes cover at least 30% of its rows. `min_input_files` is NOT needed: the gate's `any_too_many_deletes` disjunct carries no `size > 1` guard, so the delete count admits the lone file on its own. NOT UNIVERSALLY UNAVAILABLE AT DEFAULTS, and the opposite over-claim is the trap: availability depends on the rewrite planner admitting the covered data file, which at shipped defaults it need not do — on a partition holding five or more small files `enough_input_files` admits the group and the escape fires unknobbed. On the fixture (k) cites, the ratio clause admits the file. WHY it works: the legacy delete is SHADOWED, so the rewrite's scan never reads it — the new data file gets exactly today's live rows and both delete files fall dangling. Pinned by `test_v3_non_superset_refusal_is_cleared_by_rewrite_data_files`, which runs `remove_dangling_deletes(true)` at defaults and asserts the refusal names that escape. The two paths that do NOT work, and the citations stand: `RemoveDanglingDeleteFiles` (row R137) removes only deletes that apply to nothing, and this one still applies; the V3 merge-on-read DML arm refuses a data file a legacy parquet position delete still covers (row R114's U4 residue). THIS IS WHY (k) AND (j) DIFFER, and the distinction is load-bearing: in (k) the blocking delete is SHADOWED, so the rewrite can step over it; in (j) it is LIVE, so the rewrite's scan must read it and cannot. NAMED LIMIT (j), 2026-08-26: a v3 table whose ORC or Avro position deletes OVERLAP convertible ones CANNOT BE CONVERTED AT ALL. A file-scoped ORC delete the filter excludes still references a planned data file, so the shadow closure refuses; widening the filter routes it to `FeatureUnsupported` instead. No filter width clears it, and the refusal says so rather than offering a remedy that does not exist. (k)'s `RewriteDataFiles` escape does NOT transfer here, and the EVIDENCE CLASS is weaker than (k)'s, stated rather than blurred: the ORC delete is LIVE, so the rewrite's scan must apply it, and `caching_delete_file_loader` dispatches only Puffin-versus-everything-else — every non-Puffin position delete goes to `parquet_to_batch_stream`, with no ORC or Avro branch — so a genuine ORC delete fails at the parquet reader and blocks the rewrite. That is a CODE READ, not an executed result: the fork has no ORC position-delete writer, so no genuine fixture can be built, and the in-tree ORC fixtures are parquet bytes carrying an ORC stamp, which the loader reads successfully and which therefore prove nothing about this claim. **TRIGGER — (j) HOLDS ONLY WHILE THE LOADER HAS NO ORC/AVRO BRANCH.** The block is an INCIDENTAL parquet parse failure, not a deliberate guard. Land an ORC or Avro delete reader and (j) dissolves silently: the scan would apply the delete, `RewriteDataFiles` would clear the table, and nothing in the tree would flag that this limit had ended. Re-audit (j) in the same change as any such reader. NAMED RESIDUE (i): byte counts are summed over DISTINCT file paths, because every DV of one Puffin carries that whole Puffin's size. LETTERING: (h) was the dangling-delete refusal and was RETIRED 2026-08-26 when stale positions became a drop; the letter is not reused, so the sequence runs (e), (f), (g), (i), (j), (k). [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-rewritepositiondeletefiles) |
| R137 · Maintenance: `RemoveDanglingDeleteFiles` | ✅ | `api/actions/RemoveDanglingDeleteFiles.java`, `spark/.../RemoveDanglingDeletesSparkAction.java` | `maintenance/remove_dangling_delete_files.rs` (2026-06-11, O3) — `RemoveDanglingDeleteFiles::new(table).execute(catalog)` removes, in ONE `Replace` snapshot, every delete file in the current snapshot that can no longer apply to any live data file (dangling predicate ported verbatim from the Spark `findDanglingDeletes` SQL). Interop ✅ 2026-06-16 + DV e2e gap closed. [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-removedanglingdeletefiles) |
| R138 · Maintenance: `ComputeTableStats` / `ComputePartitionStats` | 🟡 | `core/PartitionStatsHandler.java`, `core/PartitionStats.java`, `api/Partitioning.java`, `api/util/PartitionUtil.java`, `api/actions/Compute*.java` | `maintenance/partition_stats.rs` — `compute_partition_stats` ports Java 1.10.0 `PartitionStatsHandler` FULL-compute aggregation (2026-06-12, X1); `UpdatePartitionStatistics` commit + interop sabotage battery landed. Stays 🟡 NOT for partition stats (that bar is met) but because `ComputeTableStats` (theta-NDV) is a separate Group-Y surface and full `Compute*Stats` breadth beyond NDV is not yet at Java parity. [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-computetablestats--computepartitionstats) **Unifier re-homed 2026-08-16 (#202, row R165):** the partition-type unifier now lives in `spec/partitioning.rs`; the stats compute path delegates (`maintenance::unified_partition_type` is a deprecated alias), and the G4 dropped-source fix removes a real failure mode — stats no longer error on tables whose partition source column was dropped. |
| R139 · Maintenance: `SnapshotTable` / `MigrateTable` / `RewriteTablePath` | 🟡 (`RewriteTablePath` FULL-rewrite BUILT + interop-proven 2026-06-17; `SnapshotTable`/`MigrateTable` still ❌ — need an external source table this library has no surface for) | `api/actions/`, `core/RewriteTablePathUtil.java` | `maintenance/rewrite_table_path.rs` — FULL-rewrite port of Java 1.10.0 core `RewriteTablePathUtil` (stage-and-plan: rewrites every absolute path prefix across the metadata graph + position-delete content into a staging dir, emits a copy-plan; data files not physically copied). Mirrors the 1.10.0 divergences exactly; 15 offline tests. Interop ✅ bidirectional 2026-06-17 (`tests/interop_rewrite_table_path.rs`, Java drives the real `replacePaths`). Stays 🟡 — incremental mode is named residue; `SnapshotTable`/`MigrateTable` remain ❌ (need an external source table this library has no surface for). [full narrative](archive/2026-06_matrix-cell-narratives.md#maintenance-snapshottable--migratetable--rewritetablepath) |
| R140 · Partition statistics: `UpdatePartitionStatistics` commit surface + `ComputePartitionStats` ACTION wrapper | ✅ (2026-06-17 — both residue items built; byte-format/registration interop-proven by Z3/R2/R3, see the Maintenance row) | `api/UpdatePartitionStatistics.java`, `core/.../SetPartitionStatistics`/`RemovePartitionStatistics`, `api/actions/ComputePartitionStats.java`, `api/PartitionStatisticsFile.java` | The COMPUTE core + on-disk write + read-back + interop (Z3/R2/R3) live in the `Maintenance: ComputeTableStats/ComputePartitionStats` row above — see there, ONE home. Both residue items now BUILT (G2, 2026-06-17): (a) `transaction/update_partition_statistics.rs` — `UpdatePartitionStatisticsAction` is the 1:1 port of Java `UpdatePartitionStatistics` (`extends PendingUpdate<List<PartitionStatisticsFile>>`): `set_partition_statistics(file)`/`remove_partition_statistics(snapshot_id)` (javap-confirmed) emitting `TableUpdate::SetPartitionStatistics`/`RemovePartitionStatistics`, exposed via `Transaction::update_partition_statistics()` (the `update_statistics()` seam clone); the commit attaches `AssertTableUUID` itself (a non-snapshot metadata update — Java `UpdateRequirements.forUpdateTable`), so it matches the proven `register_partition_stats_file` path, which now DELEGATES through this seam (ONE commit path, no duplicate). (b) `maintenance/compute_partition_stats.rs` — `ComputePartitionStats::new(table).snapshot_id(id).execute(catalog)` is the port of Java `ComputePartitionStats`/`BaseComputePartitionStats`: resolve snapshot (default current) → `compute_and_write_stats_file` → register via the seam → `ComputePartitionStatsResult{table, statistics_file}` (Java `Result.statisticsFile()`); the `Ok(None)` empty-collection case surfaces as a typed `DataInvalid` (Java `execute()` returns non-null, but there is no file to return). Wired into `ActionsProvider::compute_partition_stats` (row R153). 5 seam tests + 5 action tests (live compute/write/register over a partitioned throwaway MemoryCatalog table, explicit-snapshot, unknown-snapshot, unpartitioned-error, no-current-snapshot). Note (2026-08-16): the compute path's partition type moved homes to `spec/partitioning.rs` (row R165); no capability change. |
| R141 · Table-level statistics (`UpdateStatistics`) | ✅ | `api/UpdateStatistics.java` | `transaction/update_statistics.rs` |
| R142 · Metadata inspection tables | 🟡 | `core/.../*Table` (~15 variants) | `inspect/` — the COMPLETE ported Java table set: `snapshots`, `manifests`, the `files` family, `entries`, `history`/`refs`/`metadata_log_entries`, `partitions`, the five `all_*` tables, and the `readable_metrics` virtual column; all SQL-queryable via `iceberg-datafusion`. **Inspection interop COMPLETE (2026-06-09→10, Direction-1 — read-only tables):** pure-metadata (offline JSON) + manifest-reading A1–A3 + scan-planning A4 + `readable_metrics` (run.sh-driven). **Empty-partition drop LANDED 2026-08-14 (FW-3):** Java `BaseFilesTable.schema` / `BaseEntriesTable.schema` `TypeUtil.selectNot(DataFile.PARTITION_ID=102)` when `partitionType.fields()` is empty (`files`/`data_files`/`delete_files`/`all_files`/`all_data_files`/`all_delete_files` + `entries`/`all_entries`); Java `PartitionsTable.schema` `TypeUtil.select` omits field 1 (`partition`) AND field 4 (`spec_id`). **`PositionDeletesTable.calculateSchema` SCHEMA LANDED 2026-08-15 (FB-2):** fixed metadata columns + partition-id reassignment + the empty-partition `partition` drop (`inspect/position_deletes.rs`); scan is refused loud (`FeatureUnsupported`) — the `PositionDeletesBatchScan` port remains a residual. Provider `$`-name split is last-`$` + `MetadataTableType` vocabulary so a base table `a$b` resolves. **Positional partition-tuple read FIXED 2026-08-15 (PT-0):** `inspect/data_file.rs::append_partition` walked the partition tuple BY INDEX with no field-id matching, so under spec evolution a file written under an older spec had its values read into the projected type's columns positionally — same-typed fields (two `long` identity partitions) landed in the WRONG column with no error at all on `files`/`data_files`/`delete_files`/`all_*`/`entries`/`all_entries`/`partitions`, and mismatched types errored the whole scan. It now matches each projected field BY FIELD ID against the field ids of the spec the file was written under (`DataFile::partition_spec_id` → `partition_field_ids_by_spec`), null-filling projected fields the source spec lacks — the field-id half of Java `PartitionUtil.coercePartition` / `StructProjection.createAllowMissing`. Pinned by a Java-legal spec-evolution fixture (spec 0 `identity(x)`@1000; spec 1 `identity(y)`@1001 + `identity(x)`@1000, tuple order ≠ field-id order, both `long`) whose files-table scan reported the spec-0 file's `x` value under `y` before the fix. **Unifier hoisted 2026-08-16 (PT increment A):** `spec/partitioning.rs` owns Java `Partitioning` (`partition_type` / `grouping_key_type` / `union_partition_types` / `is_partitioned` / `coerce_partition`) with G1–G4 fixed; `maintenance::unified_partition_type` is a thin delegate. **`partitions` adopts the unified type 2026-08-16 (PT increment B):** `PartitionsTable::try_new` stores `unified_partition_type`; schema/scan/`is_unpartitioned` use it; rollup keys are `coerce_partition` (Java `PartitionUtil.coercePartition`). **`position_deletes` schema adopts the unified type 2026-08-16 (PT increment D):** remapped-child-id set and empty-partition drop follow `Partitioning.partitionType`; scan stays refused (FB-2). **`files`/`entries` adopt the unified type 2026-08-16 (PT increment C):** `data_file_fields` / `omit_empty_partition_field` / Files+Entries schema+scan project `Partitioning.partitionType`; `append_partition` stays the PT-0 field-id walk (A1, no coerce-at-append). **`readable_metrics` leaf doc strings LANDED 2026-08-15 (F-1, found by #199's battery):** Java `MetricsUtil.readableMetricsSchema` builds each of the six metric leaves with the FOUR-arg `optional(nextId.incrementAndGet(), m.name(), m.colType(field), m.doc())`, so every leaf carries its `ReadableMetricColDefinition` doc; `readable_metrics.rs::readable_metrics_field` set docs on the per-column struct and the top-level field only and left all six leaves `doc: None`. The six Java strings (`column_size` "Total size on disk", `value_count` "Total count, including null and NaN", `null_value_count` "Null value count", `nan_value_count` "NaN value count", `lower_bound` "Lower bound", `upper_bound` "Upper bound") are now emitted and pinned in `java_schema_shape.rs`. Remaining residuals keep the row 🟡: interior readable_metrics field-id order (Java HashMap-order is non-portable; compare by name), `position_deletes` scan unported (schema landed FB-2 / unified type D). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| R143 · Name mapping (schema-less Parquet) | ✅ | `mapping/` | `spec/name_mapping/` — schema-less Parquet reads resolve field ids by column name via `schema.name-mapping.default`. **Scan-wiring closed 2026-07-17 (BUG-002):** the property is parsed ONCE per plan (`TableProperties::PROPERTY_DEFAULT_NAME_MAPPING`, Java `NameMappingParser.fromJson` semantics — invalid/empty ⇒ loud `DataInvalid` naming the property, absent ⇒ `None`) and threaded onto every `FileScanTask` (`scan/context.rs` → `parse_name_mapping`), retiring the hardcoded-`None` TODO; the e2e `TableScan` pins exposed and FIXED two latent read-path bugs in the same unit — field-id-based projection when a mapping is present (`arrow/reader.rs`) and a positional-reorder column mislabel (`arrow/record_batch_transformer.rs::compare_schemas`). Delete files carry Iceberg field ids and Java's engine-agnostic reader never name-maps them (`iceberg-data` `BaseDeleteLoader`/`GenericReader` bytecode has no `withNameMapping`), so delete-task sites stay unchanged (Java-parity). Cross-engine interop: `dev/java-interop/run-interop-name-mapping.sh`. |
| R144 · Events / listeners | ✅ (2026-06-19 — ported + events genuinely fire on real scans/commits, unit-proven) | `api/events/` (`Listener`, `Listeners`, `ScanEvent`, `IncrementalScanEvent`, `CreateSnapshotEvent`), fired from `core/.../SnapshotScan`/`BaseIncrementalScan`/`SnapshotProducer` | `events/mod.rs` — 1:1 port of Java's process-global event bus: `Listener<E>` trait + static `register`/`notify_all` registry (std-only, exact-`TypeId` keying, clone-then-drop-guard re-entrancy-safe) + the 3 immutable event structs. 3 emit sites WIRED and unit-proven to genuinely fire on real fixtures: `ScanEvent` @ `scan/mod.rs`, `CreateSnapshotEvent` @ `transaction/mod.rs` (one per `AddSnapshot`, `catch_unwind`-isolated, summary excludes the `operation` key), `IncrementalScanEvent` @ `scan/incremental.rs` (append + changelog). 14 tests; row ✅ 2026-06-19. Residue (NAMED): interop is N/A (in-process bus); table-name cosmetic; panic swallowed silently (no `tracing` dep yet). [full narrative](archive/2026-06_matrix-cell-narratives.md#events--listeners) |
| R145 · Type utilities (prune/assign-ids/reassign/check-compat) | 🟡 | `types/TypeUtil.java`, `types/CheckCompatibility.java`, `types/AssignFreshIds.java`, `types/ReassignIds.java`, `types/GetProjectedIds.java`, `types/PruneColumns.java` (all 1.10.0) | `spec/schema/` — full engine-agnostic `TypeUtil` port (2026-06-18), each fn bytecode-decoded from the 1.10.0 jars: prune/project (`prune_columns.rs`+`utils.rs`), assign-fresh-ids, reassign-ids, check-compatibility, get-projected-ids. All public + pure + Java-faithful, each pinned by a unit test, with a real consumer (`update_schema.rs` delegates to assign-fresh-ids — no dead code). **Stays 🟡 pending the bidirectional Java interop round-trip the fork-flip ✅ bar requires** (unit-test + bytecode-decode only so far; e.g. Java reassigns ids / checks compatibility ⇄ Rust agrees on the same schema, both directions). Demoted from ✅ 2026-06-20 to meet the legend's strict bar. [full narrative](archive/2026-06_matrix-cell-narratives.md#type-utilities-pruneassign-idsreassigncheck-compat) Note (2026-08-16): the `StructProjection.createAllowMissing` sliver is ported as `coerce_partition` — see row R165; this row's ✅ bar (bidirectional round-trip) is unaffected. |
| R146 · Conflict-detection + `caseSensitive` + append-only builder surfaces on write actions | ✅ (full Java 1.10.0 public builder surface ported; the two former "DeleteFiles/ReplacePartitions `conflictDetectionFilter`" residue items are VOID — `javap`-proven NOT in the 1.10.0 API; `validateAppendOnly` ported 2026-06-16) | `api/DeleteFiles.java`, `api/OverwriteFiles.java`, `api/RowDelta.java`, `api/ReplacePartitions.java`, `core/MergingSnapshotProducer.java` | `transaction/snapshot.rs` (`validate_no_new_deletes_for_data_files` + the `files_after` walk) — the `MergingSnapshotProducer.validate` stack is implemented; `case_sensitive(bool)` landed 2026-06-13, interop-proven ✅ 2026-06-16 on `DeleteFiles`/`OverwriteFiles`/`RowDelta`; `validateAppendOnly` ported 2026-06-16. The two former `conflictDetectionFilter` residue items are VOID (`javap`-proven NOT in the 1.10.0 API). [full narrative](archive/2026-06_matrix-cell-narratives.md#conflict-detection--casesensitive--append-only-builder-surfaces-on-write-actions) |
| R147 · `DeleteFiles.deleteFromRowFilter(Expression)` (delete-by-predicate) | ✅ (2026-06-16, with 2 named fail-safe divergences) | `api/DeleteFiles.java` (`deleteFromRowFilter`), `core/StreamingDelete.java` | `transaction/delete_files.rs` `delete_from_row_filter(Predicate)` (2026-06-13) — per-file partition-residual KEEP/DELETE/PARTIAL-error via the shared `resolve_filter_deletes` (reused, not forked); 10 unit tests, 6/6 mutations caught (two-spec residual + by-path/filter dedup count); `operation()`=Delete constant, bytecode-verified vs 1.10.0. **Bidirectional interop ✅ 2026-06-16 (Wave 3):** the delete RESOLUTION (which file set is removed) agrees with Java `StreamingDelete.deleteFromRowFilter` BOTH directions over a PARTITIONED `identity(Category)` `{id,Category,y}` table — `filter_delete_partition` (DELETE cat=a, survivors={b}) + `filter_keep_complement` (DELETE cat=b, survivors={a}) + `filter_partial_error` (some-but-not-all ⇒ ERROR) — each engine runs the symmetric delete on the OTHER's on-disk manifests + a fail-closed sabotage battery (semantic-rename + structural-truncate), via `dev/java-interop/run-interop-builder-flips.sh` + `crates/iceberg/tests/interop_builder_flips.rs`. **Two named fail-safe divergences (Rust is STRICTER, never wrongly deletes/loses data):** (1) `markedForDelete` short-circuit unported — Rust errors where Java would delete a by-path file that ALSO partially matches (pinned by `delete_files.rs` `..._bypath_and_partial_match_diverges_failsafe`; fix belongs in `snapshot.rs`, deferred); (2) an empty-match filter (no live file matches) — Rust REJECTS the empty no-op commit, Java COMMITS a no-op Delete snapshot (live-oracle finding, kept out of the interop set as orthogonal to the capability). `caseSensitive(boolean)` co-proven on the same builder — see row R146. |
| R148 · `ScanTaskGroup<T>` + `Scan.planTasks()` split/grouping + `SplittableScanTask.split` + `BatchScan`/`ContentScanTask<F>`/`DataTask` | ✅ | `api/ScanTaskGroup.java`, `api/BaseScanTaskGroup.java`, `api/SplittableScanTask.java`, `api/BatchScan.java`, `api/ContentScanTask.java`, `api/DataTask.java` | LANDED + interop-proven ✅ both directions 2026-06-17 (U1 split/bin-pack core + U2 typed `BatchScan`). `scan/task_group.rs` + `scan/task.rs` `FileScanTask::split` (offsets-aware; Puffin non-splittable; AVRO/ORC declined; the `length == 0` whole-file sentinel, an already-ranged parent (EITHER endpoint moved off the whole file) and a `_pos`-projecting task all passed through — see the NAMED divergences below) + `scan/bin_pack.rs` (faithful `BinPacking.PackingIterator`, MoR-delete-aware weight) + `TableScan::plan_tasks()` above an unchanged `plan_files()`. 30 offline tests; bidirectional interop ✅ via `tests/interop_scan_plan.rs` (group shape == Java `planTasks()`, fail-closed sabotage). U2 typed `BatchScan` (`scan/batch.rs`, row R124) ✅ via the same oracle. **Adjacent-split merge landed 2026-07-24:** `CombinedScanTask::new` now ports Java's `BaseCombinedScanTask(List)` ctor → `TableScanUtil.mergeTasks` (javap-decoded: `REF_newInvokeSpecial BaseCombinedScanTask.<init>:(List)` is the mapper `planTasks` runs each bin through), coalescing contiguous same-file byte-range splits that share a bin into ONE spanning member (`FileScanTask::can_merge`/`merge_with` mirror `SplitScanTask.canMerge`/`merge` — same data-file path + exact `offset+len==next.start`, lengths summed, `DataFile.equals` is identity so path is the file-identity proxy). Previously Rust emitted one member per split and diverged from Java's merged `planTasks` groups whenever a bin held ≥2 adjacent splits — the nightly interop D1 runner failure (the reported "5-of-8 offset subset" was a merged-span masquerade, not a coarser row-group grid). Rust API behavior change (fewer, larger members when adjacent splits co-bin); 7 merge unit tests + mutation-proven RED. **Merge INTEROP-PINNED 2026-07-24 (G3), both directions:** the merge landed with offline unit tests only — the oracle could always REPRESENT a merge divergence (member key = `(basename,start,length)`) but its fixture never REACHED the branch (measured: with `merge_tasks` removed the unfiltered comparison still passes at 14 groups). Two dedicated fixture files close it, each planned under an isolating metrics-prunable row filter (disjoint id ranges) at the DELETE-FREE append snapshot so its splits meet an EMPTY bin-packer with weights equal to their lengths: `merge.parquet` (2 row groups, whole file under the 4096 target ⇒ both splits co-bin ⇒ ONE spanning member) and `gap.parquet` (3 row groups, middle span alone over target ⇒ splits 0+2 co-bin NON-CONTIGUOUSLY and must NOT merge — adjacency, not group-by-file). Compared D1+D2 against Java's REAL `planTasks` (`java_merge_scan_plan.json`/`java_gap_scan_plan.json` ↔ `rust_merge_scan_plan.json`/`rust_gap_scan_plan.json`). The merge is proven to have FIRED by an exact assertion on the plan SHAPE on both sides — ONE group / ONE whole-file-spanning member for `merge.parquet`, TWO groups with the outer pair INTACT for `gap.parquet` — read against the offsets-aware-split invariant (the splitter emits exactly one sub-task PER SPLIT OFFSET and ignores the target), so `>=2` offsets ⇒ `>=2` splits ⇒ a single spanning member is only producible by the merge; the span/contiguity inequalities alongside are degenerate-fixture guards, not the discriminator. Sizing guards pin the packing preconditions so a fixture drift fails loudly. Three new fail-closed legs: a Java sabotage leg (unmerged split keys AND a too-small-to-co-bin target must both diverge) and TWO PRODUCTION-SOURCE mutation legs — deleting the `merge_tasks` call from `CombinedScanTask::new` reds the merge-filtered assertion, and dropping the contiguity clause from `FileScanTask::can_merge` (⇒ group-by-file) reds the gap-filtered assertion — each requiring a real ASSERTION signal (not merely a non-zero exit, which a non-compiling mutant would also give), then restoring md5-identically and re-verifying green. Zero production-code change in G3. Deferred (do NOT block): `DataTask` (inspect surface), `ContentScanTask<F>` not separately exposed; `planTaskGroups(groupingKeyType)` partition-aware overload. **READ-SIDE (2026-08-07, U3/hazard-1):** the row-group selection a split's `(start, length)` window drives is now Java-exact — `arrow/reader.rs::filter_row_groups_by_byte_range` keeps a row group iff its MIDPOINT (real footer offset via Java `getOffset` = `min(data_page_offset, dictionary_page_offset)`, plus `compressed_size / 2` truncating) lies in the HALF-OPEN `[start, start+length)`, per parquet-mr `ParquetMetadataConverter.filterFileMetaDataByMidpoint` (bytecode-decoded 1.16.0 ≡ 1.13.1), the filter `Parquet.ReadBuilder.split()` installs. Previously an OVERLAP rule over SYNTHESIZED `4 + Σ compressed_size` offsets: a straddling row group was decoded by BOTH adjacent sub-tasks (silent duplicate rows), and any non-contiguous file (padding / inline bloom filters — measured drift 4/686/1368 vs 4/542/1080) duplicated even for offsets-ALIGNED splits. Bidirectionally interop-proven ✅ via `tests/interop_ranged_read.rs` + `dev/java-interop/run-interop-ranged-read.sh` (hand-declared 800-byte tiling on both sides; D2 ships a bloom-PADDED file whose offset drift is asserted; fail-closed OVERLAP source mutation). Java's Regime-B `file_offset`+`invalidFileOffset` repair chain is deliberately NOT ported — it IS the synthetic model, and parquet-rs cannot decode a footer that reaches it without the `encryption` feature. NAMED residue: the merge's file-identity proxy is the data-file PATH where Java uses `DataFile` REFERENCE equality (`BaseFile`/`GenericDataFile` override neither `equals` nor `hashCode` — javap-confirmed); divergence is unreachable under the one-manifest-entry-per-live-path invariant, but that is an invariant, not a type-level guarantee (Java forecloses the class structurally by leaving whole-file `BaseFileScanTask` off `MergeableScanTask`). **NAMED divergence — split declines AVRO/ORC (2026-08-07, U3):** `FileScanTask::split` returns the whole-file task unchanged for AVRO and ORC, where Java splits them (bytecode-confirmed: `FileFormat` marks PARQUET/ORC/AVRO splittable, PUFFIN/METADATA not). Reason: this crate's Avro OCF and ORC readers materialize WHOLE files and never read `start`/`length`, so splitting handed every sub-task the entire file and an N-way split silently returned N copies of it (measured: a 500-row Avro OCF split four ways returned 2,000 rows). `is_splittable` still ports Java's `FileFormat` table faithfully — the gate is a SEPARATE `reader_honors_byte_range` predicate over the READ path, backed by `ArrowReader::reject_ranged_whole_file_task`, which fails a ranged AVRO/ORC task closed on any other route in. The cost is intra-file PARALLELISM only, never rows; closing it needs Avro block-range / ORC stripe-range reads, at which point flip `reader_honors_byte_range` and NEVER `is_splittable`. Second fork-local clause, same function: a task carrying the legacy whole-file sentinel `length == 0` (which every read path here spells as whole-file) passes through as ONE task, because the fixed-size walk would emit ZERO sub-tasks and drop the file from `plan_tasks` while `plan_files` still returned it — silent total row loss; Java cannot reach this since `BaseFileScanTask.length()` is always the file size. **Third fork-local clause, same function (2026-08-08, U3 cycle 5):** an ALREADY-RANGED parent (`start != 0`) passes through as ONE task too. Java forecloses re-splitting STRUCTURALLY — `BaseFileScanTask` implements `SplittableScanTask`, but the `SplitScanTask` its splitter emits does NOT — whereas this crate uses one `pub` type with `pub` fields and a derived `Deserialize` for both. Both real branches treat the byte space as absolute from zero, so re-splitting RELOCATED the window to offset 0: measured on a 3-row-group / 60-row file, a parent covering bytes 139..1185 (ids 20..59) came back as windows over 0..1046 and read ids 0..59 — bytes the parent never owned, with its own tail dropped. That is silent CORRUPTION, not the row loss the sentinel clause fixed. Cost: parallelism, on a shape no planner path produces. **Widened 2026-08-08 (U3 cycle 6)** to `start != 0` OR `length != file_size_in_bytes`: a PARTIAL parent is an already-ranged task too, and it reached the offsets-aware branch, which takes the manifest offsets verbatim and so ran its windows past the parent's end — offsets `[0, 300, 700]` on a parent owning bytes 0..500 of a 1000-byte file yielded windows over 0..700 plus a degenerate empty one. Java forecloses this half structurally as well: `BaseFileScanTask.length()` is always `file.fileSizeInBytes()`, so its only splittable shape spans the whole file. **Fourth, at the other split site (same date):** `TableScan::plan_tasks` suppresses splitting ENTIRELY when the scan projects `_pos`. The `_pos` read path decodes whole files with ordinals from 0 and rejects every ranged task with a typed `FeatureUnsupported`, so unconditional splitting manufactured exactly the shape the next layer refuses — a `_pos` scan that `to_arrow()` served correctly was a TOTAL OUTAGE on the `plan_tasks` / `PartitionWork` seam (measured: 0 rows and two typed errors), and the error told the caller to plan without splitting when the caller never chose to. `to_arrow`'s `expand_within_file_parallel_tasks` already suppressed itself the same way; Java has no analogue (`_pos` is an engine-side metadata column there, and its readers carry the row-group start ordinal into a split). **Hoisted to the PRIMITIVE 2026-08-08 (U3 cycle 6):** `FileScanTask::split` itself now returns the task unchanged when `project_field_ids` contains `RESERVED_FIELD_ID_POS`, because `split` is `pub` and reachable without either caller — measured on a 3-row-group / 60-row file projecting `_pos`, the whole-file task read 60 rows while a `split(391)` produced three sub-tasks that ALL failed typed, including the `start == 0` one. Both call-site guards are retained as defensive-but-redundant. [full narrative](archive/2026-06_matrix-cell-narratives.md#scantaskgroupt--scanplantasks-splitgrouping--splittablescantasksplit--batchscancontentscantaskfdatatask) |
| R149 · `ExpressionParser` JSON serialization (`toJson`/`fromJson`) | ✅ | `core/.../expressions/ExpressionParser.java` | `expr/expression_parser.rs` (2026-06-17) — canonical codec over `Predicate`: `to_json` emits the bytecode-confirmed wire shape (`{type,term,value}` / `{type,term,values}` / logical `{and,or,left,right}` / `{not,child}` / bare `true`/`false`), op hyphen-map (`is_null`→`is-null`…), and `SingleValueParser` value forms (int/long→JSON number, float/double→`Float`/`Double.toString` number-token `1.0E10`/`1.0E-4`, date/time/ts/tstz→ISO string with JDK fractional-trim, decimal→plain string, binary/fixed→UPPER hex, uuid lowercase); schema-aware `from_json(_, &Schema)` recovers the typed `Datum` from the bound field and REJECTS transform/aggregate terms (`FeatureUnsupported`); DEPTH-LIMITED recursion. Wired into `ScanReport.filter` (`metrics/mod.rs`) via custom serde; deser uses the schema-less untyped path matching Java `ScanReportParser`. **Interop ✅ BOTH directions 2026-06-17** (`tests/interop_expression.rs` + `run-interop-expression.sh` + `ExpressionParserOracle`): Rust `to_json` byte-matches Java REAL `ExpressionParser.toJson` AND Java `fromJson(rust_json,schema)`+bind+toJson byte-matches, over a 34-expr battery (every op + every value type incl. float/double + logical + always-true/false); set `in`/`not-in` compared set-semantically (unordered); anti-circular (Java builds the battery independently); 4-sabotage battery (op-hyphen + date-value + float-value corruption) fails closed. **Named residue (scoped OUT of byte-1:1):** float/double values where JDK 11 `FloatingDecimal` is NON-minimal (large-magnitude / long-mantissa, e.g. JDK 11 `6.0372357323402578E18` vs the shortest `6.037235732340258E18`; float quirks `9.9999999E14`/`1.00000003E16`) — Rust emits the minimal shortest-round-trip form (== JDK 19+), diverging only from the JDK 11 oracle; pinned by unit `float_residue_is_jdk_nonminimal`. Non-finite floats (`Infinity`/`NaN`) rejected. |
| R150 · `UnboundAggregate`/`BoundAggregate`/`AggregateEvaluator` (count/min/max) + `Unbound`/`BoundExtract` | ✅ | `api/.../expressions/{UnboundAggregate,BoundAggregate,AggregateEvaluator,UnboundExtract,BoundExtract}.java` | `expr/visitors/aggregate_evaluator.rs` (2026-06-17) — the aggregate tree (`UnboundAggregate{count_star/count/min/max}` → `bind` → `BoundAggregate`) + `AggregateEvaluator` that folds count/min/max from manifest `DataFile` METRICS with NO data scan, mirroring Java `CountStar`/`CountNonNull`/`MinAggregate`/`MaxAggregate`/`AggregateEvaluator` (`javap`-confirmed 1.10.0). **Fold (Java-exact):** `count(*)`=Σ`record_count`; `count(col)`=Σ(`value_count[id]`−`null_value_count[id]`, null default 0, checked sub/add on `u64`); `min`=min over `lower_bounds[id]`, `max`=max over `upper_bounds[id]` via typed `Datum::partial_cmp`. **Not-pushable invalidation (the staller, mirrors `NullSafeAggregator.update`):** a required metric missing for ANY file latches the aggregator invalid — `count(col)` needs BOTH `value_counts` AND `null_value_counts` to hold `id`; `min`/`max` need the bound present OR an all-null column (`value_count>0 && null_count==value_count`, the Java `allNull` short-circuit — no bound to contribute, stays valid); `all_valid()`=false ⇒ `results()` yields `None` so the engine falls back to a real scan, never a silently-wrong partial. 17 unit tests incl. all the cant-push cases; mutation-proven (drop the invalidation ⇒ 5 cant-push tests fail). **`Bound`/`UnboundExtract` CUT (frontier-parked variant-shredding):** the only aggregate term modelled is a column `Reference` — there is no `Extract` term to build, so a shredding path cannot enter the tree; a future `Extract` term bound through an aggregate must be rejected (`FeatureUnsupported`). **✅ 2026-06-19 (G1):** cross-impl CONFORMANCE interop vs Java 1.10.0 `AggregateEvaluator.result()` — both engines independently fold an identical hand-declared 3-file fixture over 10 aggregates (count(\*)/count/min/max) and agree, + an anti-circular hand-computed anchor + a not-pushable fixture (a dropped value-count latches `all_valid=false` on BOTH sides); mutation-proven non-vacuous (`dev/java-interop/run-interop-aggregate.sh` + `InteropOracle.AggregateOracle` + the in-crate env-gated `interop_aggregate_matches_java`). The fold is a PURE FUNCTION of metrics — no on-disk artifact to byte-round-trip; reading Java's manifest-metric `ByteBuffer`s is covered by the scan/inspection read-path interop. NAMED SEPARATE residue (NOT this evaluator row): scan-side aggregate PUSH-DOWN wiring (the query-planning consumer) is not yet built. |
| R151 · Catalog trait accessors: `name()`, `properties()`, `commitTransaction(List<TableCommit>)`, `invalidateTable`/`invalidateView` | 🟡 (4 accessors landed + unit-tested 2026-06-17; `commitTransaction` split out; local-value only, no interop) | `api/catalog/Catalog.java` (`name`/`invalidateTable` defaults), `api/catalog/ViewCatalog.java` (`invalidateView` default), `api/catalog/SupportsNamespaces.java`, `core/.../rest/RESTCatalog` (`properties`/`commitTransaction`) | `crates/iceberg/src/catalog/mod.rs` — four NON-BREAKING `Catalog`-trait default methods landed + unit-tested 2026-06-17: `name()` (=`UNNAMED_CATALOG` sentinel), `invalidate_table`/`invalidate_view` (no-op defaults), `properties()` (documented Rust-convenience, NOT a Java `Catalog`-interface method — javap confirms it is REST-only). `name()`/`properties()` overridden per impl from held config (REST/Glue/HMS/S3Tables/SQL/Memory); `#[automock]` auto-derives mocks; per-impl + bare-impl + mutation-guard tests, all offline. `commitTransaction(List<TableCommit>)` split out / deferred. Stays 🟡 — local-value accessors, no Java interop round-trip. [full narrative](archive/2026-06_matrix-cell-narratives.md#catalog-trait-accessors-name-properties-committransactionlisttablecommit-invalidatetableinvalidateview) |
| R152 · `RewriteFiles` DELETE-file rewrite surface (`addFile(DeleteFile)`, 4-arg `rewriteFiles`) | ✅ (2026-06-20 — bidirectional interop proven; seq semantics distinct + non-vacuous) | `api/RewriteFiles.java` (`addFile(DeleteFile)`/`addFile(DeleteFile,long)`, 4-arg `rewriteFiles`) | `transaction/rewrite_files.rs` `add_delete_file`/`add_delete_files` + the explicit-seq overload (Java `addFile(DeleteFile,long)`) + the 4-set `rewrite_files` (data/delete replace+add) (2026-06-14) — added deletes route through the shared `snapshot.rs` `write_added_delete_manifests` (REUSED from RowDelta, not forked); explicit seq stamped vs inherited per `BaseRewriteFiles` (javap: `pendingDeleteFile(file,null)` inherit vs `Long.valueOf(seq)` explicit); negative explicit seq REJECTED fail-loud (manifest-writer strip → resurrection). 10 unit tests, 10/10 mutations caught incl. seq-strip + 4-arg cross-wire resurrection (scan-level) + shared-helper-from-RowDelta; the 3 preconditions byte-match the Java strings. Interop PROVEN 2026-06-20: `tests/interop_rewrite_files_delete_surface.rs` — LOCKED FIXTURE (S1 append, S2 rowdelta-DF0, S3 rewrite+DF_inherited+DF_exp); D1 Rust reads Java table → inherited-del seq==3==S3-snap-seq, exp-del seq==2; D2 Rust writes, Java verify → same; non-vacuity: seq 3→99 sabotage correctly rejected. Seq semantics DISTINCT (inherited≠explicit). |
| R153 · `ActionsProvider` + `DeleteReachableFiles` + `ConvertEqualityDeleteFiles` actions; `SupportsNamespaces` partial property set/remove | ✅ (2026-06-17 — all components built; `ConvertEqualityDeleteFiles` is read-identity-proven offline + interop, G4 capstone) | `api/actions/{ActionsProvider,DeleteReachableFiles,ConvertEqualityDeleteFiles}.java`, `api/catalog/SupportsNamespaces.java` | `maintenance/{actions_provider,delete_reachable_files,convert_equality_delete_files}.rs` (2026-06-16/17) — `ActionsProvider` entry point (pub trait, 1:1 with Java's 12 default methods) + `DeleteReachableFiles` (DROP TABLE PURGE engine) + `ConvertEqualityDeleteFiles` (eq→pos, read-identity-proven offline + interop, G4 capstone) + `SupportsNamespaces` partial property set/remove. All components built. [full narrative](archive/2026-06_matrix-cell-narratives.md#actionsprovider--deletereachablefiles--convertequalitydeletefiles-actions-supportsnamespaces-partial-property-setremove) |
| R154 · `Literal.to(Type)` literal type promotion (`Datum::to`) | 🟡 (numeric/temporal/identity promotions + strict string/decimal accept-sets landed 2026-06-23/24; value-level →decimal + hex/nano residue) | `api/.../expressions/Literals.java` (`*Literal.to(Type)`) | `spec/values/datum.rs` `Datum::to` — additive 1:1 port of the Java `Literals` accept-set grounded in the verbatim source: int→float/double, long→float/double/date (int-range sentinel), float→double, double→float (±Float.MAX sentinel), decimal→decimal (returns self, scale unchanged), fixed↔binary (length-checked), string→uuid/date/time/timestamp[tz]. `StringLiteral.to` is now STRICT: string→{boolean,int,long} REJECT (no Java case ⇒ `default:null`; the over-permissive arms + their now-dead `string_to_i128`/`i128_to_i32` helpers were removed 2026-06-24), matching Java exactly. `DecimalLiteral.to` is likewise STRICT: decimal→long REJECTS (Java has only `case DECIMAL: return this` ⇒ `default:null`; the pre-existing over-permissive `Int128→Long` arm + its now-dead `i128_to_i64` helper + the orphaned `LONG_MAX`/`LONG_MIN` consts were removed 2026-06-24 — `Int128` is exclusively decimal-mantissa storage, so the arm was always Decimal→Long and additionally returned a wrong value for any non-zero scale by long-casting the raw mantissa). `Datum::to` conversion suite = 36 tests incl. at-exact-boundary sentinel + string-reject + decimal→long-reject + length-mismatch baits (mutation-proven). Pure in-memory conversion (no on-disk artifact ⇒ unit-tested vs the verbatim Java source, interop N/A). NAMED residue: every numeric value-level →decimal (rust_decimal 96-bit/scale-28 vs Java `BigDecimal` unbounded `setScale(_,HALF_UP)`); string→decimal (a fully ABSENT arm — Java `StringLiteral.to(DECIMAL)` is `new BigDecimal(str)` with no scale coerce; deferred with the hex-decode group, not for a scale-precision reason); timestamp[tz]→date + long→time (Java omits the range-check Rust constructors enforce); all `timestamp_nano` arms (parked per upstream PR 11775); string→fixed/binary hex decode. |
| R155 · `MetricsConfig` / `MetricsModes` column metrics modes | 🟡 (mode value type + property resolution landed 2026-06-24; **bound truncation now APPLIED in the Parquet writer 2026-06-25**; data-writer config plumbing + schema-dependent inference + Java interop are residue) | `core/.../MetricsModes.java`, `core/.../MetricsConfig.java`, `parquet/.../ParquetMetrics.java`, `api/.../util/{Unicode,Binary}Util.java` | `spec/metrics_config.rs` — `MetricsMode` (None/Counts/Truncate(N>0)/Full; `parse` mirrors `MetricsModes.fromString`; `Display` round-trips each Java `toString`) + `MetricsConfig::from_properties` (default `write.metadata.metrics.default` → `truncate(16)`; per-column `write.metadata.metrics.column.<name>`; invalid → `tracing::warn!`+fallback). **Bound truncation (2026-06-25):** `MetricsMode::truncate_lower_bound`/`truncate_upper_bound` port `ParquetMetrics.truncateLower/UpperBound` + `UnicodeUtil`/`BinaryUtil` (string=code-points, binary=bytes; max-variant increments + drops when un-incrementable; **only STRING/BINARY** truncate, matching Java's `switch` default); `parquet_writer.rs` applies per-column mode in `parquet_to_data_file_builder` (None→skip column entirely, Counts→counts no bounds, Truncate→truncated, Full→full) — `ParquetWriterBuilder::new` now defaults to `truncate(16)` == Java `MetricsConfig.getDefault()` (the on-disk flip: data files now write truncated bounds), `new_with_metrics_config` overrides. `MetricsConfig::for_position_delete` (path/pos→Full, Java `forPositionDelete`) wired into ALL production position-delete writers (datafusion `delete.rs` + maintenance `RewritePositionDeleteFiles`/`ConvertEqualityDeleteFiles`/`RewriteTablePath`) via the chainable `ParquetWriterBuilder::with_metrics_config`, so delete-file path pruning stays precise. 23 unit + 2 writer-level tests (mutation-proven: truncate-min/max prefixes, 0xFF/max-codepoint overflow→drop, surrogate-gap skip, None/Counts/Full end-to-end). NAMED residue: threading a table's *resolved* config through the DATA-file writer construction (data writers currently use the static `truncate(16)` default, not `from_properties(table.properties())` — so non-default `write.metadata.metrics.*` on data files is unhonored; `parquet_files_to_data_files` already derives it) · `forEqualityDelete` + eq-delete writer wiring (correctness-safe to omit: truncation only widens eq bounds) · wide-schema `max-inferred-column-defaults` cap + sorted-column auto-promotion (need schema/sort-order) · nested-column dotted-name override coverage · Java interop proving byte-level truncated-bound parity (keeps this 🟡 not ✅). |
| R156 · LOW-importance backlog (consolidated) | ❌ | various `api/`/`core/` | One row for the small-surface residue so the plan can see it without bloat: variant predicates + variant metrics; `ExpressionUtil` helpers; `CatalogProperties` name constants. Each is a narrow add; none gates a phase. Promote to its own row when work starts. `SnapshotUpdate.toBranch` promoted to row R168. |
| R157 · Commit-outcome taxonomy (`CommitStateUnknownException` semantics) | 🟡 (flipped ❌→🟡 2026-07-08) | `api/exceptions/CommitStateUnknownException.java`, `core/SnapshotProducer.java` (`commit()`: unknown outcome ⇒ NO file cleanup, NO retry, surfaced — 1.10.0 bytecode `onlyRetryOn(CommitFailedException.class)` + the dedicated `CommitStateUnknownException` rethrow ahead of the cleanup catch), `core/BaseTransaction.java`, `core/rest/ErrorHandlers.java` (commit handlers: 5xx ⇒ unknown), `aws/glue/GlueTableOperations.java` (`doCommit` reconciliation ⇒ `CommitStateUnknownException`) | `error.rs` `ErrorKind::CommitStateUnknown` + `Transaction::commit`'s retry gate refuses the KIND regardless of the `retryable` flag (Java decides by CLASS, never a flag); no cleanup on the unknown branch (the commit path deletes nothing on failure — pinned so none is introduced). Sent-vs-unsent classification per catalog: REST `query_catalog_for_commit` (post-send transport ⇒ unknown; connect/build ⇒ terminal as before) + 500/502/503/504 ⇒ unknown per `ErrorHandlers$CommitErrorHandler` (503 arm added) + 200-with-lost-response ⇒ unknown, tables AND views; SQL `from_sqlx_commit_error` (`Io`/`Protocol`/`WorkerCrashed` post-send ⇒ unknown; CAS conflict stays retryable; the previously-DISCARDED SQL-transaction `COMMIT` error now propagates for all SqlCatalog statements); Glue + S3 Tables `SdkError` dispatch classification (io/timeout dispatch, operation timeout, response-error ⇒ unknown; construction + user/other dispatch ⇒ terminal) + modeled service errors (`InternalService`/`OperationTimeout`/`InternalServerError` ⇒ unknown; `ConcurrentModification`/`Conflict` stay retryable). Reconciliation-by-refresh PORTED 2026-07-09 (`transaction/commit_status.rs` + `Transaction::reconcile_unknown_commit_outcome`, Java `BaseMetastoreOperations.checkCommitStatus[Strict]`): on an unknown outcome the library re-reads the catalog with the `commit.status-check.*` knobs (Java names/defaults; `retry(n)` = n+1 attempts, 2.0-factor backoff, total-timeout stop) and searches the reloaded snapshot SET for the attempted snapshot ids — landed (even buried under a concurrent third-party commit) ⇒ commit resolves `Ok`, no re-apply; absent-after-refresh ⇒ the ORIGINAL unknown surfaces (Java 1.10.0 production non-strict semantics: strict-FAILURE ⇒ UNKNOWN, bytecode offsets 11-34 — never a retryable failure, a pending in-flight request could still land); still-unreadable after the budget ⇒ unknown surfaces. Crown-jewel mock test (`transaction/mod.rs`) proves reconciled-success / no-retry / no-double-commit / no-cleanup on a durably-landed-but-unacknowledged commit against a real in-memory catalog; gate + classifiers + reconciliation (7 mutations: re-apply-on-landed, pointer-only, absent-as-landed, unbounded, reconcile-always, never-reconcile, default+name drift) mutation-proven both directions. RESIDUE (why 🟡): real-catalog conformance is the credentialed slice (todo queue item 6); metadata-only commits (no `AddSnapshot`) carry no snapshot evidence at the catalog-agnostic seam and are NOT reconciled (Java's metadata-location check covers them — the location is catalog-assigned and unknown to this seam); Java's `COMMON_INFO` message text not mirrored verbatim (semantics carried by the kind + messages). S3 Tables service-side maintenance commits concurrently with any writer, raising the ambient ambiguity rate. Engine-side guidance: [../ENGINE_CONTRACT.md](../ENGINE_CONTRACT.md) §8. |
| R158 · Staged create/replace table transaction (`Catalog.newCreateTableTransaction` / `newReplaceTableTransaction`) | ✅ (MemoryCatalog unit-proven 2026-07-14; Java 1.10.0 interop battery BOTH directions 2026-07-16 — `dev/java-interop/run-interop-staged-txn.sh` + `tests/interop_staged_txn.rs`) | `api/catalog/Catalog.java` (`newCreateTableTransaction` / `newReplaceTableTransaction`), `core/BaseTransaction.java` | `transaction/staged_table.rs` — `StagedTableTransaction::{begin_create,begin_replace,add_data_files,commit}` + `Transaction::apply_locally` + Catalog `publish_create_table` (default=`register_table`) / `publish_replace_table` (MemoryCatalog CAS under one lock). Writes + metadata land on FileIO first; one catalog pointer publish. Failure before/during publish: create leaves `table_exists=false`; replace leaves original current. Replace builds ON TOP OF the existing metadata (Java `TableMetadata.buildReplacement`): retains table UUID + snapshot history + metadata log (appended, not truncated), resets the `main` ref (no current snapshot), applies the new schema/spec/sort-order/props/location as current. Replace-schema field-ids are taken from the caller AS-IS; `last_column_id` advances monotonically (never reduced). Format version is PRESERVED unless a `format-version` PROPERTY directs an upgrade (absent⇒keep; higher⇒upgrade; lower/unparsable⇒`DataInvalid`; `creation.format_version` ignored — the builder V2 default would silently upgrade V1) (D4). Named residue: (1) replace-publish does NOT read-validate the staged metadata before the pointer swap (create-publish does) — safe today because `begin_replace` writes through the catalog's own FileIO, a defense-in-depth gap for real-catalog wiring; (2) staged replace restarts metadata-file versioning at v0/v1 rather than continuing monotonically from the existing table's version (to be derived when real catalogs are wired); (3) replace-schema ids are caller-provided, NOT Java's name-matching `TypeUtil.assignFreshIds` reassignment against the base — a base-aware fresh-id helper is the follow-up (D5). Bidirectional Java 1.10.0 interop proven 2026-07-16 (`dev/java-interop/run-interop-staged-txn.sh`, `StagedTxnOracle` ⇄ `tests/interop_staged_txn.rs`): create + replace + format-version scenarios, the replace invariant set E-INV(1-7) asserted per cycle in BOTH directions, a V1 table surviving a replace as V1 on both sides, structural cross-check, and a 3-step fail-closed sabotage battery. Engine recipe: [ENGINE_CONTRACT.md](../ENGINE_CONTRACT.md) §8a. |
| R159 · REST OAuth2 automatic token refresh (`token-refresh-enabled`) | 🟡 (flipped ❌→🟡 2026-07-18; offline mockito pins, live long-session soak is credential/docker-gated) | `core/.../rest/auth/OAuth2Manager.java` (`keepRefreshed`), `core/.../rest/auth/RefreshingAuthManager.java`, `core/.../rest/auth/OAuth2Util.java` (`OAuth2Util$AuthSession.scheduleTokenRefresh` / `refreshCurrentToken`), `core/.../rest/auth/OAuth2Properties.java` (`TOKEN_REFRESH_ENABLED_DEFAULT=true`, `MAX_REFRESH_WINDOW_MILLIS=300000`, `MIN_REFRESH_WAIT_MILLIS=10`) | `catalog/rest/src/client.rs` now refreshes proactively via **lazy refresh-before-use** (sanctioned adaptation of Java's background scheduler): `TokenState { token, refresh_at }` derives the refresh instant with Java's exact `scheduleTokenRefresh` arithmetic — `refreshWindow = min(ttl/10, 5min)`, `wait = max(ttl − refreshWindow, 10ms)` (bytecode-decoded 2026-07-18). `authenticate` refreshes when the cached token has entered its window/expired, so no request carries a known-expired token; single-flight `refresh_gate` collapses a concurrent stampede to ONE token-endpoint exchange; refresh reuses `get_token_endpoint()`/`client_credentials` exactly (no new endpoint or credential-POST location). Knob `token-refresh-enabled` (catalog.rs accessor, Java default true) — when false, legacy exchange-once-cache-forever is preserved exactly. Missing `expires_in` ⇒ no refresh scheduled (Java parity); refresh failure is suppressed and the prior token kept (Java `suppressFailureWhenFinished`), initial-acquisition failure propagates. Pins (mockito, offline): single-flight-under-8-concurrent, fresh-token-no-refresh, disabled-zero-traffic, missing-`expires_in`, refresh-failure-keeps-old, init-failure-propagates, schedule-rule numbers; 5 mutations proven RED both directions. **RESIDUE (why 🟡):** (1) Java refreshes a still-valid token with the **token-exchange** grant (`subject_token`), only re-fetching with the credential once expired; this fork always uses `client_credentials` re-exchange — observably equivalent for credential clients, wire grant differs; (2) **token-only clients (no credential) are not proactively refreshed** (no token-exchange machinery) — Java can keep them alive pre-expiry; (3) live multi-hour session soak against a real OAuth server is credential/docker-gated, not in the offline gate. DEVIATIONS documented in `client.rs` module header; closing (1)/(2) needs the token-exchange grant (separate item). |
| R160 · Vended storage credentials (`LoadTableResult.storage-credentials`) | 🟡 (impl + offline behavioral pins 2026-07-18; ✅ gated on live-cloud credentialed interop) | `core/.../rest/RESTSessionCatalog.java` (`newFileIO(SessionContext, Map, List<Credential>)` → `CatalogUtil.loadFileIO`), `core/.../aws/s3/S3FileIO.java` (`clientForStoragePath` longest-`startsWith` select + `clientByPrefix` `buildKeepingLast` overlay), `core/.../rest/credentials/Credential.java`, `core/.../rest/responses/LoadTableResponse.java` (`credentials()` accessor; the wire-key constant is `STORAGE_CREDENTIALS` — screaming-snake — in `LoadTableResponseParser`, holding the JSON field `storage-credentials`) | WIRED 2026-07-18: `catalog/rest/src/catalog.rs` `select_vended_credential` picks the longest vended `prefix` that prefixes the table storage path (`metadata_location`) and `load_file_io` overlays that credential's config onto the `FileIO` props LAST (vended wins on key collision). The SELECTION RULE (longest-prefix, strictly-longer replacement, vended-wins overlay, silent no-match) mirrors Java `newFileIO`/`S3FileIO.clientForStoragePath`; the GRANULARITY is adapted (2026-07-18): Java retains the full `List<Credential>` inside `S3FileIO` and re-selects per accessed file path (every `newInputFile`/`newOutputFile`/`deleteFile` re-runs `clientForStoragePath`), whereas the fork selects ONCE at the table's `metadata_location` and overlays that single credential into the table's static per-table `FileIO` props. No-match ⇒ silent skip (Java's base-client fallback, no error); zero-credentials path unchanged. Secrets land in `StorageConfig`, whose redacted `Debug` already covers the vended keys (`s3.access-key-id`→`key`, `s3.secret-access-key`→`secret`, `s3.session-token`→`token`; needle list unchanged) — composition pinned. Offline pins in `catalog.rs` (selection longest-prefix, overlay+collision winner, no-match skip, zero-cred regression, redaction composition), each RED under invert-selection / swap-overlay / drop-wiring mutations. Residues: 🟡 not ✅ because ✅ needs a live-cloud credentialed round-trip (real vended STS creds honored by object storage) unrunnable offline; vended-credential expiry/refresh (Java `VendedCredentialsProvider`) not built — the OpenDAL `FileIO` takes a static props snapshot; fork does not enforce Java `Credential.validate` (non-empty prefix/config). Granularity divergence (2026-07-18): (a) multi-prefix tables (data bucket ≠ metadata bucket) receive the `metadata_location` credential for ALL accesses where Java selects per-path; (b) a credential whose prefix covers only the data path and not `metadata_location` is dropped entirely (fork applies none; Java would use it for data reads) — per-path selection is not expressible in the OpenDAL flat-props `FileIO` without core storage changes. Bytecode-verified 2026-07-17/18. |
| R163 · Arrow `timestamptz` timezone annotation (`UTC` vs `+00:00`) | 🟡 (unit-proven 2026-08-14; Spark `toArrow` convention) | Spark `toArrow` / Java-family `timestamp[us, tz=UTC]`; Iceberg-core has no Arrow schema | `arrow/schema.rs` `UTC_TIME_ZONE` = `"UTC"` (was `"+00:00"`). Iceberg→Arrow (`schema_to_arrow_schema`, inspect builders, `get_arrow_datum`, ORC/Avro readers) now emit `Timestamp(_, "UTC")`. Arrow→Iceberg (`arrow_schema_to_schema` / `arrow_schema_to_schema_auto_assign_ids`) still accepts BOTH `"UTC"` and `"+00:00"` via `is_utc_time_zone` — narrowing is out of scope. Values/instants unchanged. Parquet write funnel still relabels the UTC-alias pair metadata-only (F-A2-3). Stays 🟡: no Spark `toArrow` interop in this lane (`make interop` OUT). |
| R164 · DataFusion `IcebergCatalogProvider` namespace-scoped snapshot walk | ✅ (unit-proven 2026-08-14; lazy `list_tables` 2026-08-15) | n/a (DataFusion construction option; not an `iceberg-core` type) | `crates/integrations/datafusion/src/catalog.rs` `try_new_with_namespace_scope` — additive constructor; existing `try_new` signature unchanged and still full-catalog namespace BFS. Named namespace(s) PLUS descendants (same #191 nested BFS + U+001F naming). Empty list walks nothing. `CatalogProvider` trait unchanged. Per-namespace `list_tables` is lazy (`IcebergSchemaProvider`: construction issues zero listings; first successful access caches forever; a failed listing is not cached — `table()`/register/deregister surface it, `table_names`/`table_exist` return empty/false). Pins: scoped walk never issues `list_namespaces(None)` and never lists sibling tables; unscoped still walks namespaces; empty scope issues zero listings; construction issues zero `list_tables`. See [ENGINE_CONTRACT](../ENGINE_CONTRACT.md) §1. |
| R169 · DataFusion metadata-table scan projection | ✅ (unit-proven 2026-09-01; interop N/A) | n/a (DataFusion `TableProvider::scan`; not an iceberg-core type) | `crates/integrations/datafusion/src/table/metadata_table.rs` + `physical_plan/metadata_scan.rs` — `scan` honors `projection`: subset, reorder, and empty (SELECT count(*)) emit the projected schema and keep the full row count. `TableProvider::schema` stays the full schema. Pins in `metadata_table.rs`. Spark projects metadata tables in the engine; iceberg-core has no DataFusion provider. |
| R170 · DataFusion `table_names` catalog-parity listing | ✅ (unit-proven 2026-09-01; interop N/A) | Spark `SHOW TABLES` lists catalog entries only; metadata twins are `$`-name resolved, not enumerated | `crates/integrations/datafusion/src/schema.rs` `IcebergSchemaProvider::table_names` returns catalog names only. `$`-name `table` / `table_exist` / last-`$` + `MetadataTableType` vocabulary are unchanged. Pins in `schema.rs` and `tests/integration_datafusion_test.rs`. |
| R165 · `Partitioning` / `PartitionUtil.coercePartition` | 🟡 (unit + shape-pinned 2026-08-16, conductor-16 A–D #202–#205; no interop round-trip yet) | `api/Partitioning.java` (`partitionType` / `groupingKeyType` / `unionPartitionTypes` / `isPartitioned`) + `api/util/PartitionUtil.java` (`coercePartition`) | `spec/partitioning.rs` — the cross-spec unifier as a first-class surface: `partition_type` (field-id-ascending union with G1 unknown-transform refusal and G2 loud `DataInvalid` on same-id conflicts), `grouping_key_type`, `union_partition_types` (`pub(crate)`, dropped-source tolerant — G4 turns a hard error into correct output), `is_partitioned`, `coerce_partition` (required-field-missing refusal), plus `TableMetadata::unified_partition_type`; `maintenance::unified_partition_type` is a deprecated thin alias. Adopted by every inspect surface: `partitions` (#203), `position_deletes` schema (#204), `files`/`entries`/`all_*` projection (#205 — `append_partition`'s PT-0 field-id walk unchanged, correct by construction against the unified type). Named residue: `rewrite_position_delete_files.rs` groups by `(spec_id, partition)` — strictly FINER than Java's `groupingKeyType` grouping; conservative, never wrong, only a missed compaction opportunity (accepted-finer, not adopted). Flips ✅ on a bidirectional interop round-trip per the matrix legend. |
| R161 · Partition-path rendering (`PartitionSpec.partitionToPath`): URL escaping + human string | 🟡 (escaping Java-exact + interop-proven 2026-07-25; FIXED/BINARY base64 CLOSED 2026-07-31; temporal/year-month-hour/float-double human-string residue remains) | `api/.../PartitionSpec.java` (`partitionToPath` → `escape` = `java.net.URLEncoder.encode(s, "UTF-8")`; both the field NAME and `Transform.toHumanString` go through it), `core/.../SnapshotSummary.java` (`Builder.updatePartitions` keys `changed-partition` summaries by the same path, prefix `partitions.`), `api/.../transforms/TransformUtil.java` (the human strings) | `spec/partition.rs` `escape_partition_path_component` + `escaped_partition_pair` — the single seam every `name=value` pair is built through (`partition_to_path`, `try_partition_to_path`, and all three `name=null` fallbacks), so no branch can skip it — and each of the FOUR call sites is pinned INDIVIDUALLY, unescaping any one alone reds a distinct test (Java authority for the `name=null` branches: `Transform.toHumanString(Type, T)` returns the literal `"null"` unconditionally, before it switches on the type — 1.10.0 bytecode offsets 0-6). Java's escaper is `application/x-www-form-urlencoded`, NOT RFC-3986: `A-Z a-z 0-9 - _ . *` pass through, a space becomes `+`, everything else is one UPPERCASE `%XX` per UTF-8 byte (byte-wise iteration is equivalent to Java's UTF-16 loop — every safe character is single-byte ASCII; Java's unpaired-surrogate `?` case is unreachable from a Rust `str`). The `=` inside a pair and the `/` between pairs stay raw. **FORMAT-VISIBLE, approved (D6, 2026-07-25):** data-file LOCATIONS and `changed-partition` summary KEYS change for partition names/values holding characters outside the safe set; safe-set values are BYTE-IDENTICAL to pre-change output (two no-churn regressions, both RED under an over-eager escaper). Blast radius is not confined to odd strings, and byte-stability is a property of the rendered OUTPUT type, never of the transform's name. ALWAYS-MOVING (every value, because the human string holds a `:` and, for four of them, a space): `identity` over `timestamp`, `timestamptz`, `time`, `timestamp_ns` and `timestamptz_ns` — FIVE fork-supported types, measured and pinned (`the_five_always_moving_temporal_types_move_for_every_value`). ESCAPER-SENSITIVE (moves exactly when the rendered value leaves the safe set): `identity` AND `truncate` over `string` — `Transform::result_type` returns the SOURCE type for `truncate`, so `truncate(string, N)` renders a string and an ordinary space in a truncated prefix is enough; the live battery's own `truncate_string` case is Java's `s_trunc=a%2Fb+c` (in-tree pin `truncate_is_byte_stable_except_over_string`); `unknown` belongs in this bucket too — its result type is `string` on BOTH sides (Java `UnknownTransform.getResultType` → `Types.StringType`; fork `Transform::result_type`), so it escapes exactly like `identity(string)`. BYTE-STABLE: `bucket` (→ int), `day` (→ date), `year`/`month`/`hour` (→ int), `void` (→ `null`), `truncate` over int/long/decimal (binary/fixed human strings are now base64 — see CLOSED 2026-07-31 below), and `identity` over date/uuid/decimal/bool/int/long/float/double; `identity`/`truncate` over binary/fixed are base64 human strings (Java-exact since 2026-07-31) — escaper-stable iff the base64 alphabet stays inside the safe set (e.g. `YS9i`), escaper-moving when it holds `+`/`/`/`=`. Tables written pre-fix keep their raw paths and stay readable — readers resolve every data file from the manifest's recorded `file_path`, never by re-deriving the partition directory. Pre-fix a `/` in a value forged extra directory levels (`a b=x/y/c/d=5` for a 2-field spec) and an unmatched summary key — and, more seriously, DESTROYED INJECTIVITY of tuple → directory: two DISTINCT tuples of ONE spec could render ONE path (`(1/b=2, 3)` and `(1, 2/b=3)` both → `a=1/b=2/b=3`), as could tuples under two specs of one evolving table (1-field `(1/b=2)` and 2-field `(1, 2)` both → `a=1/b=2`), silently merging two partitions' data files into one directory AND their `partitions.<path>` summary entries into one key (per-partition record counts summed). Escaping restores injectivity — pinned by `two_distinct_tuples_can_no_longer_collide_on_one_directory`. Pins (14 in `partition_path_escaping_tests` + 1 in `snapshot_summary.rs`): a 95-character printable-ASCII sweep, a 31-row jar-oracle value table (incl. 2/3/4-byte UTF-8), an 8-row field-name table, ALL THREE `name=null` branches separately (the lenient fallback, the `void`-past-end branch, the NULL literal), structure-vs-content, space-vs-`+` distinctness, the temporal blast-radius pin, the truncate output-type pin, the injectivity pin, and the two no-churn regressions; 9 mutations (no-escaping / value-only / lowercase-hex / alphanumeric-only / space-as-`%20` / plus ONE PER SITE for the four `escaped_partition_pair` call sites) each caught by a distinct minimal pin set — the per-site legs exist because a simultaneous mutation of all `name=null` branches would prove only that at least one is covered. INTEROP PROVEN 2026-07-25 — `dev/java-interop/run-interop-partition-path.sh` + `InteropOracle$PartitionPathOracle` ⇄ `tests/interop_partition_path.rs`: 22 named cases byte-match Java's own `partitionToPath`, built independently on each side (anti-circular), with case-id set-equality and a two-leg fail-closed sabotage (un-escape one expectation; drop one case) that HARD-FAILS if a corruption cannot be applied. `partitionToPath` is a PURE function with no on-disk artifact, so cross-impl conformance is the strongest available evidence. **NAMED RESIDUE (why 🟡 not ✅) — the HUMAN STRING, measured in-tree 2026-07-25 against the same jar oracle, orthogonal to the escaping and NOT fixed here:** `identity(timestamp)` renders `2017-11-16 22:31:08` (chrono's space separator → escaped `+`) where Java renders ISO `2017-11-16T22:31:08`; `identity(timestamptz)` renders `2017-11-16 22:31:08 UTC` where Java renders `2017-11-16T22:31:08+00:00`; the V3 nanosecond pair (`timestamp_ns` / `timestamptz_ns`) diverges IDENTICALLY (JVM-measured: Java renders the same two ISO strings for them); CLOSED 2026-07-31 (QC, D8): EVERY transform whose OUTPUT type is `binary` or `fixed` now renders standard Base64 via `Datum::to_human_string` / `TransformUtil.base64encode` parity (`java.util.Base64.getEncoder()`, NOT URL-safe) — `identity(binary)`, `identity(fixed)` AND `truncate(binary)` all emit `YS9i` for the bytes `61 2F 62` (pinned `identity_binary_and_fixed_render_java_base64` + flipped `truncate_is_byte_stable_except_over_string`); base64 alphabet chars outside the URLEncoder safe set (`+`, `/`, `=`) are then escaped (e.g. `0xFB 0xFF` → `+/8=` → `%2B%2F8%3D`); `Display for Datum` still renders UPPERCASE hex for binary/fixed (orthogonal surface); `identity(float)`/`identity(double)` render Rust's `Display` where Java renders `Float`/`Double.toString` — `1` vs `1.0`, `10000000000` vs `1.0E10`, `-0` vs `-0.0`, `inf` vs `Infinity` (both sides measured 2026-07-25; `1.5` and `NaN` agree) — and every character of either form stays inside the safe set, so the ESCAPER never moves them and their BYTE-STABLE classification above is unaffected; `year`/`month`/`hour` render the raw ordinal (`55` / `625` / `456789`) where Java's `TransformUtil` renders `2025` / `2022-02` / `2022-02-09-21`. Already matching: `day` (`2022-01-08`), `date` and `time` identity, string/int identity, `bucket`, and `truncate` over int/long/decimal/string/binary (base64 closed 2026-07-31) — and `identity(time)` is a divergence R161 CLOSES (pre-fix the fork emitted the raw `22:31:08` against Java's escaped `22%3A31%3A08`; both now agree). The interop battery is confined to the matching set, so a failure there is an escaping divergence. All four timestamp divergences (micro + nano) remain pinned as an alarm (`assert_ne!` against Java's own form — `the_five_always_moving_temporal_types_move_for_every_value`); the binary base64 ALARM was flipped to `assert_eq!` when QC closed it 2026-07-31. Remaining OPEN human-string residue (still needs its own approval to close): temporal ISO vs chrono space/UTC, `year`/`month`/`hour` ordinal vs `TransformUtil` calendar forms, and `identity(float)`/`identity(double)` Rust `Display` vs Java `Float`/`Double.toString` (`1` vs `1.0`, `1.0E10` vs `10000000000`, `-0` vs `-0.0`, `inf` vs `Infinity` — both sides measured; `1.5` and `NaN` agree; every character of either form stays inside the safe set so the ESCAPER never moves them). `Transform::Unknown` is named in the ESCAPER-SENSITIVE bucket (result type `string` on both sides). FORMAT-STABILITY (QC base64): newly-written partition directory segments for binary/fixed-typed values change from UPPERCASE hex to standard Base64 (then URL-escaped); existing tables stay readable — readers resolve every data file from the manifest's recorded `file_path`, never by re-deriving the partition directory. |
| R162 · Timestamptz / timestamptz_ns in `data_file` metadata projection | 🟡 (unit-proven 2026-08-14; inspection interop pending) | `core/.../BaseFilesTable` / `PartitionsTable` via `DataFile.getType(partitionType)` | `inspect/data_file.rs` `append_partition_field`: `Timestamptz` → `TimestampMicrosecondBuilder` + `extract_i64`; `TimestamptzNs` → `TimestampNanosecondBuilder` + `extract_i64` (mirrors `readable_metrics.rs`). Schema conversion (`type_to_arrow_type`) already emitted timestamptz children; the refuse was the append match (`partition field type {other} is not supported in the data_file metadata projection`). Manifest Avro `Long` decode (`spec/values/serde.rs`) now accepts `timestamp_ns` / `timestamptz_ns` the same way it already accepted `timestamp` / `timestamptz` so a V3 identity partition can be reloaded before projection. `.files` / `.partitions` project identity-partition values; `Uuid` and `Fixed` stay `FeatureUnsupported` (negative-pinned). Writer/commit path unchanged. Stays 🟡 pending Java inspection interop over a timestamptz-identity-partitioned table. |
| R166 · V3 row lineage (`_row_id` / `_last_updated_sequence_number`, `first_row_id` inheritance) | ✅ | `core/.../ManifestReader` (`idAssigner` + `ManifestReader$1`), `core/.../avro/ValueReaders` (`$RowIdReader`, `$LastUpdatedSeqReader`, `fileFieldReader`) | WRITE side was already complete: `next-row-id` in `TableMetadata`, snapshot row ranges, and `ManifestListWriter::assign_first_row_id`. Two READ-side gaps closed 2026-08-24 (F-13 V1+V2). **V1 — `DataFile::first_row_id` inheritance** (`spec/manifest/entry.rs` `assign_first_row_ids`, wired into `ManifestFile::load_manifest_with_schema_fallback`, the seam every IN-TREE reader funnels through — scan via `io/object_cache.rs`, `inspect/`, `maintenance/`. NOTE the public `Manifest::parse_avro` bypasses it, so an external consumer reading manifests directly gets un-inherited ids; Java has the same shape, its rule living only in `ManifestReader`): the fork passed the stored value through, so every file read back carried `None`. Java's absent arm OVERWRITES to null; the present arm skips DELETED and already-assigned entries WITHOUT advancing the counter. **V2 — scan materialization** (`arrow/record_batch_transformer.rs` `ColumnSource::RowId` / `RowIdFromFile` / `LastUpdatedSeqFromFile`, fed by `FileScanTask::first_row_id` / `file_sequence_number`): `_row_id` = stored value, else `first_row_id + physical ordinal`; `_last_updated_sequence_number` = stored value, else the file's sequence number. Implemented at the SHARED transformer rather than per-format, so Parquet and Avro get identical semantics from one rule — which required BOTH readers to let the two reserved ids through: the Parquet projection (`is_row_lineage_field`) and `build_expected_schema` (Avro/ORC). Exempting only one left the other silently discarding a stored id. `_row_id` inherits `_pos`'s whole-file in-order decode requirement, its ranged-split refusal, AND its split SUPPRESSION. Only `FileScanTask::split` branch 1c is load-bearing there — it returns the whole-file task, so the two `scan/mod.rs` guards are DEFENSIVE RESTATEMENTS (removing either leaves the suite green), matching the pattern that file already documents for its other conjuncts. Without branch 1c the planner hands the reader ranged sub-tasks it refuses one by one: a total outage, not a wrong value. **BREAKING (two items):** (a) `FileScanTask` (public, not `#[non_exhaustive]`) gains two fields, so downstream struct literals must be updated; (b) `get_metadata_field` (public) now returns `_row_id` and `_last_updated_sequence_number` as OPTIONAL rather than required — a Java-parity fix, but a downstream that built a non-nullable Arrow field from either sees an observable nullability change. Two deliberate divergences, both fail-closed where Java's `long` wraps: the `i64` narrowing at assignment and the `i64` addition at materialization. Missing inputs yield a NULL column, matching Java exactly: `ValueReaders.rowIds(null, …)` is `constant(null)`, and `lastUpdated` returns `constant(null)` if EITHER `rowIdConst` or `fileSeq` is null — so a V1/V2 file reports NULL for both columns rather than its sequence number. Both reserved fields are OPTIONAL (Java `MetadataColumns` offsets 144/159); the fork had them REQUIRED, which made the null column unrepresentable. A stored `_row_id` in the file WINS over the computed one, which required letting the two reserved ids through the Parquet projection and the leaf expansion, and forcing the transformer's `Modify` path (the schema fast paths hand a column back verbatim, nulls included). **RESIDUE:** (1) ORC DOES get the arm — it shares `build_expected_schema` with Avro — but only its ABSENT-column half is verified; a STORED `_row_id` in an ORC file is UNTESTED because no in-repo ORC writer stamps `iceberg.id`, and Java's ORC reader is outside `iceberg-core` so that arm has no oracle either; (2) projecting `_row_id` under a ranged split is REFUSED, not supported, for the same ordinal reason as `_pos` — the planner suppresses the split so this is unreachable through `plan_tasks`, but remains reachable through the public `PartitionWork` / direct-reader seam; (3) no interop leg yet proves Java reads fork-assigned row ids. A DELETE manifest never lends row ids: `load_manifest` passes `None` for `Deletes` content. That is Java's behaviour reached the way Java reaches it — `ManifestFiles.readDeleteManifest` uses the 5-arg `ManifestReader` constructor, which hardcodes a null `firstRowId`, so the range never arrives. Java's 6-arg constructor does carry a "First row ID is not valid for delete manifests" precondition, but NO call site can trip it, so REFUSING such a manifest would reject a table Java scans fine — a fail-closed where Java is fail-open. The fork briefly did exactly that. Decoded bytecode + the why-both-readers-need-the-exemption narrative: [task/f13-v3-row-lineage-ledger.md](../../task/f13-v3-row-lineage-ledger.md). **INTEROP LEG LANDED 2026-08-25 — residue (3) CLOSED, row flipped 🟡→✅.** `dev/java-interop/run-interop-row-lineage.sh` + `tests/interop_row_lineage.rs`, both directions over a V3 table built across TWO snapshots so the counter must advance WITHIN a manifest (a=0, then b=5 by a's record count) and ACROSS snapshots (manifest 2 at 8, `next_row_id` 12). D1: Java writes, and the fork's view — built through `ManifestFile::load_manifest`, the `assign_first_row_ids` seam — equals the view Java's own `ManifestReader.idAssigner` resolved. D2: the fork commits the equivalent table and Java reads it back with the same view builder, reporting 0 failures and no null `first_row_id`. A THIRD test closes D2's circularity: D2 renders ONE table twice, so a wrong-but-consistent writer would pass it, and `rust_assigns_the_same_row_ids_java_does` compares the two INDEPENDENTLY produced views with file names stripped — proving the fork ASSIGNS the ids Java assigns, not merely that Java can parse them. **MATERIALIZATION is covered separately, in both directions**, because the manifest view cannot reach it: inheritance is metadata, but `_row_id` is resolved PER ROW by the reader. `rust_materializes_java_row_ids` and `java_materializes_rust_row_ids` scan with `_row_id` + `_last_updated_sequence_number` projected — Java through `IcebergGenerics.project(MetadataColumns.schemaWithRowLineage(..))` — and diff per row (ids 0..11, `last_updated_seq` 1 for the first snapshot's files and 2 for the second's). Mutation-proved RED at base: dropping the read-side inheritance call (every file reads back null), a read-side counter advancing by 0 (file b takes 0, not 5), a write-side range advancing by 0 (file c takes 0, `next_row_id` 0), and every row in a file sharing ONE `_row_id` — a table-corruption-class defect that the manifest view alone could NOT see and that only the materialization tests catch. **What this leg did NOT pin, measured not assumed:** the `!= Deleted` skip guard SURVIVED its mutation (`== Added` stayed green), so that arm was pinned by the unit domain table in `spec::manifest::entry::first_row_id_tests` and not by interop. F-7 U1 CLOSED that gap and corrected the reason — see the F-7 U1 paragraph below. **Residues (1) and (2) remain open and are NOT covered by this leg**: the ORC stored-`_row_id` arm is untested, and it has no oracle because **Java's ORC reader does not implement row lineage at all** — `iceberg-orc` 1.10.0 contains zero row-lineage classes, verified against the jar, so there is nothing to compare against rather than a reference that is merely out of scope. The STORED-`_row_id`-wins arm is likewise unit-pinned on EVERY format, not only ORC: no writer inside the parity envelope emits a stored `_row_id` column — Java's `GenericAppenderFactory` does not — so no fixture either side can build reaches it, and collapsing `RowIdFromFile` to `RowId` survives this leg while four lib tests catch it. The ranged-split refusal is still reachable through the public `PartitionWork` seam. One scope note on what D2 pins: `_row_id` is COMPUTED by each reader from `(first_row_id, position)`, so the two sides share the input and not the rule, and that input is pinned independently by the assignment cross-check — but `last_updated_seq` is ECHOED from fork-written metadata by both readers, so D2 pins the READING rule, not the fork's sequence-number ASSIGNMENT, a separate long-standing capability with its own rows. The ✅ rests on the strict bar being met for the capability's Parquet/Avro path — unit tests AND a bidirectional interop round-trip, both halves of the row — with these two named as residue, the same shape as every other fork-flipped ✅ row here. It does NOT rest on the legend's named-unproven-slice allowance, which is written for BASE-INHERITED rows and does not apply to a fork-flipped one. The write-side `existing_rows_count` arm, which no test reached until this unit, is now pinned by `assigned_range_advances_by_existing_rows_not_only_added`. **F-7 U1 (2026-08-25) — two production changes and the leg that found the second.** (1) ADD-SEAM SUPPRESSION: `SnapshotProducer::new` now takes a required `FirstRowIdPolicy`. `Suppress` clears the `first_row_id` of every added data file, mirroring Java `MergingSnapshotProducer.add(DataFile)` calling `Delegates.suppressFirstRowId` (1.10.0 bytecode, offset 73; `Delegates$1.firstRowId()` is `aconst_null; areturn`). `FastAppend` and `BaseRewriteManifests` extend Java `SnapshotProducer` and never suppress, so `append.rs` and `rewrite_manifests.rs` pass `Preserve`; `MergeAppend`, `OverwriteFiles`, `ReplacePartitions`, `RewriteFiles`, `RowDelta`, `DeleteFiles` and `CherryPickOperation` all extend `MergingSnapshotProducer` and pass `Suppress`. The argument is required rather than defaulted so a new producer cannot inherit the wrong half by omission. `DeleteFiles` and `RewriteManifests` hand the producer no data file, so the rule is vacuous there and their mutations stay green — recorded, not counted. Java suppresses DATA files only: `add(DeleteFile)` wraps with `Delegates.pendingDeleteFile` and leaves the field alone, and the fork's delete path is untouched. (2) MANIFEST-LIST ORDER (BEHAVIOR CHANGE, found by the new interop leg): the fork emitted the carried-forward manifests BEFORE the newly written ones. Java emits new first — `MergingSnapshotProducer.apply` builds `concat(prepareNewDataManifests(), filteredExistingData)` then the same for deletes, and `FastAppend.apply` builds `writeNewManifests()` then `snapshot.allManifests()`. The V3 manifest-list writer assigns each unassigned DATA manifest its range IN LIST ORDER, so the old order gave a newly added file a row id Java does not give it: measured on a rewrite that replaces a 5-row file, the fork assigned the new file 15 where Java assigns 12. Self-consistent within one writer, so no table read back wrong — but two engines writing the same operations disagreed on row identity, which is the one thing row lineage exists to fix. `manifest_file()` now partitions the processed existing manifests by content and emits added-data, existing-data, added-delete, existing-delete. The rule has THREE conjuncts and each is pinned separately, because only the first of them shows up cross-engine: added-data before existing-data (2 red out of 10 interop tests when broken); the existing DATA manifests keeping their source-list order (1 red out of 10 interop, and 1 red out of 3477 lib tests — it bites as soon as TWO carried-forward data manifests still need a range, which is why the `upgraded` fixture takes two V2 appends). The discriminator that actually fires under a swap is the `manifests` array's `added_files` asymmetry — the rewritten manifest carries `added_files: 0`, the carried-forward one `added_files: 1`, and `strip_names` never touches that array. The four distinct record counts (`f=3, g=2, i=4, h=1`) are defence in depth for the `files` half, NOT the operative guard: setting `i` to match `g` on both sides leaves the swap RED anyway, measured); and the data group preceding the delete group (1 red out of 3477 lib tests; no interop leg can see it because neither row-lineage fixture carries a delete manifest). **Divergence found while building this fixture, and NOT ported (see row R107):** `h` is 1 row because Java REFUSES the commit otherwise — `SnapshotProducer.apply` (offsets 324-364) asserts `added-records <= deleted-records` on a `replace` operation, `Preconditions.checkArgument("Invalid REPLACE operation: %s added records > %s replaced records")`. The fork has no such check and commits it; verified by writing a 5-row replacement for a 3-row file, which landed as a `replace` snapshot that GREW the table. The constraint is `SnapshotProducer.apply`'s, not `BaseRewriteFiles`'s. The data/delete split is `MergingSnapshotProducer.apply`'s shape ALONE — Java `FastAppend.apply` and `BaseRewriteManifests.apply` append the prior list un-split — and the fork applies it to all nine producers. That difference is unreachable: only a merging producer can introduce a delete manifest and it appends its delete group last, so every list either engine writes is already data-then-deletes and the split is a no-op there. **INTEROP EXTENDED (F-7 U1):** the main fixture grew a `RewriteFiles` and an `OverwriteFiles` commit, so a survivor crosses two rewrites as an `Existing` entry and keeps its `first_row_id` (5) and its per-row `_row_id`; and a SECOND `upgraded` fixture — V2, two files, upgrade to V3, then a rewrite as the FIRST V3 commit — runs both directions with its own cross-check. **The `== Added` versus `!= Deleted` mutation named above is NO LONGER unpinned by interop: it is RED on the upgraded legs** (4 red out of 10 interop tests). The MEASURED reason the four-snapshot fixture alone still leaves it green corrects the earlier claim: it is not that the fixture lacks `Existing` entries — it now has them — but that a rewrite reads its source through the assigning reader on BOTH sides, so every survivor it writes forward already carries a stored id and the `is_some()` clause short-circuits the status test. Only a manifest whose own range is absent produces a live `Existing` entry with no stored id, and a V2-to-V3 upgrade is the only way either implementation reaches that. RESIDUE (F-7 U1): Java `MergingSnapshotProducer.add(ManifestFile)` carries `Preconditions.checkArgument(manifest.firstRowId() == null, "Cannot append manifest with assigned first_row_id: %s")` on its `canInheritSnapshotId() and null snapshotId` branch. It is NOT ported, because the fork has no merging-producer manifest-append surface to port it onto: the only `add_manifest` in the tree is `RewriteManifestsAction::add_manifest`, and Java's `BaseRewriteManifests.addManifest` carries no such check (its `copyManifest` passes `manifest.firstRowId()` straight through to `ManifestFiles.copyRewriteManifest`). A related hazard is NAMED rather than fixed: that surface accepts an externally supplied manifest, and `ManifestListWriter::assign_first_row_id`'s already-assigned arm keeps such a range WITHOUT advancing `next_row_id`, so two manifests could claim overlapping ranges. No in-tree caller reaches it. **F-7 slice 1 (2026-08-30):** format-v3 `RewriteDataFiles` and DataFusion COW `OverwriteFiles` persist stored `_row_id` / `_last_updated_sequence_number` (Java `SparkRewriteTable.rewriteSchema` / `SparkCopyOnWriteOperation.requiredMetadataAttributes`). Pins: `rewrite_data_files_lineage_tests`, `row_lineage_cow.rs`, compact D2 of `run-interop-row-lineage.sh`. |

> **Citing rows (2026-07-01).** Every capability row carries a **permanent anchor** in its first
> cell — `R<id>`, e.g. `R117`. Cite rows as **"row R117"**, never by file line number: line
> numbers shift when ANY line is inserted above (the +2 drift that broke ~45 citations between
> 2026-06-17 and 2026-07-01 came from two PROSE lines added above the table — prose edits are not
> safe either). Anchor IDs equal the row's file line number at stamping
> time (2026-07-01), never change, and are never reused; a NEW row takes the next unused ID
> (R158 was next at stamping) and may be inserted anywhere in the table. Enforced by
> `scripts/check_matrix_anchors.sh` (`make check-matrix-anchors`, in CI): every data row anchored,
> IDs unique, every `row R<n>` citation in the live docs resolves, and the 5-pipe row audit.
> The checker assumes this file contains exactly this ONE table — adding a second table needs a
> checker update first. Bare-number citations in dated archives / superseded narratives reference
> historical line-numbering epochs and are deliberately left as-is.

## Headline gaps (ranked by effort × value)

> **Superseded as a ranking (2026-07-01):** the live ranked queue is
> [task/todo.md](../../task/todo.md) §"ACTIVE (2026-07-01)" — one home for PRIORITY. This section
> is retained as per-gap NARRATIVE detail only; several items below record capabilities that have
> since landed (statuses live in the matrix rows above).

1. **Write engine** — everything beyond fast-append (`RowDelta`, `RewriteManifests`, merge append).
   **Started 2026-06-07: `DeleteFiles` 🟡 + `OverwriteFiles` 🟡 + `ReplacePartitions` 🟡 + `RewriteFiles` 🟡.**
   `DeleteFiles` delete-by-path/reference + the foundational manifest-filter / rewrite machinery in
   `SnapshotProducer::process_deletes`; `OverwriteFiles` composes that delete path with the fast-append
   add path in one `Overwrite` snapshot, reusing the now-shared `SnapshotProducer::{resolve_delete_paths,
   current_manifests}` (factored out of `DeleteFiles`; `current_manifests` carries DATA **and** DELETE
   manifests forward — Increment 2b 2026-06-10 — so outstanding MoR deletes survive every delete-bearing
   commit); `ReplacePartitions` (dynamic partition
   overwrite) adds a by-PARTITION sibling resolver `SnapshotProducer::resolve_partition_deletes` that feeds
   the SAME `process_deletes` rewrite path (every partition an added file belongs to is replaced; an
   unpartitioned table is a full replace; `replace-partitions=true` summary marker); `RewriteFiles` (the
   compaction-commit primitive) replaces a set of DATA files with a new set in one `Replace` snapshot,
   reusing the by-path `resolve_delete_paths` + `process_deletes` machinery unchanged (Java
   `BaseRewriteFiles`: `operation()` = `REPLACE`, delete set must be non-empty, `failMissingDeletePaths`).
   All four `MemoryCatalog`-tested (post-commit SCAN live set + cross-partition isolation + provenance +
   compaction-to-fewer-files). Deferred: `overwriteByRowFilter` / delete-by-row-filter / static
   `replaceByRowFilter` (metrics evaluators), DELETE-file rewrite + `dataSequenceNumber` preservation
   (delete-file write path), concurrent-commit conflict validation (serializable isolation), and
   data-level Java interop.
2. **Schema/partition evolution + snapshot management** — `UpdateSchema`, `UpdatePartitionSpec`, **and the
   `ManageSnapshots` ref-operation surface** are now **✅** (bidirectional Java interop round-trips landed
   2026-06-07 via the `dev/java-interop/` oracle + the `crates/iceberg/tests/interop_update_schema.rs`,
   `interop_update_partition_spec.rs`, and `interop_manage_snapshots.rs` Direction-1 tests; one
   `generate`/`verify` pass covers all THREE capabilities — 7 schema + 7 partition + 7 manage-snapshots
   scenarios, both directions, all 21 PASS). The `UpdatePartitionSpec` interop surfaced (and fixed,
   in-scope) a real Rust↔Java divergence: the partition-name↔schema collision check was identity-only and
   rejected the legitimate V1 void replacement; relaxed to identity-OR-void (source-id-gated) to mirror
   Java's bind path. `UpdateSchema` column initial/write **defaults** are plumbed and
   `Schema::check_compatibility` (in `TableMetadataBuilder::add_schema`) now mirrors Java
   `Schema.checkCompatibility` in FULL — both the **V3-only initial-default guard** (rejects a non-null
   `initial_default` below v3) AND the **V3-only type gate** (rejects a `timestamp_ns`/`timestamptz_ns`
   field, incl. nested, below v3). For `ManageSnapshots`, both the **"Snapshot model + refs"** row and the
   **ref-operation surface** of the **"Snapshot management"** row are now ✅; **`cherrypick` landed 2026-06-11**
   (`transaction/cherry_pick.rs` — the WAP publish commit replaying data files via `SnapshotProducer`) and is
   **🟡 unit-proven, interop deferred** — it is a standalone action, not part of the `manage_snapshots`
   ref-op surface. The manage-snapshots interop surfaced a
   read divergence the Rust V2/V3 snapshot reader required `sequence-number`, while the spec
   (`format/spec.md` lines 1979 & 2002) and Java's `SnapshotParser` both default an absent
   `sequence-number` to 0 on read; **FIXED 2026-06-07** with `#[serde(default)]` on
   `_serde::SnapshotV2`/`SnapshotV3.sequence_number`, so Rust now reads the seq-0 V1→V2-upgrade-carryover
   table class Java emits. The sibling spec-mandated default-to-0 read fields on the **Avro manifest /
   manifest-list path** — manifest-list `content`/`sequence-number`/`min-sequence-number`; manifest-entry
   `sequence_number`/`file_sequence_number`; data-file `content` — were **VERIFIED already-correct and pinned
   2026-06-07** (per-field empirical proof + mutation-verified regression tests; see the "Manifest +
   manifest-list read/write" row). The table-metadata **`last-sequence-number`** sibling is CLOSED (Arc G
   2026-06-11): Rust's strict-required-on-V2/V3 read ALREADY MATCHES Java 1.10.0 — bytecode shows
   `TableMetadataParser.fromJson` reads it via `JsonUtil.getLong` (THROWS when absent) for `formatVersion>1`
   and hard-defaults to 0 for V1; spec line 179 explicitly permits the V2 throw. The proposed
   `#[serde(default)]` "fix" would have DIVERGED (accepting V2 metadata Java rejects); strict is correct and
   is now pinned.
3. **Format & type breadth** — ORC + Avro data files; remaining V3 types (variant, geo, unknown). The
   V3-only **type** gate (Java `Schema.MIN_FORMAT_VERSIONS`: `timestamp_ns`/`variant`/`unknown`/`geometry`/
   `geography` require v3) is now **enforced for the representable types** (`timestamp_ns`/`timestamptz_ns`,
   and `variant` since F1 2026-06-11) in `Schema::check_compatibility`; `unknown`/`geometry`/`geography`
   get a one-line `min_format_version` arm each when those types land (tracked in `task/todo.md`).
   (`timestamp_ns` and column default values already landed in the 0.8/0.9 base — see the matrix.)
4. **Views in catalogs** (`ViewCatalog` + view operations).
5. **Maintenance actions** (expire/orphan/compaction/rewrite-deletes/compute-stats/migrate).
6. **Encryption** (`EncryptionManager`, KMS, encrypted FileIO/manifests).
