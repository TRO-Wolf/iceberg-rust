<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# map.md — crates/iceberg/tests/

## Purpose

Integration tests of the `iceberg` crate — currently the **Rust side of the Java interop
round-trips**. Each file loads committed fixtures from `crates/iceberg/testdata/interop/`
(Java-generated by [dev/java-interop/](../../../dev/java-interop/map.md)), replays the same
operations through the public transaction/scan API against a real `MemoryCatalog` commit, and
asserts structural equality with Java's result. All run **offline** in plain `cargo test`; under
`ICEBERG_INTEROP_GEN=1` they additionally write `rust_evolved` outputs for the Java `verify` pass
(Direction 2).

## Contents

| File | Proves | Status |
|---|---|---|
| `interop_update_schema.rs` | `UpdateSchema` — 7 scenarios incl. level-order nested ids, defaults, V3 guards | ✅ both directions |
| `interop_update_partition_spec.rs` | `UpdatePartitionSpec` — 7 scenarios incl. V1 void replacement, id recycling | ✅ both directions |
| `interop_manage_snapshots.rs` | `ManageSnapshots` ref operations — 7 scenarios | ✅ both directions |
| `interop_scan_exec.rs` | data-level scan execution: Java-written parquet + position/equality deletes + partitioned MoR read by Rust, and the reverse | landed 2026-06-09 |
| `interop_dv_scan.rs` | D1 deletion-vector scan execution: Rust scans a Java-1.10.0-written **V3** table whose merge-on-read deletes are a real Puffin `deletion-vector-v1` blob (`BaseDVFileWriter`); sibling raw-blob decode pin (>2^32 positions + run containers) lives in `delete_vector::tests` (env-gated lib test, same `ICEBERG_INTEROP_DV_DIR`) | landed 2026-06-10 (Direction 1; Direction 2 with the DV writer) |
| `interop_dv_write.rs` | D2 deletion-vector WRITING (Direction 2, blob-level): Rust `DVFileWriter` writes a real Puffin DV file (3 referenced files; position 0, a 5000-run, >2^32, a dense-gap key, the array/run size-tie set {0,1,2}) for Java's production reader to verify, then asserts the Rust blobs are BYTE-IDENTICAL to Java's own serialization (env `ICEBERG_INTEROP_DV_WRITE_DIR`; phases driven by `run-interop-dv.sh` steps 4-6) | landed 2026-06-10 |
| `interop_dv_table.rs` | D4 deletion-vector TABLE level (Direction 2, the headline) + METADATA level: the GEN test COMMITS a complete V3 table (2 identity(category) partitions, 2 real parquet data files fast_appended, ONE puffin with TWO DVs via `DVFileWriter` + `row_delta`) for Java's PRODUCTION scan (`IcebergGenerics`) + manifest-API DeleteFile cross-check; the meta test asserts Rust's canonical snapshot-metadata views of BOTH sides' DV chains equal Java's own (`added-dvs`, operation, manifest/seq semantics — the E1-family 3-way) (env `ICEBERG_INTEROP_DV_TABLE_DIR`; phases driven by `run-interop-dv.sh` steps 7-11) | landed 2026-06-10 |
| `interop_dv_replace.rs` | Arc-E Inc 2 deletion-vector REPLACEMENT (Direction 2, table + metadata + blob): the GEN test commits the writer-merged replacement chain (fast_append + DV1{1} + `DVFileWriter::with_previous_deletes` merged DV2{1,3} + `remove_deletes`) for Java's PRODUCTION scan (`DvReplaceOracle`: merged DV applied, DV1 ABSENT from manifests); the meta test asserts the canonical views match all 3 ways (the FIRST LIVE `removed-dvs`); the byte test asserts the merged blob is byte-identical to Java's same merge (the Run-store tie pin) (env `ICEBERG_INTEROP_DV_REPLACE_DIR`; phases driven by `run-interop-dv.sh` steps 12-16) | landed 2026-06-10 |
| `interop_rowdelta_meta.rs` | E1 metadata-level row-delta interop: the canonical snapshot-metadata view over the 3 scan-exec fixtures (env-gated) | landed 2026-06-10 |
| `interop_write_actions_meta.rs` | E2 + Increment-4 metadata-level write-actions interop: ONE eight-step chain (delete/overwrite/replace-partitions/rewrite/rewrite-manifests/merge-append) + a delete-bearing seq-preserving `rewrite_files` fixture B, all via the canonical view (env-gated) | landed 2026-06-10 |
| `interop_cherrypick.rs` | S2 cherrypick metadata-level interop: three fixtures (ff / replay / dedup) in both directions; `snapshot_meta_view` canonical views byte-equal on both sides | landed 2026-06-11 |
| `interop_staged_wap.rs` | Z1 staged-WAP interop: three fixtures (S-ff / S-replay / S-dedup) × 2 views (staged-but-unpublished + post-publish) in both directions, BOTH sides using REAL `stage_only()` / `stageOnly()` staging APIs. GEN test (`ICEBERG_INTEROP_STAGED_WAP_GEN_DIR`) lands `rust_staged_table` + `rust_final_table`; compare test (`ICEBERG_INTEROP_STAGED_WAP_DIR`) asserts Rust's `snapshot_meta_view` of Java-produced staged + final tables equals `java_staged_meta.json` / `java_final_meta.json`. Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_multi_spec.rs` | Z2 multi-spec interop: SINGLE FIXTURE, BOTH DIRECTIONS. Chain: ms1 fast_append F1(spec0) → ms2 spec-evolve add identity(b) → ms3 fast_append F2(spec1) → ms4 ONE multi-spec fast_append (F0 spec0 + F3 spec1). Proves: per-spec manifest grouping, the single multi-spec commit is constructible cross-language, partition tuples rendered under each manifest's OWN spec (file's-own-spec rule), and the W3 spec-id tiebreaker at sort position 10 is the ONLY disambiguator for the two tie-shaped ms4 manifests (F0.record_count == F3.record_count == 10). GEN test (`ICEBERG_INTEROP_MULTI_SPEC_GEN_DIR`) writes the chain to `rust_table/`; compare tests (`ICEBERG_INTEROP_MULTI_SPEC_DIR`) assert Rust's canonical view of BOTH chains equals `java_meta.json`. Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_multispec_merge.rs` | AC·OO #5 multi-spec MERGING-ACTION interop (the DELETE-side sibling of Z2 — GAP_MATRIX row 94 residue "multi-spec delete commits"): SINGLE FIXTURE, BOTH DIRECTIONS. Chain: ms1 fast_append F0(spec0) → ms2 spec-evolve add identity(b) → ms3 fast_append F3(spec1) → ms4 ONE multi-spec `row_delta` (D0 posDelete spec0 + D1 posDelete spec1). Proves the per-spec DELETE-manifest grouping Z2 never reached: `SnapshotProducer::write_added_delete_manifests`' `group_delete_files_by_spec` produces TWO DELETE manifests with `partition_spec_id` 0 AND 1 in ONE row_delta snapshot (op `delete`, the 1.10.0 two-branch rule), mirroring Java `MergingSnapshotProducer.newDeleteFilesAsManifests` → `newDeleteFilesBySpec` (bytecode-verified); the W3 spec-id tiebreaker (sort position 10) is the ONLY disambiguator for the two tie-shaped ms4 DELETE manifests (both rc=1); DELETE-entry partition tuples rendered under each manifest's OWN spec. ONE data manifest per spec before ms4 (deliberate) keeps Java's order-dependent DATA-manifest force-merge dormant so the unit isolates the DELETE grouping. GEN test (`ICEBERG_INTEROP_MULTISPEC_MERGE_GEN_DIR`) writes the chain to `rust_table/`; compare tests (`ICEBERG_INTEROP_MULTISPEC_MERGE_DIR`) assert Rust's canonical view of BOTH chains equals `java_meta.json`. Java D1 judgment + 4-sabotage battery (truncate-manifest / drop-ms4 / control / wrong-spec-rendering SB4) via `dev/java-interop/run-interop-multispec-merge.sh` + `MultiSpecMergeOracle`. Env-gated no-ops for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX row 94) |
| `interop_partition_stats.rs` | Z3/R2/R3 partition-stats file interop: 13 TESTS, BOTH DIRECTIONS + cross-version projection + incremental SUBTRACT arm + uuid/time/fixed/binary exotic types. **Z3 base (GEN `ICEBERG_INTEROP_PARTITION_STATS_GEN_DIR` + D2 `ICEBERG_INTEROP_PARTITION_STATS_DIR`):** V2 `identity(category)`, S1 fast-append (cat=a 3rec + cat=b 2rec), S2 row-delta pos-delete (cat=a 1rec); GEN writes `rust_table/metadata/final.metadata.json` + `expected_stats.json`, D2 reads Java parquet vs `java_stats.json`. Cross-version test: Java V2 stats (12 cols, no `dv_count`) read against V3 schema → `project_struct_type_to_batch` null-fills `dv_count`→0. **Incremental (`*_INCR_DIR`):** GEN+D2 over the SUBTRACT arm. **UUID (`*_UUID_DIR`, R2):** `identity(partition_id uuid)`, 16-BE-byte `FixedSizeBinary(16)`. **Time/fixed/binary (`*_TIME_DIR`/`*_FIXED_DIR`/`*_BINARY_DIR`, R3):** `identity(partition_time time)` (45296789012 micros, decodes to `PrimitiveLiteral::Long`), `identity(partition_fixed fixed[4])` (0xdeadbeef → `PrimitiveLiteral::Binary` len 4), `identity(partition_binary binary)` (0x0102030405 → `PrimitiveLiteral::Binary`); each GEN writes `<type>_expected.json`, each D2 reads Java parquet vs `java_<type>_stats.json`; hand-declared anti-circular constants. Java D1 judgment via `{verify}-interop-partition-stats[-<mode>]` in `run-interop-partition-stats.sh`. Sabotage: 4 corruptions (incl. 7b now fail-closed). Env-gated no-ops for offline `cargo test`. | landed 2026-06-12, R3 2026-06-13 |
| `interop_theta.rs` | I1 theta-blob puffin interop: unpartitioned V2 `{id long, name string, val long}`, File A (5 rows exact mode) + File B (1M rows estimation mode, Alpha lgK12/seed9001 ndv=1004032). GEN test (`ICEBERG_INTEROP_THETA_GEN_DIR`): real 2-file table → `ComputeTableStats::execute` → `rust_stats.puffin` + `rust_stats_expected.json` for Java `PuffinReader`+`CompactSketch.wrap(...).getEstimate()`. D2 test (`ICEBERG_INTEROP_THETA_DIR`): reads `java_stats.puffin` (Java `PuffinWriter`+datasketches-java-3.3.0 Alpha) via production `PuffinReader`+`CompactThetaSketch::deserialize`, integer-exact ndv both modes. Java D1 judgment via `verify-interop-theta` in `run-interop-theta.sh`. Sabotage: 4 (truncate Rust puffin; SEMANTIC theta-halve via footer-parsed offset → estimate doubles, ndv cross-check catches; truncate Java puffin; corrupt ndv JSON). Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_wap_data.rs` | I3 data-level WAP interop: V2 `identity(category)` table, 8-row fixture (base 4 + bump 1 + staged 3), REPLAY shape, BOTH DIRECTIONS. GEN test (`ICEBERG_INTEROP_WAP_DATA_GEN_DIR`): production `stage_only()` + DataFileWriter → `rust_table/metadata/final.metadata.json` + `rust_staged_snapshot_id.json` for Java `manageSnapshots().cherrypick()`. D2 test (`ICEBERG_INTEROP_WAP_DATA_DIR`): reads `java_cherrypick_table` (Java `stageOnly()` + cherry-pick) via production `CherryPickAction` + `to_arrow()`, pins 8 exact rows (anti-circular ground-truth) + partition routing + `published-wap-id`/`source-snapshot-id`. Java D1 judgment via `verify-interop-wap-data` in `run-interop-wap-data.sh`. Sabotage: 4 (truncate metadata; bogus manifest-list path; SEMANTIC `wap.id` corruption → wrong `published-wap-id`; remove staged snapshot). Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_view.rs` | I2 view metadata interop: SINGLE FIXTURE `{id long, name string}` with 2 SQL versions (V1: spark/`id>0`, V2: spark/`id>100`). GEN test (`ICEBERG_INTEROP_VIEW_GEN_DIR`): `MemoryCatalog` create + `ReplaceViewVersionAction` → 2 versions, writes `rust_view_metadata.json` (via `ViewMetadata::write_to`) + `rust_view_expected.json`. D2 test (`ICEBERG_INTEROP_VIEW_DIR`): reads `java_view_metadata.json` (Java `InMemoryCatalog.buildView create + replace`, `ViewMetadataParser.toJson`) via `ViewMetadata::read_from`, asserts all fields vs `java_view_expected.json`. Tolerance test (`test_view_tolerance_controls`, always runs): Java-ordered JSON (view-uuid first, no `properties`) parses on Rust side; Rust round-trip always emits `properties`. Wire-format field-order divergence pinned cosmetic (next-wave byte-exact item). Java D1 judgment via `verify-interop-view` in `run-interop-view.sh`. Sabotage: 5 cases (6a alter Rust SQL→Java D1 FAIL; 6b drop default-namespace→Rust parse FAIL; 6c dangling current-version-id=99→Rust FAIL; 6d alter Java SQL→Rust assert FAIL; 6e tolerance PASS). | landed 2026-06-12 |
| `interop_overwrite_conflict.rs` | C1 conflict-validation interop: `OverwriteFiles.validate_no_conflicting_data()` + `conflict_detection_filter` vs Java `BaseOverwriteFiles.validateAddedDataFiles` — proves the conflict DECISION (ACCEPT/REJECT), not surviving rows. Unpartitioned V2 `{id long, y long}`, S0 base + S1 concurrent add (real parquet ⇒ `y` bounds); 3 scenarios (`ge50_overlap`→REJECT, `ge50_excluded`→ACCEPT, `nofilter_any`/`alwaysTrue`→REJECT), hand-declared anti-circular. GEN test (`ICEBERG_INTEROP_OVERWRITE_CONFLICT_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks Rust's own decision; D1 test (`ICEBERG_INTEROP_OVERWRITE_CONFLICT_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric overwrite (REJECT = non-retryable `DataInvalid`). Java D2 judgment via `verify-interop-overwrite-conflict` in `run-interop-overwrite-conflict.sh`. Sabotage: semantic-swap (no-conflict table where REJECT expected) + structural-truncate, both fail-closed. Env-gated no-ops for offline `cargo test`. | landed 2026-06-15 (GAP_MATRIX row 91) |
| `interop_replace_partitions_conflict.rs` | C4 conflict-validation interop (2nd slice after C1): `ReplacePartitions.validate_no_conflicting_data()` vs Java `BaseReplacePartitions`'s PARTITION-SCOPED conflict validation (`file_in_replaced_partition`, NO `conflict_detection_filter` / NO `case_sensitive` — `javap`-confirmed) — proves the conflict DECISION (ACCEPT/REJECT), not surviving rows. PARTITIONED `identity(category)` V2 `{id long, category string}`, S0 base cat=a+cat=b + S1 concurrent add into ONE partition; 2 scenarios (`replaced_partition` concurrent→cat=a→REJECT, `other_partition` concurrent→cat=b→ACCEPT), hand-declared anti-circular. GEN test (`ICEBERG_INTEROP_REPLACE_PARTITIONS_CONFLICT_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks Rust's own decision; D1 test (`ICEBERG_INTEROP_REPLACE_PARTITIONS_CONFLICT_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric replace (fresh cat=a file + `validate_from_snapshot(S0)` + `validate_no_conflicting_data`; REJECT = non-retryable `DataInvalid`, message contains "conflicting files"). Java D2 judgment via `verify-interop-replace-partitions-conflict` in `run-interop-replace-partitions-conflict.sh`. Sabotage: semantic-swap (no-conflict table where REJECT expected) + structural-truncate, both fail-closed. Env-gated no-ops for offline `cargo test`. | landed 2026-06-15 (GAP_MATRIX row 92) |
| `interop_rowdelta_conflict.rs` | C3 conflict-validation interop (3rd slice, the RICHEST write-action conflict unit — GAP_MATRIX row 94): RowDelta's THREE conflict axes vs Java `BaseRowDelta` — proves the conflict DECISION (ACCEPT/REJECT), not surviving rows. Unpartitioned V2 `{id long, y long}`; 6 scenarios (two per axis, reject+accept), hand-declared anti-circular. Axis 1 `validate_no_conflicting_data_files()` + `conflict_detection_filter(y>=50)` (concurrent fast_append DATA, the C1 metrics shape — `data_conflict_{reject(y[60,70])/accept(y[10,20])}`); axis 2 `validate_no_conflicting_delete_files()` + filter (concurrent row_delta eq-delete keyed on `y`, real parquet ⇒ `y` bounds — `delete_conflict_{reject(y[60,70])/accept(y[10,20])}`); axis 3 `validate_data_files_exist([path])` (concurrent OVERWRITE removed the referenced file, `{OVERWRITE}` default op set ⇒ rejects WITHOUT `validate_deleted_files()` — `files_exist_{reject/accept}`, shared S0 f0+f1 / S1 overwrite-removes-f0-adds-f2 history; the referenced path is DERIVED from the table via live@S0 vs live@S1, not hardcoded). GEN test (`ICEBERG_INTEROP_ROWDELTA_CONFLICT_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks Rust's own decision; D1 test (`ICEBERG_INTEROP_ROWDELTA_CONFLICT_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric row delta (REJECT = non-retryable `DataInvalid`). Java D2 judgment via `verify-interop-rowdelta-conflict` in `run-interop-rowdelta-conflict.sh`. Sabotage (residue-stripped, pinned per C4 lesson): semantic-swap (accept table where REJECT expected, pinned to the outcome-mismatch line) + structural-truncate (pinned to the load/parse-error line), both fail-closed. Env-gated no-ops for offline `cargo test`. | landed 2026-06-15 (GAP_MATRIX row 94) |
| `interop_deletefiles_conflict.rs` | C2 conflict-validation interop (the SIMPLEST write-action conflict unit — GAP_MATRIX row 93): DeleteFiles' SINGLE axis `validate_files_exist()` (no filter) vs Java `StreamingDelete.validateFilesExist` → `ManifestFilterManager.failMissingDeletePaths` — proves the conflict DECISION (ACCEPT/REJECT), not surviving rows. SAME concurrent-removal shape as C3's files-exist axis, via DeleteFiles instead of RowDelta. Unpartitioned V2 `{id long, y long}`; 2 scenarios sharing one history (S0 append f0+f1; S1 overwrite removes f0 adds f2), hand-declared anti-circular: `same_file_reject` (targets the REMOVED f0 → REJECT) + `different_file_accept` (targets the SURVIVING f1 → ACCEPT); the targeted path is DERIVED from the table via live@S0 vs live@S1 (not hardcoded). GEN test (`ICEBERG_INTEROP_DELETEFILES_CONFLICT_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks Rust's own decision; D1 test (`ICEBERG_INTEROP_DELETEFILES_CONFLICT_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric `delete_files(<derived path>).validate_from_snapshot(S0).validate_files_exist()` (REJECT = non-retryable `DataInvalid` "Cannot commit, missing data files"). Java D2 judgment via `verify-interop-deletefiles-conflict` in `run-interop-deletefiles-conflict.sh` (Java `StreamingDelete` has no `validateFromSnapshot` — its loaded head IS S1 — yet the decision matches). Sabotage (residue-stripped, pinned per C4/C3 lesson): semantic-rollback (`same_file_reject` head rolled back to S0 so f0 is live ⇒ ACCEPT, pinned to the outcome-mismatch line — table-swap is vacuous since both scenarios share one history) + structural-truncate (pinned to the load/parse-error line), both fail-closed. Env-gated no-ops for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX row 93) |
| `interop_rewritefiles_conflict.rs` | C5 conflict-validation interop (the MECHANICALLY most complex write-action conflict unit — GAP_MATRIX row 95): RewriteFiles' `validate_no_new_deletes_for_data_files` (run automatically in `validate()`, `ignore_equality_deletes = data_sequence_number.is_some()`) vs Java `BaseRewriteFiles.validate` — proves the conflict DECISION (ACCEPT/REJECT), not surviving rows. The seq-preservation + position-vs-equality-delete nuance. PARTITIONED V2 `{x long, y long, z long}` identity(x) (the rewrite_files.rs unit-test fixture); S0 appends A (x=0) + B (x=1), S1 a concurrent row_delta adding a delete file; the symmetric rewrite replaces A with a fresh A' (`validate_from_snapshot(S0)` + optional `data_sequence_number(A's seq)`). 4 scenarios, hand-declared anti-circular: `no_seq_eq_delete_reject` (no seq + concurrent EQUALITY delete on A → REJECT), `seq_eq_delete_accept` (seq preserved + the SAME equality delete → ACCEPT, ignored), `seq_position_delete_reject` (seq preserved but a concurrent POSITION delete on A is ALWAYS fatal → REJECT), `disjoint_delete_accept` (a POSITION delete on the disjoint B → ACCEPT). Scenarios 1+2 differ ONLY by the seq knob. A is DERIVED from the loaded table (live x=0 data file at S0), no cross-engine path coupling. C2 vacuous-reject GUARD: A is live at BOTH S0 and S1 (the delete ADDS, it does not REMOVE A) so the by-path resolution + empty-delete precondition pass — only the concurrent-delete validation can reject; the reject asserts the exact `found new ... delete for replaced data file` message. GEN test (`ICEBERG_INTEROP_REWRITEFILES_CONFLICT_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks Rust's own decision; D1 test (`ICEBERG_INTEROP_REWRITEFILES_CONFLICT_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric rewrite (REJECT = non-retryable `DataInvalid`). Java D2 judgment via `verify-interop-rewritefiles-conflict` in `run-interop-rewritefiles-conflict.sh`. Sabotage (residue-stripped, pinned): disjoint-swap (the C2 vacuous-reject mutation test — feed `no_seq_eq_delete_reject` the disjoint table ⇒ re-derived ACCEPT, pinned to the outcome-mismatch line) + position-to-equality-swap (feed `seq_position_delete_reject` the eq-delete table ⇒ a preserved-seq rewrite ignores the equality delete ⇒ ACCEPT, proving the position-vs-equality branch is load-bearing) + structural-truncate (load/parse-error line), all fail-closed. Env-gated no-ops for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX row 95) |
| `interop_remove_dangling.rs` | Maintenance `RemoveDanglingDeleteFiles` interop (post-charter unit #1; status in GAP_MATRIX): the dangling-delete GC action proven against Java's `findDanglingDeletes` semantics WITHOUT Spark — the real action is Spark-surface (`RemoveDanglingDeletesSparkAction`, NOT on the iceberg-core oracle classpath), so the Spark-action-output comparison is N/A and the proof is THREE anti-circular engine-agnostic claims with the dangling set hand-declared identically both sides from the spec: (1) SEMANTICS-MATCH — Rust removes EXACTLY the hand-declared dangling set + keeps the rest (D1), Java INDEPENDENTLY recomputes `findDanglingDeletes` (the `(spec_id, partition)` min-seq grouping + the `<`/`<=` off-by-one + the DV referenced-file join) over the PRE table's live entries and confirms the SAME set (D2); (2) API CONTRACT — `removed-{position,equality}-delete-files`/`removed-dvs` counters + surviving-delete set match both directions; (3) CORRUPTION SAFETY (read-identity) — merge-on-read live id set IDENTICAL before↔after cleanup (Rust scans pre+post; Java `IcebergGenerics` reads pre + the Rust-cleaned table). TWO worlds (V3 mandates DVs for position deletes ⇒ split): V2 `{id,cat,y}` identity(cat) — `pk` pos-KEEP @min boundary, `pr` pos-REMOVE, `ek` eq-KEEP, `er` eq-REMOVE at the same-seq OFF-BY-ONE boundary, `ne` REMOVE min-IS-NULL — + V3 `dv` DV-REMOVE (ref gone, a REAL Puffin DV — closes the prior pure-fn-only DV gap) / `dk` DV-KEEP. NAMED 1:1-faithful divergence: Java auto-prunes a dangling DV at the commit removing its referenced file (so the DV-REMOVE is reachable only on the Rust-built table; over Java's table Rust removes NOTHING, agreeing with Java's already-clean state). GEN test (`ICEBERG_INTEROP_REMOVE_DANGLING_GEN_DIR`) writes per world `rust_table<suffix>` (pre) + `rust_table_cleaned<suffix>` (post-action) + self-checks; D1 test (`ICEBERG_INTEROP_REMOVE_DANGLING_DIR`) `register_table`s each Java `table<suffix>` and runs the action. Java D2 judgment via `verify-interop-remove-dangling` in `run-interop-remove-dangling.sh`. Sabotage battery 3 (fail-closed, control-gated, pinned per line): wrong-cleaned-swap (a NOT-cleaned table ⇒ survivor-mismatch line) + read-identity-breaker (a cleaned table whose read differs from PRE ⇒ read-identity-BROKEN line) + structural-truncate (load/parse-error line). Env-gated no-ops for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX row 135) |
| `interop_delete_reachable.rs` | Maintenance `DeleteReachableFiles` interop (post-charter unit #3; status in GAP_MATRIX row 151): the `DROP TABLE PURGE` engine proven against Java's engine-agnostic iceberg-CORE reachable-set logic — `ReachableFileUtil.{metadataFileLocations(table,true), manifestListLocations, statisticsFilesLocations, versionHintLocation}` (1.10.0 javap-verified) + a `Snapshot.allManifests` content scan — NON-CIRCULAR (the oracle is Java's REAL reachable logic). Cross-engine token = a path-INDEPENDENT CATEGORY-COUNT descriptor (`data;pos_delete;eq_delete;manifest;manifest_list;metadata_json;version_hint;statistics`) hand-declared identically in `DeleteReachableOracle.Reachable` + this test (Java/Rust write at different temp roots/UUIDs ⇒ raw paths never compared). Fixture: a V3 multi-snapshot table `{id,cat,y}` identity(cat) with data + 2 Puffin DVs (the V3 position-delete form) + an equality delete + a statistics file + a previous metadata.json. D2 (this GEN test, `ICEBERG_INTEROP_DELETE_REACHABLE_DIR`): reads Java's `final.metadata.json`, recomputes ITS OWN descriptor, asserts == `java_reachable.json`, runs `DeleteReachableFiles` end-to-end with a collecting deleter (planned-set descriptor → `rust_deleted.json`) + physically purges a THROWAWAY copy's resident files. D1 (Java judges, via `run-interop-delete-reachable.sh`): Java's `ReachableFileUtil` recomputation == `rust_reachable.json` AND `rust_deleted.json` (DELETE-COMPLETENESS — every category fully purged, no over-delete). Sabotage: under-count the DATA category ⇒ Java fails closed via the `reachable descriptors differ` line. Env-gated no-op for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX row 151) |
| `interop_builder_flips.rs` | Wave 3 builder-flips interop (proves the GAP_MATRIX `deleteFromRowFilter` row + the `caseSensitive` slice of the conflict-detection row; statuses live in GAP_MATRIX): DeleteFiles' two builder flips `delete_from_row_filter(Predicate)` + `case_sensitive(bool)` vs Java `StreamingDelete.deleteFromRowFilter`/`caseSensitive`. The FIRST interop exercising the by-row-filter delete RESOLUTION (which file set is removed), not a conflict ACCEPT/REJECT. PARTITIONED V2 `{id long, Category string, y long}` identity(Category) — MIXED-CASE `Category` is the case discriminator. 5 scenarios, hand-declared anti-circular: `filter_delete_partition` (`Category=="a"` → DELETE cat=a, survivors={b}), `filter_keep_complement` (`Category=="b"` → DELETE cat=b, survivors={a}; the complement pins the surviving set tracks the FILTER), `filter_partial_error` (`y>=55` vs a straddling cat=b helper y[40,60] → some-but-not-all → ERROR), `case_insensitive_match` (wrong-cased `category` binds under `case_sensitive(false)` → survivors={b}), `case_sensitive_reject` (the SAME wrong-cased ref under the default `true` fails to bind → ERROR). DELETE/KEEP signal = the set of surviving Category partition values (path-independent). GEN test (`ICEBERG_INTEROP_BUILDER_FLIPS_GEN_DIR`) writes each `<scenario>/rust_table` + self-checks; D1 test (`ICEBERG_INTEROP_BUILDER_FLIPS_DIR`) `register_table`s each Java `<scenario>/table` and runs the symmetric `delete_from_row_filter(...).case_sensitive(...)`. Java D2 judgment via `verify-interop-builder-flips` in `run-interop-builder-flips.sh`. Sabotage (residue-stripped): semantic-rename (`Category`→`category` in `case_sensitive_reject` so the wrong-case filter now binds ⇒ ERROR→survivors={b}, pinned to the outcome-mismatch line — proves the caseSensitive bind is reached) + structural-truncate (load-error line). 2 NAMED fail-safe divergences (Rust stricter): `markedForDelete` short-circuit unported + empty-match filter (Rust rejects, Java commits no-op) — both kept out of the set. Env-gated no-ops for offline `cargo test`. | landed 2026-06-16 (GAP_MATRIX `deleteFromRowFilter` + `caseSensitive`-slice rows) |

## I want to...

| I want to... | go to |
|---|---|
| Add an interop test for a new capability | copy the structure of `interop_update_schema.rs` (scenario table + `apply_scenario_ops` + structural compare); scenarios must exist identically in `InteropOracle.java` |
| Regenerate the fixtures these read | [dev/java-interop/map.md](../../../dev/java-interop/map.md) |
| Understand what "equal" means here | structural equality via the spec types' `PartialEq` (schema/spec/refs), never raw bytes |

## Pointers

- **Up:** [crates/iceberg/](..) · **Related:** [dev/java-interop/map.md](../../../dev/java-interop/map.md)
  (fixture generation + Direction-2 verify), [docs/testing.md](../../../docs/testing.md) (the
  testing contract these satisfy), `testdata/interop/` (the committed fixtures).

## Debug

### Known failure modes

| Symptom | Likely cause |
|---|---|
| Interop test fails but unit tests pass | A real parity divergence — the harness's purpose; compare against the Java source, not the Rust unit-test expectations |
| Test passes locally, Direction 2 fails | Rust emits metadata Java rejects — run the `-d2` script and read Java's parse/validation error |
| Fixture file not found | Fixtures are committed under `crates/iceberg/testdata/interop/` — a new scenario needs a `generate` run first |
| Scenario drift | Rust `apply_scenario_ops` and Java `InteropOracle.scenarios()` must stay op-for-op identical — they are mirrored by hand |
| `register_table` rejects a pre-written metadata file | The catalog reads via its OWN FileIO: build `MemoryCatalog` `with_storage_factory(Arc::new(LocalFsStorageFactory))` over a tempdir, put the file under a `metadata/` subdirectory, and name it `<version>-<uuid>.metadata.json` — a bare `base.metadata.json` fails "Invalid metadata file name format" |
| Can't name an action type / call `.commit()` from an integration test | `TransactionAction` is `pub(crate)` — drive evolution through the PUBLIC path (catalog register + `Transaction` + `commit`); do NOT widen production visibility for a test. The catalog path is stronger anyway (it runs the `TableRequirement` checks) |

### First checks

- Run the single failing scenario with `-- --exact` and diff the two metadata JSONs by hand —
  field-id mismatches jump out.

### Escalate to

- [dev/java-interop/map.md#debug](../../../dev/java-interop/map.md#debug) for oracle-side issues;
  the owning source map for Rust-side divergences.
