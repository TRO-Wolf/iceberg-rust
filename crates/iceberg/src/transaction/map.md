<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# map.md — crates/iceberg/src/transaction/

## Purpose

The atomic table-mutation layer: every metadata- or data-changing operation is a
`TransactionAction` applied through a `Transaction` (Java analogues: `Transaction`,
`SnapshotProducer`, `MergingSnapshotProducer`, and the per-action `Base*` classes in
`core/`). This is the heart of the Phase-1 (metadata evolution) and Phase-2 (write
engine) parity work.

## Contents

| File | Java analogue | What it does |
|---|---|---|
| `mod.rs` | `Transaction` | `Transaction` struct: action registry, `commit()` retry loop (Java `Tasks$Builder` schedule via `commit_backoff.rs`; retries only `retryable()` errors and NEVER `ErrorKind::CommitStateUnknown` — the may-have-landed class, Java `SnapshotProducer.commit()`'s `onlyRetryOn(CommitFailedException)` + dedicated `CommitStateUnknownException` rethrow; GAP_MATRIX row R157), the post-loop `reconcile_unknown_commit_outcome` seam (an unknown outcome is resolved by refresh via `commit_status.rs`; landed ⇒ `Ok` with the reloaded table, no re-apply; absent/undecidable ⇒ the ORIGINAL unknown error surfaces; commits with no `AddSnapshot` carry no evidence and skip it), `latest_attempt_snapshot_ids` evidence capture (cleared per attempt, set before `update_table`), `starting_snapshot_id` plus `starting_ref_snapshot_ids` (per-branch heads at txn start, so `to_branch` validate/retry walks the named ref — row R168), the `pub fn` ctor per action; `do_commit` skips `catalog.update_table` entirely when the actions emitted zero updates (Java `ops.commit` early-return on `base == metadata` — a no-op commit writes no metadata version, moves no pointer, fires no event); `Self::apply` likewise skips `update_table_metadata` when ONE action emits zero updates — requirements are still checked and `existing_updates`/`existing_requirements` still extended, but the metadata (and its `last_updated_ms`) is not rebuilt in memory, so `apply_locally` returns the input metadata `Arc` unchanged (F-ICE-RESIDUES-21B R-02; pin `staged_table_tests.rs::apply_locally_empty_updates_keep_the_metadata_arc`); test fixtures `make_v1_table`/`make_v2_table`/`make_v2_minimal_table` + the in-catalog pair `make_v2_minimal_table_in_catalog`/`make_v3_minimal_table_in_catalog` (same schema/spec — pick V2 for parquet position deletes, V3 for deletion vectors; the `validateDeleteFileForVersion` gate) |
| `action.rs` | `PendingUpdate` | The `TransactionAction` / `ApplyTransactionAction` seam: `commit(self: Arc<Self>, &Table) -> Result<ActionCommit>`; default-no-op `validate` hook (conflict validation runs in `do_commit` against the refreshed base); `target_ref()` (default `main`) so retry/validate walk the named branch (row R168) |
| `to_branch.rs` | `SnapshotUpdate.toBranch` | `to_branch(name)` on the seven Java-supporting snapshot producers; `SnapshotProducer` parent/ref/OCC/validate walk the named branch (row R168) |
| `commit_backoff.rs` (+ `commit_backoff_tests.rs`) | `Tasks$Builder.runTaskWithRetry` | The commit-retry sleep schedule, bytecode-verified against 1.11.0 (`F-COMMIT-JITTER-1` ledger): `sleep = (int)min(minWait * 2^(attempt-1), maxWait) + nextInt(max(1, (int)(delay * 0.1)))` — attempt 1-based so the first sleep is `min-wait`; stops when `attempt >= num-retries + 1` or wall-clock elapsed exceeds `total-timeout` (the FIRST failure is exempt from the timeout, strict `>`; Java's `(int)(delay*0.1)` == `delay/10` integer division). An `Iterator<Duration>` (backon `Backoff`) with injectable elapsed/jitter sources for the pins |
| `commit_status.rs` | `BaseMetastoreOperations.checkCommitStatus[Strict]` / `CommitStatus` | Reconciliation-by-refresh for `ErrorKind::CommitStateUnknown` (row R157): `check_commit_status_strict` re-reads the catalog with bounded retries (`commit.status-check.*` knobs, Java names/defaults; `retry(n)` = n+1 attempts, 2.0-factor backoff clamped at max-wait, total-timeout stop) and classifies `Success(reloaded)` / `Failure` / `Unknown` by searching the reloaded snapshot SET for the attempted snapshot ids (history, not just the current pointer — a commit buried under a concurrent writer still resolves landed). The caller (`mod.rs`) applies Java's production NON-strict conversion: strict-`Failure` ⇒ surfaced as the original unknown (1.10.0's only production callers — Glue/DynamoDb — use the non-strict variant; absent-after-refresh is NEVER a retryable failure, a pending in-flight request could still land). Named divergences in the module docs (snapshot-id evidence vs Java's metadata-location; metadata-only commits not reconciled; REST/SQL unknowns also reconciled) |
| `snapshot.rs` | `SnapshotProducer` / `MergingSnapshotProducer` | Shared snapshot-producing machinery: the required `FirstRowIdPolicy` constructor argument (`Suppress` clears an added data file's `first_row_id`, Java `MergingSnapshotProducer.add(DataFile)`; `Preserve` is `FastAppend` + `RewriteManifests`, which extend Java's `SnapshotProducer` — row R166), the Java-order manifest list (added-data, existing-data, added-delete, existing-delete: the V3 manifest-list writer assigns row-id ranges IN LIST ORDER, so all three conjuncts of that order are behaviour — pinned by `manifest_list_order_tests` plus the row-lineage interop suite), manifest writing, `process_deletes` (matches a removed DATA entry by PATH and a removed DELETE entry by the Java `DeleteFileSet` triple `(location, content_offset, content_size_in_bytes)`, via `snapshot/removal_targets.rs` — F-18: two DVs share one Puffin path, so path keying tombstoned an untouched sibling blob), `resolve_delete_paths` (by-path DATA removal), `resolve_removed_delete_files` (by-triple DELETE removal, the `removeDeletes` apply-side sibling — Java `MergingSnapshotProducer.delete(DeleteFile)` → `ManifestFilterManager.delete(F)` → `deleteFiles.add`), `resolve_partition_deletes`, `current_manifests` (returns the FULL manifest list — DATA **and** DELETE — so every delete-bearing action carries outstanding MoR delete manifests forward, Java `MergingSnapshotProducer.apply` L973-1011; dropping them silently resurrects deleted rows), PER-SPEC added-manifest grouping (`write_added_manifests`/`write_added_delete_manifests` group added DATA/DELETE files by `partition_spec_id` and write ONE manifest per (content × spec), spec-id descending — Java `MergingSnapshotProducer.newDataFilesBySpec`/`newDeleteFilesBySpec` + `FastAppend`; `validate_added_data_files`/`validate_added_delete_files` accept ANY existing spec and reject only an UNKNOWN id with Java's "Cannot find partition spec %s for {data,delete} file: %s"), the CONTENT-KEYED filtering writer `new_filtering_manifest_writer` (builds a DATA writer for a DATA source manifest, a DELETE writer for a DELETE source — Java's `DataFileFilterManager` vs `DeleteFileFilterManager`; a rewritten DELETE manifest MUST stay a DELETE manifest or the read path stops applying its survivors), `with_added_delete_files` (inherited seq — RowDelta) / `with_added_delete_files_with_seq` (per-file OPTIONAL explicit data seq, the `Delegates.PendingDeleteFile.dataSequenceNumber()` analogue: `None` inherits, `Some(seq)` stamped by `write_added_delete_manifests` like Java `writeDeleteFileGroup`'s `add(file, dataSeq)` — used by `RewriteFiles.addFile(DeleteFile, long)`) / `with_removed_delete_files` builder setters, `with_stage_only` (WAP staging — Java `SnapshotProducer.stageOnly()`: `commit()` emits `AddSnapshot` ALONE without `SetSnapshotRef`, so current-snapshot-id/main/snapshot-log stay unchanged on disk; the staged snapshot still consumes a sequence number; published later by `cherry_pick`), `new_cluster_manifest_writer` (fresh manifest of a given CONTENT per spec id, shared by `rewrite_manifests` + `merge_append` + the producer's own added-manifest grouping), the `ManifestProcess` seam (async + `Result` + `&mut SnapshotProducer`; `DefaultManifestProcess` passthrough for fast append, `MergeManifestProcess` for merge append), the concurrent-commit walk `files_after` + wrappers (`added_data_files_after`, `added_delete_files_after`, `added_dv_candidate_delete_files_after` — the wider `{Overwrite, Delete, Replace}` DV op set, `deleted_data_files_after`), the shared conflict test `first_conflicting_file` / `validate_no_conflicting_added_data_files`, the REPLACE record-count guard in `replace_record_count.rs` (row R107: after summary and before manifest IO, `Operation::Replace` with `added-records > deleted-records` is `DataInvalid`; an absent key counts as 0, Java `SnapshotProducer.apply` offsets 311-364 / `PropertyUtil.propertyAsLong`), and the delete-file format-version gate `validate_delete_file_for_version` + `dv_desc` (Java `validateDeleteFileForVersion` / `ContentFileUtil.dvDesc` — V1 throws, V2 rejects DVs, V3 requires DVs, eq exempt). `process_deletes` lives in `snapshot/manifest_filter.rs` and ports `dropDeleteFilesOlderThan` (Java 1.11.0 `MergingSnapshotProducer.apply` L981-990, `ManifestFilterManager` L462-467/L516-522): on every merging operation (`SnapshotProduceOperation::drops_old_delete_files`, default `true`; `false` only for `FastAppend` + `RewriteManifests`, the two Java `SnapshotProducer` subclasses) the minimum is taken over the filtered existing DATA manifests' `min_sequence_number` (unassigned skipped, identity `last_sequence_number`), and a live delete entry with `0 < data seq < minimum` becomes a `Deleted` entry and is counted in the `removed-*` summary keys. Scope mirrors Java's manifest-open gate: every live delete manifest when the commit removes a data file, else only the delete manifests that hold an explicitly removed delete file; a delete manifest whose own `min_sequence_number` is not below the minimum is not read. `removeDanglingDeletesFor` (the DV half — Java 1.11.0 `MergingSnapshotProducer.apply` L994-995 → `ManifestFilterManager.isDanglingDV`) lives in `process_deletes` too (F-DANGLING-DV-COMMIT-1): on the same merging-operation set, a commit that removes data files opens every live delete manifest and tombstones each Puffin DV whose `referenced_data_file` names a removed data file (`manifest_filter.rs::is_dangling_dv`, over `delete_file_index::{is_deletion_vector, referenced_data_file_location}`) — the drop is per-ENTRY so a sibling blob in the same Puffin survives, parquet position deletes are untouched (the sequence GC decides them), and the dropped blob feeds the `removed-*` keys. The `RewriteDataFiles` planner-side drop stays in `maintenance/rewrite_data_files_dv.rs`; the opt-in cleanup pass is `maintenance/remove_dangling_delete_files.rs` |
| `snapshot/conflict_filter.rs` | `MergingSnapshotProducer.validateAddedDataFiles` filter evaluation (`ManifestGroup.filterData`) | Shared per-file conflict test: the caller's `conflictDetectionFilter` (default `AlwaysTrue`) binds once, then each concurrent file gates on its own spec's partition projection (`InclusiveProjection` + `ExpressionEvaluator`) before `InclusiveMetricsEvaluator`. Unknown-spec files stay conflicting. Called by the three `validate_*_on` walks in `snapshot.rs` |
| `occ_scoped_tests.rs` | — (fork OCC battery) | 19 fault-injected scoped-conflict tests (serializable vs snapshot × row-delta/overwrite, files-exist, append retry, unknown-no-retry, NOT filters). Wired from `action.rs`, not `mod.rs`: `mod.rs` sits exactly on its 1947-line legacy ceiling, so the wiring lives next to the action seam the battery drives through. Shared helpers are `pub(crate)` for the projection file |
| `occ_scoped_projection_tests.rs` | — (fork OCC battery, round 2) | 4 projection pins sharing the battery helpers: truncate-range boundary (strict-swap killer), older-spec conservative conflict, promoted match/mismatch. Split from `occ_scoped_tests.rs` at its 1000-line ceiling |
| `append.rs` | `FastAppend` | Fast append (Java `newFastAppend`). `existing_manifest` carries EVERY prior manifest forward UNFILTERED (Java `FastAppend.apply` → `snapshot.allManifests`), INCLUDING all-tombstone manifests — do NOT re-add a `has_added_files`/`has_existing_files` filter here (that drops a manifest emptied by a copy-on-write delete, a manifest-LIST structure divergence; fixed Wave-4 Group O 2026-06-11). The MERGING append (`merge_append.rs`) DOES filter — that is intentional Java `shouldKeep` parity. `stage_only()` (WAP staging — adds the snapshot without moving `main`). `to_branch(name)` (row R168) |
| `merge_append.rs` | `MergeAppend` / `ManifestMergeManager` | Merge append (Java `newAppend`): append data files in one `Operation::Append` snapshot like fast append, then BIN-PACK + MERGE the manifest list per `commit.manifest-merge.enabled` / `commit.manifest.min-count-to-merge` / `commit.manifest.target-size-bytes`. Provenance-preserving (carried entries → `add_existing_entry`; this-commit entries → `add_entry`; old tombstones suppressed). Ported `BinPacking.packEnd` lives in a private `bin_packing` submodule. Delete-manifest merging deferred (carried unchanged) |
| `delete_files.rs` | `StreamingDelete` | Delete data files by path/reference; `delete_from_row_filter(Predicate)` (Java `deleteFromRowFilter` → `deleteByRowFilter` — per-file partition RESIDUAL + strict/inclusive metrics KEEP/DELETE/PARTIAL via the SHARED `SnapshotProducer::resolve_filter_deletes`, unioned with by-path deletes; PARTIAL match is a non-retryable error, `operation()` stays the constant `Delete`); opt-in `validate_files_exist()`; `stage_only()` (WAP staging — adds the `Delete` snapshot without moving `main`) |
| `overwrite_files.rs` (+ `overwrite_files_operation.rs`) | `BaseOverwriteFiles` | Explicit add+delete in one `Overwrite` snapshot; opt-in `validate_no_conflicting_data()` + `conflict_detection_filter`; the `SnapshotProduceOperation` (operation classification incl. the staged-replace `allow_empty_commit` opt-in, row R172) lives in `overwrite_files_operation.rs` |
| `replace_partitions.rs` | `BaseReplacePartitions` | Dynamic partition overwrite; opt-in concurrent-commit conflict validation (`validate_no_conflicting_data`/`_deletes`) + opt-in same-commit `validate_append_only` (Java `validateAppendOnly`→`failAnyDelete`: reject non-retryably if the overwrite would remove any existing live file — one `!resolved.is_empty()` check on the resolved partition deletes in `delete_files`). **The two `dataSpec()` scope branches** (Java `MergingSnapshotProducer.dataSpec()` = the spec shared by the ADDED files, NOT the table default): `is_full_table_replace` mirrors `BaseReplacePartitions.apply`'s `dataSpec().fields().isEmpty()` ⇒ `deleteByRowFilter(alwaysTrue)` (resolved through the shared `resolve_filter_deletes`, so EVERY live data file goes, including files under older partitioned specs), and `conflict_scope` mirrors `BaseReplacePartitions.validate`'s `dataSpec().isUnpartitioned()` ⇒ the `alwaysTrue` overloads (`ConflictScope::AllFiles`) instead of the `PartitionSet` form (`ConflictScope::Partitions`). The two predicates DIFFER on purpose — `fields().isEmpty()` vs `isUnpartitioned()` (also true for an ALL-VOID spec) — so an all-void data spec is wide for validate and narrow for apply; do not harmonize them. NOT ported: `dataSpec()`'s two `Preconditions.checkState` throws (no added files; mixed-spec adds) — `data_spec_id()` returns `None`, which can only fall back to the NARROWER branch |
| `replace_record_count.rs` | `SnapshotProducer.apply` REPLACE record-count check | Shared `Operation::Replace` guard (row R107): after summary, before manifest IO, refuse `added-records > deleted-records`; absent key = 0. Tests in `replace_record_count_tests.rs` |
| `rewrite_files.rs` | `BaseRewriteFiles` | Compaction-commit primitive (`Operation::Replace`); `data_sequence_number` preservation (added files keep the replaced files' data seq so outstanding equality deletes still apply), `validate_from_snapshot` + `validate` (shared `validate_no_new_deletes_for_data_files`, `ignore_equality_deletes = seq preserved`); carries DELETE manifests forward except the entries the producer's delete-file sequence GC retires. The REPLACE record-count guard lives in the shared producer (`replace_record_count.rs`, row R107), not only here. **DELETE-file REMOVAL surface** `delete_delete_file`/`delete_delete_files` (Java `RewriteFiles.deleteFile(DeleteFile)` → `MergingSnapshotProducer.delete(DeleteFile)`): routes removed delete files through the producer's `with_removed_delete_files` apply-side removal (operation stays Replace; `removed-*` summary counters). **DELETE-file ADD surface** `add_delete_file`/`add_delete_files` (Java `addFile(DeleteFile)` → inherited seq) + `add_delete_file_with_sequence_number` (Java `addFile(DeleteFile, long)` → EXPLICIT data seq, the `Delegates.PendingDeleteFile.dataSequenceNumber()` analogue: stamped via `SnapshotProducer::with_added_delete_files_with_seq` → `write_added_delete_manifests`, REUSING RowDelta's added-delete machinery — NOT a fork) + the 4-set `rewrite_files_with_deletes(data_to_replace, delete_to_replace, data_to_add, delete_to_add)` (Java 4-arg `rewriteFiles`, one Replace snapshot). The explicit-seq overload is the `RewritePositionDeleteFiles` corruption line: a wrong seq resurrects/over-deletes rows (scan-level crown-jewel pin + on-disk pre-inheritance pin; negative seq rejected). All three `validateReplacedAndAddedFiles()` preconditions enforced with Java-exact messages; the third (`addsDeleteFiles ⇒ deletesDeleteFiles`, "Delete files to add must be empty…") is now REACHABLE and pinned both directions. This is the commit vehicle `maintenance/remove_dangling_delete_files.rs` (`RemoveDanglingDeleteFiles`) drives |
| `rewrite_manifests.rs` | `BaseRewriteManifests` | Manifest re-organization (NOT data change): cluster live data-manifest entries into new manifests via the provenance-preserving `add_existing_entry` path, and/or explicit add/delete manifest replacement; `Operation::Replace`, live set unchanged. Extends `SnapshotProducer` (NOT `MergingSnapshotProducer`). Opt-in `rewrite_delete_manifests(bool)` (default `false` = Java core immunity): when `true`, DELETE manifests matching `rewrite_if` are clustered into their own new manifests under `(cluster_key, spec_id, content)` keys — the `RewriteManifestsSparkAction` delete leg (RePark `CALL rewrite_manifests` parity, F-RM-DELETES-1); a rewritten manifest with zero live entries contributes no output, so an emptied delete manifest is dropped; delete-entry data/file sequence numbers are preserved explicitly on disk. Opt-in pins in `rewrite_manifests_deletes_tests.rs` (wired from `action.rs` — this file sits at its legacy ceiling) |
| `rewrite_manifests_deletes_tests.rs` | — (F-RM-DELETES-1 pins) | 5 delete-leg pins: V2 parquet position deletes and V3 deletion vectors each collapse 2 delete manifests to 1 with provenance preserved on disk, an emptied delete manifest drops out, explicit `false` keeps Java-core immunity, `rewrite_if` gates delete manifests identically to data |
| `rewrite_manifests_cluster.rs` | — (P-RM-SORT-BY) | `cluster_by_columns(Vec<String>)`: a data-driven cluster key over the named partition columns' values (resolved per file spec at commit); empty input is Spark's `sort_by must not be empty when provided`; last setter wins against `cluster_by`. Also homes the moved `validate_deleted_manifests` (file-size split) |
| `rewrite_manifests_cluster_tests.rs` | — (P-RM-SORT-BY pins) | 6 pins: value grouping, constant-key contrast, the empty refusal, the unknown-column error, a two-column key, last-setter-wins. Wired from `action.rs` (this file sits at its legacy ceiling) |
| `snapshot/removal_targets.rs` | `ManifestFilterManager` `deletePaths` / `deleteFiles` | F-18: `RemovalTargets` + `RemovalHits` + `DeleteFileMatcher` — the per-manifest-kind removal key. DATA by path, DELETE by the `DeleteFileSet$DeleteFileWrapper` triple, so a shared-Puffin sibling blob survives its neighbour's removal. `RemovalTargets::wants` lets `process_deletes` carry a manifest of an untargeted content kind forward WITHOUT parsing it. Only a DATA path can be reported missing there: a DELETE blob was already resolved against a live entry in `commit()` |
| `row_delta.rs` / `row_delta_fresh_dv.rs` | `BaseRowDelta` | Merge-on-read commit: data + position/equality/deletion-vector delete files in one snapshot; **F-22 (2026-09-03):** `close_touched_dv_containers_with_partitions` collects live non-Puffin position deletes on the same delete-manifest pass (`DvContainerClose::legacy_deletes`) and returns `data_sequence_numbers` when the data-manifest walk ran. **F-23 (2026-09-03):** that walk is skipped when `pending_legacy` is empty and `known_partitions` covers every touched path (`data_sequence_numbers` empty then; F-26: the in-tree MoR DELETE/UPDATE plans supply that map from their scan tasks, so the skip fires in tree, not only via RePark); otherwise it stops once every wanted path is found. **F-23 r2 (2026-09-03):** the first data manifest is loaded alone and only the tail is buffered (an ordered buffer of 8 issued 8 GETs for a one-manifest hit on any store whose read is not ready on the first poll); with no legacy deletes only the touched paths `known_partitions` missed are wanted, so `data_sequence_numbers` carries just those. **F-25 (2026-09-04):** the commit-door walk in `row_delta_fresh_dv.rs` stops the same way: newest-first, one load before buffering `DV_IO_CONCURRENCY`, exit once every added-DV key resolves (newest-file DV at 192 manifests: 1 read latent and non-latent); oldest-manifest and never-found keys keep the full walk. Merge uses `load_legacy_positions_by_path`. **F-21 (2026-09-02):** DataFusion `write_deletion_vectors` merges legacy parquet position deletes (file-scoped removed, partition-scoped kept) via `delete_legacy_merge.rs`; `row_delta_fresh_dv` door blocks file-scoped (`referenced_data_file_location.is_some()`) unless this commit removes it; a file-scoped delete whose sequence is below the data file's does not block (seq pin). Partition-scoped deletes continue. File scope is not the only door test. **F-17:** `add_delete_file_with_sequence_number` stamps a rewritten sibling DV's original data seq. ADD-side (`add_data_files`/`add_deletes`) + apply-side REMOVAL of delete files (`remove_deletes(DeleteFile)` → producer `removed_delete_files` → tombstone in the rewritten DELETE manifest; `removed-dvs`/`removed-position-delete-files`/`removed-equality-delete-files` summary) AND of DATA files (`remove_rows`/`remove_data_files` → `RowDeltaOperation::delete_files` → `resolve_delete_paths` → `process_deletes` rewrite + `deleted-data-files`/`deleted-records` summary — the SAME removal machinery `OverwriteFiles`/`DeleteFiles` use; landed 2026-06-11); `validate_no_conflicting_data_files/_delete_files`, `validate_data_files_exist`, always-on `validateAddedDVs` (op set `{Overwrite, Delete, Replace}`) + `validateNoConflictingFileAndPositionDeletes`; the fresh-DV door (`validate_fresh_dvs_only`) with its `remove_deletes` escape hatch (a DV may shadow a live delete IFF that delete is removed in the SAME commit — the Java merge-and-replace contract; DataFusion `write_deletion_vectors` now runs that merge). **`operation()` is the 1.10.0 bytecode TWO-branch form** (`addsDeleteFiles && !addsDataFiles ⇒ Delete; else ⇒ Overwrite` — NO Append branch; MAIN's append arm is post-1.10.0, the 2026-06-08 "third condition" lesson settled against the jar) |
| `manage_snapshots.rs` | `ManageSnapshots` | Branch/tag CRUD, rollback(-to-time), set-current, fast-forward, retention. `rollback_to_time` resolves at commit over the CURRENT ancestry only (Java `SetSnapshotOperation.findLatestAncestorOlderThan`, strict `<`, never a sibling); empty ancestry — including a table with no current snapshot — fails with Java's "Cannot roll back, no valid snapshot older than: %s" |
| `expire_snapshots.rs` | `ExpireSnapshots` / `RemoveSnapshots` | **METADATA-ONLY** snapshot expiry (the `cleanExpiredFiles(false)` semantics; 1.10.0 `internalApply` bytecode-verified): per-branch age+count retention (contiguous-prefix walk, per-ref overrides beat the `history.expire.*` defaults), branch/tag `max_ref_age_ms` ref expiry (`main` never ref-expires), unreferenced-snapshot retention, explicit `expire_snapshot_id` (seed set; retained-ref heads guarded with Java's message), GC gate (`gc.enabled`). Emits `RemoveSnapshotRef` + `RemoveSnapshots` + per-ref `RefSnapshotIdMatch` guards (rejects the concurrent-rollback over-expiry window); no-op emits nothing. **NEVER deletes files — physical cleanup is the explicit post-commit step in `expire_cleanup.rs`** (deferred: `cleanExpiredMetadata`, interop). The apply side (`TableMetadataBuilder::remove_snapshots`) prunes statistics + dangling refs per Java — completed in the same increment |
| `expire_cleanup.rs` | `ReachableFileCleanup` / `FileCleanupStrategy` / `RemoveSnapshots.commit()` cleanup half | **DELETES FILES** (B2). `ExpireSnapshotsCleanup`: `commit_and_clean(tx, catalog)` (commit-THEN-clean, deletion structurally unreachable on a failed commit — Java's post-commit-only ordering) + the two-state core `clean_expired_files(before, after) -> CleanupReport`. 1.10.0 `ReachableFileCleanup` set algebra (bytecode-rederived): expired snapshots' manifest lists (minus retained-shared — a Rust-only under-deletion guard, Java deletes unconditionally); candidate manifests minus retained manifests BY PATH (`GenericManifestFile` path equality); LIVE entry paths (status != DELETED, both sides — `isLiveEntry`) of dying manifests minus retained live paths, DATA + DELETE manifests both (the Java cleanup projection omits `content`, defaulting DATA — delete manifests are walked identically; a DV entry's path is its puffin ⇒ shared-puffin dedup); stats locations before − after. `CleanupReport` reports the content funnel BOTH as the union field `deleted_content_files` AND per content class through the DERIVED views `deleted_data_files()` / `deleted_position_delete_files()` / `deleted_equality_delete_files()` (Spark's three per-content-type `expire_snapshots` columns; classification carried from the manifest entry's `content_type()` — a DV puffin counts as a POSITION delete because Java tags by `ContentFile.content()` alone, never by format). Injectable `delete_with` fn (dry-run/test seam, Java `deleteWith`); failures COLLECTED in the report (read-candidate → skip its files; read-retained → clear ALL content deletions, Java's catch-Throwable→empty; delete failure → record + continue) — the no-logging-dep replacement for Java's log-and-continue. `gc.enabled` re-honored at the door. Deferred: `IncrementalFileCleanup` (an optimization with stricter eligibility — module docs say why), interop |
| `staged_table.rs` | `Catalog.newCreateTableTransaction` / `newReplaceTableTransaction` | **Staged create/replace** (GAP_MATRIX R158): `StagedTableTransaction` builds metadata on FileIO without catalog publish; `add_data_files` + `commit` publishes once (`publish_create_table` / MemoryCatalog `publish_replace_table` CAS). `Transaction::apply_locally` applies actions without catalog. **Create-publish is all-or-nothing** — a staged-metadata reload failure leaves NO catalog pointer (the `register_table` default reads-before-inserts), so `table_exists` stays false and re-create/`IF NOT EXISTS` succeeds. **Replace builds ON TOP OF the existing metadata** (Java `TableMetadata.buildReplacement`) — `begin_replace` retains the table UUID + snapshot history + metadata log (appended, not truncated), resets the `main` ref, and applies the new schema/spec/sort-order/props/location as current (replace-schema field-ids taken from the caller AS-IS, NOT Java's name-matching `assignFreshIds`; `last_column_id` monotonic — D5). Format version is PRESERVED unless a `format-version` PROPERTY directs an upgrade (absent⇒keep; higher⇒upgrade; lower/unparsable⇒`DataInvalid`; `creation.format_version` ignored — D4). It reuses the existing/caller location, never a `__staged_replace` suffix, so repeated CREATE OR REPLACE never relocates the table (finding N2). The staged metadata file continues the base pointer's version (N → N+1) when the pointer parses, KEEPING the base's naming convention — a Hadoop-named `vN` base stages `v(N+1)` via `MetadataLocation::with_next_version` and is written through the exclusive-create seam (`write_commit_metadata`), so a second staged replace onto an existing `v(N+1)` fails with retryable `CatalogCommitConflicts` and leaves the winner's file byte-identical (F-HADOOP-VN-REPLACE-1, matching `HadoopTableOperations.commit`'s renameToFinal-never-overwrites). `commit`'s pending-file materialization uses `apply_locally_in_place` (`pub(crate)`, staged-txn only): the final metadata is written to the staged `v(N+1)` target rather than re-running `with_next_version` — publishing exactly one new version per replace (Java `BaseTransaction` writes one `nextVersion` at commit; F-ICE-RESIDUES-21B L-002, pins `hadoop_replace_{with,without}_files_publishes_only_next_version`); `apply_locally` keeps its `with_next_version` bump for direct callers (the stale-version collision pin depends on it). On a Hadoop-named target the stage write is DEFERRED — `begin_replace` only probes `v(N+1)` (+ gzip siblings) for an early retryable conflict and holds the metadata in memory like Java `BaseTransaction`; `commit` writes the FINAL bytes exactly once through `write_commit_metadata`'s exclusive create before the pointer CAS, so a version file is never overwritten even if a winner landed the pointer on it meanwhile (V-001; pin `tests/hadoop_version_commit.rs::hadoop_staged_replace_never_overwrites_a_live_next_version`); a `<version>-<uuid>` base keeps a fresh uuid so concurrent replaces cannot collide; it restarts at `00000-<uuid>` when the base does not parse or a caller-supplied location relocates the table (`staged_table_version_tests.rs` pins; F-GLUE-REPLACE-1 D-3). `GlueCatalog.publish_replace_table` CASes on the Glue version-id through `UpdateTable` and read-validates the staged file before the send (F-GLUE-REPLACE-1; `catalog/glue/src/replace_publish_tests.rs`). Java interop proven 2026-07-16 (R158). `with_replace_write(true)` (row R172) commits pending files with RTAS replace semantics through `overwrite_by_row_filter(AlwaysTrue)`: `overwrite` with added files, `delete` with none, no `deleted-*` summary keys; default false keeps `append`. |
| `cherry_pick.rs` | `CherryPickOperation` | Write-audit-publish (WAP) "publish" commit: publish a STAGED snapshot onto `main`. Three shapes (Java `cherrypick(long)` + `apply` FF precedence): fast-forward (parent==head → move main, NO new snapshot), APPEND replay, OVERWRITE+`replace-partitions` replay (adds+removes via `process_deletes`). `source-snapshot-id`/`published-wap-id` tags; the WAP-id dedup runs at DISPATCH time inside `plan` for the APPEND and replace-partitions shapes (Java `cherrypick()` calls `WapUtil.validateWapPublish` in those branches — for replace-partitions after the parent-ancestor check — BEFORE `apply`'s fast-forward precedence, so a duplicate `wap.id` beats the already-picked message), and `validate` re-runs it LAST after validateNonAncestor (already-ancestor + double-publish dedup) and validateReplacedPartitions (changed-partition since `picked.parent`) — Java `validate` L166-169 order. `validate` runs against the refreshed base and is skipped for the fast-forward shape; its `starting_snapshot_id` is intentionally unused (the window comes from the picked snapshot's parent) and dispatch errors surface non-retryably from `plan` before any update is emitted. `validate` and `commit` share ONE `plan` result through `cached_plan` — a `Mutex`-held `(base Arc<TableMetadata>, Arc<CherryPickPlan>)` pair reused only while `Arc::ptr_eq` on the base proves `commit` received the same base `validate` planned against (single-action `do_commit`); any `Self::apply` rebuild (multi-action transactions) or retry refresh yields a new Arc and forces a re-plan, so the cache can never serve a stale plan. The plan itself is `Arc`-cached, so a hit is a refcount bump, not a clone of every replay `DataFile` (F-ICE-RESIDUES-21B R-01; `commit` clones only the fields the producer consumes, once, in the Replay arm); consumption is pinned by `commit_uses_the_plan_cached_by_validate` (L-003). The saving is real: `plan` reads the picked snapshot's manifest list via `picked_snapshot_changes`. The WAP-id dedup (`validate_wap_publish`/`is_wap_id_published`, Java `WapUtil.validateWapPublish`/`isWapIdPublished`) walks the CURRENT ancestry and rejects a duplicate `wap.id` with the verbatim `DuplicateWAPCommitException` message, comparing against BOTH each ancestor's own `wap.id` (caught after a FAST-FORWARD publish) AND its `published-wap-id` (caught after a REPLAY publish). `write.wap.enabled` is ENGINE-side only — core never gates an ordinary commit (V3 settled OUT by 1.10.0 bytecode; `SnapshotProducer.apply` never calls `WapUtil`). STANDALONE action (not on `ManageSnapshotsAction` — it needs the producer, the ref-op action doesn't have one). 🟡 unit-proven, interop deferred |
| `publish_changes.rs` | `PublishChangesProcedure` | WAP publish by wap id: `staged_snapshot_for_wap_id(&TableMetadata, &str)` is Java's `PublishChangesProcedure` lookup ONLY — scans ALL snapshots for summary `wap.id` (0 → "Cannot apply unknown WAP ID '<id>'"; >1 → "Cannot apply non-unique WAP ID. Found multiple snapshots with WAP ID '<id>'") and never calls `WapUtil`, so an already-published unique match returns `Ok`; the `DuplicateWAPCommitException` lives in `CherryPickAction::validate_wap_publish` on the append and replace-partitions arms only, so a second publish of an FF-published delete/non-replace overwrite gets CherryPickOperation's "not append, dynamic overwrite, or fast-forward" instead (Q-26d-2). `PublishChangesAction` (`Transaction::publish_changes(wap_id)`) resolves the wap id ONCE per attempt into a bound snapshot id + ONE `CherryPickAction` held in a `Mutex` (Java `PublishChangesProcedure` binds `snapshotId` once; `SnapshotProducer.commit` retries `apply()` with that bound id — never re-scanning `snapshots()`); each `validate`/`commit`/retry re-checks the bound snapshot still exists and still carries `wap.id == wap_id`, so expire-and-restage fails "Cannot cherry-pick unknown snapshot ID: %s" rather than publishing the replacement. Also the single home of the four `stage_only()` builders (merge_append / overwrite_files / replace_partitions / row_delta — the `to_branch.rs` pattern for a cross-cutting producer knob) |
| `stage_only_tests.rs` | — (WAP pins, F-STAGE-ONLY-1) | Stage + lookup pins and the `pub(crate)` helpers `stage_only_publish_tests.rs` shares: five stage pins (one per RePark write action — staged snapshot exists with `wap.id`, `main`/snapshot log unmoved, main read unchanged, readable by id), four lookup pins (found / unknown / non-unique / already-published, Java texts verbatim). Wired from `action.rs` — `mod.rs` sits on its legacy ceiling |
| `stage_only_publish_tests.rs` | — (WAP publish pins, F-STAGE-ONLY-1) | Publish pins driven off the shared `stage_only_tests` helpers: FF keeps `wap.id`, replay stamps `source-snapshot-id` + `published-wap-id`, unknown id, double-publish, V3 fresh `first_row_id` at publish, and the bound-id pins (a second same-wap.id staged between `validate` and `commit` still publishes the bound id via its main-ref update; expire + restage of the wap id fails "Cannot cherry-pick unknown snapshot ID: %s" rather than publishing the replacement) |
| `update_schema.rs` | `SchemaUpdate` | Schema evolution incl. `union_by_name`, column defaults (✅ interop-proven); a structurally equal result (`is_same_schema`: field ids, names, types, nullability, order, doc, defaults, identifier-field ids as a SET) emits ZERO updates and `do_commit`'s empty-commit skip writes no version (Java `SchemaUpdate` → `TableMetadata.Builder.addSchemaInternal` reuse + `ops.commit` `base == metadata`; `tests/hadoop_version_commit.rs::update_schema_noop` pins) |
| `update_partition_spec.rs` | `BaseUpdatePartitionSpec` | Partition evolution (✅ interop-proven) |
| `sort_order.rs` | `BaseReplaceSortOrder` | **The template action** — mirror this when adding a new action. `sort_by(name, Transform, SortDirection, NullOrder)` takes a transform-bearing term (Java `asc(Term, NullOrder)`); `asc`/`desc` are the `Transform::Identity` shorthands. Width/type/unknown-transform validation with the run-25d Spark write-order oracle's message shapes; equal-order id reuse lives in `TableMetadataBuilder::reuse_or_create_new_sort_id` (F-WRITE-ORDER-TRANSFORM-1) |
| `update_location.rs` / `update_properties.rs` / `update_statistics.rs` / `upgrade_format_version.rs` | misc `Base*` | Location / properties / statistics / format-version updates |

## I want to...

| I want to... | go to |
|---|---|
| Commit a snapshot onto a named branch | `to_branch.rs` + `SnapshotProducer::with_target_branch`; each producer builder has `to_branch(name)` (row R168) |
| Scan and commit a named branch from DataFusion | `IcebergTableProvider::with_commit_branch` — scans resolve `snapshot_for_ref`; DML commits still call `to_branch` (row R168). A missing ref errors on the read leg (SELECT/DELETE/UPDATE); INSERT without a target scan still creates the branch |
| Add a new transaction action | copy the `sort_order.rs` pattern: builder struct + `#[async_trait] impl TransactionAction` returning `ActionCommit::new(updates, requirements)`; wire `mod` + `pub fn` ctor in `mod.rs` |
| Add conflict validation to an action | the `validate` hook (`action.rs`) + the shared walk/helpers in `snapshot.rs`; mirror `replace_partitions.rs` or `row_delta.rs` |
| Cluster manifests by column values instead of a closure | `rewrite_manifests_cluster.rs` (`cluster_by_columns`; a SQL literal can never become a Rust closure, so the action builds the key internally) |
| Understand the REPLACE record-count guard | `replace_record_count.rs` (called from `SnapshotProducer::commit` after summary, before manifest IO; row R107) |
| Understand the commit/retry loop | `mod.rs` `Transaction::commit` — refresh, re-apply, `validate` (non-retryable `DataInvalid` on conflict), catalog `update_table`; an `ErrorKind::CommitStateUnknown` from the catalog is never retried — it is RECONCILED by refresh (`commit_status.rs`: landed ⇒ `Ok`, else the unknown error surfaces; no cleanup either way) (row R157) |
| Find the Java source being mirrored | reference checkout at `/tmp/iceberg-java-ref` (re-clone `apache/iceberg` if absent) |
| Check what is/isn't interop-proven | [docs/parity/GAP_MATRIX.md](../../../../docs/parity/GAP_MATRIX.md) |

## Pointers

- **Up:** [crates/iceberg/src/](..) (no map yet) · repo contract: [AGENTS.md](../../../../AGENTS.md)
- **Related:** `spec/table_metadata_builder.rs` (the low-level metadata primitives actions emit into);
  [../writer/map.md](../writer/map.md) (produces the `DataFile`s actions commit);
  [../../tests/map.md](../../tests/map.md) (interop tests); [dev/java-interop/map.md](../../../../dev/java-interop/map.md) (the oracle)

## Debug

### Known failure modes

| Symptom | Likely cause |
|---|---|
| New action's `.commit()` not found in tests | The `TransactionAction` trait must be `use`d in the test module to call `.commit()` |
| Flaky test comparing `last_updated_ms` with `<` | Two metadata versions can share a millisecond — assert `<=` plus a structural change (e.g. metadata-log growth) |
| Conflict validation never fires | `validate` runs in `do_commit` against the *refreshed* base; a test must commit the concurrent change between transaction *build* and transaction *commit* |
| Retry loop spins on a validation failure | Conflict errors must be **non-retryable** `ErrorKind::DataInvalid` (Java's non-retryable `ValidationException`) — check the error kind |
| Fixture lacks the ref/snapshot your test needs | `make_v2_table()` has only `main`; build a forked fixture via `add_snapshot` + `set_ref`, and set the grafted snapshot's `timestamp_ms` against the metadata's `last-updated-ms` |
| Parity divergence from Java found late | Verify against the Java *source* (`Preconditions.checkArgument`, early-return no-ops) before implementing — not intuition |
| Schema/spec guard never fires for CTAS or catalog commits | Guards belong at the `TableMetadataBuilder` choke point (e.g. `add_schema`), NOT in an action's `commit()` — the action only EMITS updates; `apply` is where every path converges. Note the blast radius: only tests that APPLY updates hit it, not tests that merely inspect the emitted shape |
| Action emits over-constrained commit requirements | Derive each `TableRequirement` from the update that induces it (Java `UpdateRequirements`): `AddSpec` ⇒ last-assigned-partition-id, `SetDefaultSpec` ⇒ default-spec-id — never emit guards unconditionally |
| A lifted guard exposes latent bugs in the path it guarded | When removing a coarse precondition/guard, audit the ENTIRE previously-unreachable path — the guard may have masked a second bug no test could reach (the lifted `has_outstanding_delete_files` guard hid `existing_manifest` dropping every DELETE manifest). _Promoted 2026-06-11._ |
| Surviving entries silently corrupted by a rewrite | The #1 corruption class: re-stamping a surviving/carried-forward entry's snapshot id or sequence numbers. `add_existing_entry` preserves provenance; `add_entry` RESTAMPS. Pin with a cross-snapshot provenance test (see docs/testing.md) |
| A commit ABORTS mid-flight instead of returning an error | `commit()` runs `summary()` BEFORE `manifest_file()`, and the summary path is infallible: any panic in a summary helper preempts every typed error the later phases would have produced. The `removed_*_files` loops feed it tuples nothing validated, and `file_partition_spec` substitutes the DEFAULT spec for an unknown spec id — so the helpers it calls (e.g. `PartitionSpec::partition_to_path`) must be TOTAL. _Added 2026-07-25 (WG3-L2)._ |
| A REPLACE rewrite grows the table | the shared producer must refuse `added-records > deleted-records` after summary and before any manifest IO (row R107). If a 3-to-5 rewrite commits, the guard is missing or ran too late |
| `to_branch(missing)` + conflict validation false-conflicts the parent snapshot's files | `starting_snapshot_for` must fall back to the txn-start main head when the named ref is absent, so `files_after` is exclusive of that parent |
| Fresh-DV door fail-open or false-reject on a diverged branch | `validate_fresh_dvs_only` must walk `latest_snapshot(metadata, target_branch)`, not `current_snapshot()` |
| `cluster_by_columns` rewrites one manifest per file, or errors on a known column | The key resolves each named column against the FILE's spec (`cluster_key_for_columns`); a column absent from that spec is a loud error, and values of different types key apart. Pins: `rewrite_manifests_cluster_tests::*` |

### First checks

- Does the action emit **only changed** refs/updates (no-op suppression)? Java emits nothing for
  create-then-remove / replace-to-same.
- Run the **full** parallel lib suite (`cargo test -p iceberg --lib`), not just the new module's
  filter — added load surfaces latent races elsewhere.
- Prove a metadata action end-to-end by driving its emitted updates through
  `TableMetadataBuilder` (`update.apply(builder)`) — the unbound `apply()` shape skips spec dedup,
  `LAST_ADDED` resolution, and bind-time name checks; only a full catalog commit exercises bind.
- New conflict validation? It needs the no-override tx-captured-start test (docs/testing.md), a
  non-retryable `DataInvalid` (+ `!retryable()` assertion), and a REAL concurrent commit through the
  catalog between txn-build and txn-commit.

### Escalate to

- On-disk format questions → `spec/` (source of truth for serialization).
- Interop failures → [dev/java-interop/map.md#debug](../../../../dev/java-interop/map.md#debug).
